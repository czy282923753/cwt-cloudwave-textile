#!/bin/sh
. "$(dirname -- "$0")/common.sh"
init_backup
lock_backup
postgres_tools
case "${1:-}" in
  daily) node "$backup_bin/files.mjs" retain-daily "${2:-}" ;;
  weekly)
    restic_repository
    node "$backup_bin/weekly.mjs" retain
    ;;
  *) refuse retention-kind ;;
esac
