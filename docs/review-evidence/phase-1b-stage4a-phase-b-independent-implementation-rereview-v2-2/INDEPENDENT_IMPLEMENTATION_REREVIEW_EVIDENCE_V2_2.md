# Independent Implementation Re-review Evidence V2.2

## 1. Scope and isolation

This package supports the Fresh independent implementation re-review of exact Candidate `b7ad96b24da45de00cae2cdb961a9aefcbc99496`. It is review evidence, not Candidate implementation, remediation, acceptance, merge authority, or authorization for a later Phase.

- Formal Candidate: `/Users/calvin/.codex/worktrees/07a1/CWT（CloudWave Textile）项目`.
- Detached exact snapshots: `/tmp/cwt-imp2-v2-review.ExCyn2/candidate`, `baseline`, `lifecycle`, and `full2`.
- Reviewer worktree: `/Users/calvin/.codex/worktrees/2a07/CWT（CloudWave Textile）项目`.
- Pinned runtime: Node `24.14.0`; pnpm `11.9.0`; TypeScript `5.9.3`; Vitest `4.1.10`; Next `16.2.12`.
- Installed dependencies were copied into disposable snapshots by filesystem copy-on-write only. No install, package download, registry access, Provider/API, credential, network, Staging/Production, Deploy, Publish, Index, formal import, merge, Push, or Phase C/D/E action occurred.

The exact Candidate and formal worktree remained clean. Reviewer-only source changes existed in disposable snapshots only.

## 2. Identity and immutable inputs

Independent Git/object checks established:

- exact ref/HEAD/parent/code HEAD/failed V1/Design start;
- one linear remediation history and documentation-only successors after code HEAD;
- earlier failed V1/V2/V3 implementations are non-ancestors;
- frozen tag peels to `31c0e405acfdd0d05200d0fb2531e897a541a2c4`;
- failed-V1-to-final scope is 42 paths = 40 payload entries plus manifest and aggregate, with zero deletions;
- 40/40 Candidate payload manifest and prior FAIL 4/4 manifest pass;
- Schema, Migration, snapshot, journal, seed, dependency set, and lockfile remediation delta is zero;
- public blob identity is 4/4 and pagination helper is absent at both Design start and Candidate.

Global diff-check exits 2 with exactly the two immutable diagnostics required by the review instruction. Owned-scope diff-check exits 0.

## 3. Fresh causal probes

### 3.1 NH01

The real availability service was exercised against disposable PGlite Product Draft, Content Draft, Product Revision, Content Revision, and missing Revision targets. Admin and matching editors reached the next safe Phase B result. Cross-role actors received `authorization_denied`. Existing/wrong-version/missing cases did not disclose Revision existence to the wrong role. Static inspection confirms one target select per union branch, no lock, and no second authorization policy.

Disposition: **CLOSED after correction attempt 1**.

### 3.2 M01

The exact old `1111/2222/3333/version99` vector is now rejected. Fresh target-substitution used:

- target: Editorial Revision `5b1890ce-bc48-4171-9a61-a407cc8c26e9`, version 7;
- selector/returned Product: `22222222-2222-4222-8222-222222222222`;
- returned/current Product version: 5/5;
- returned binding: correct Revision ID/version;
- value: safe `fabricStyle="plain weave"`.

The real central context policy returned `ok:true`. The DTO can prove the Revision tuple and Product selector tuple separately, but it contains no authoritative Revision `entity_type`/`entity_id` link between them.

Disposition: **OPEN after correction attempt 2**.

### 3.3 M02

The exact old suffix family is now rejected. The Fresh vector used 30 individually valid Product Description paragraph blocks:

`Repeated plain <alpha..dahlia> weave narrative.`

Each block cited the allowed `src_01:fabricStyle` ref. All 30 individual controls passed, and the combined result also returned `ok:true`. The family changes one middle token, outside the implementation's two-token prefix/suffix removal.

Disposition: **OPEN after correction attempt 2**.

### 3.4 M04

The prior real `src/app/page.tsx` non-foldable conditional `import()` fault now exits 1 with `fail_closed_unsupported_acquisition` and exact path/rule/AST/acquisition diagnostics.

A new untracked, uniquely classified `business-consumer` CommonJS file destructured `module.require` to `load` and invoked it with a non-foldable choice containing `@/server/ai/phase-b-composition`. The exact checker exited 0. `collectAcquisitions` tracks identifier initializers and direct/property calls, but does not turn an object-binding pattern into an acquisition origin.

Disposition: **OPEN after correction attempt 2**.

### 3.5 New snapshot-hash/classifier root

The strict context classifier scans `association.snapshotHash` as an ordinary string value. A deterministic corpus generated 1,000 valid version-4 UUIDs from SHA-256 seeds, prepared and authorized each exact Revision association, then classified its 64-hex snapshot hash under the pinned runtime and selected registry.

- allowed: 410;
- protected match: 590;
- unsupported/invalid control: 0;
- all 590 matches: `value.personal-phone.structured.v2`.

First deterministic match:

- seed index 1;
- Revision ID `b534aed1-c661-4d8f-84cb-38c12157c622`;
- hash `b574a4fc557400009c7cc935adad4ceda3b1dbaa7f07fd35972605390f06dafc`.

The failure is reproducible in the full context at path `/association/snapshotHash`. It is a new root, not the A-08 repetition root.

## 4. Verification matrix

| Check | Independent result |
|---|---|
| fixed hashes / manifests | 40/40 and prior 4/4 PASS; aggregate and deletion record exact |
| focused AI | 13 files / 149 tests PASS |
| M02 registry | 46/46 PASS |
| raw JSON + output | 2 files / 47 tests PASS |
| lint | PASS, zero warnings |
| strict typecheck | PASS |
| full suite | 111 files / 566 tests PASS in the final clean installed-dependency snapshot |
| accepted DB verifier | PASS, 40 historical artifacts and exact scope |
| independent Schema mapping | 21/21 `ai_model_config`; 96/96 `ai_runs`; exact order across Design/snapshot/Drizzle/Migration |
| Prompt bundle/history | PASS; Production manifest empty; history `c6f9714.....111301a...` |
| architecture baseline | final docs HEAD 528 candidates / 446 executables / 12 classes / zero unclassified or ambiguous |
| type/graph/mutation gates | 2 positive / 6 negative; 19 declared graph faults; 28 lifecycle mutations PASS |
| official Next lifecycle | absent 528/446; present 529/447; `next-env.d.ts` 247 bytes / `7b550d...`; two runs stable |
| server bundle | webpack PASS; 51 server files / 16 client chunks; marker/non-leak controls PASS |
| isolated migrated/noindex | memory-only through 0020; test/feature false/noindex true; public Index false; zero config/run rows |
| public identity | four exact blobs PASS; pagination helper absent |
| diff-check | global exact two immutable diagnostics; owned scope exit 0 |
| Candidate state | exact final HEAD, clean |

The full suite was first attempted using a local dependency copy whose peer symlink pointed to an expired prior disposable worktree; DOM matcher/module-resolution failures resulted before a complete suite verdict. A second already-installed local dependency tree resolved both DOM and `saxes` controls, and the final full suite passed. No installation or download was used. The invalid local dependency attempts are environment/process evidence, not Candidate failures.

## 5. Proportional exclusions and process exceptions

- No full public-site build is claimed. The exact Design-start Google Font path remains unchanged and is network-dependent; Phase B server isolation was proved with the authorized local fixture instead.
- No real Provider, Staging/Production, or PostgreSQL operational behavior was exercised.
- Two direct pnpm invocations aborted before dependency mutation because pnpm requested a dependency-directory purge. They caused no materialization, download, registry/network access, lock change, or Candidate change. All successful checks used direct pinned local binaries.
- One initial Next Turbopack fixture command failed because the fixture package was outside the inferred root. The explicit webpack fixture command required by the checked evidence then passed without network.
- Two Prompt-history invocations used incorrect flag spelling before the exact `--base=... --candidate=...` invocation passed. No repository state changed.

## 6. Evidence files

- `REVIEWER_FRESH_CHALLENGES_V2_2.patch` contains the exact Fresh M01/M02 test and M04 CommonJS fault source used in the disposable Candidate.
- `REVIEWER_FRESH_VERIFICATION_OUTPUT_V2_2.txt` records consolidated command outcomes, vectors, counts, and dispositions.
- `SHA256SUMS.txt` protects this evidence narrative, the challenge patch, consolidated output, and independent report.
