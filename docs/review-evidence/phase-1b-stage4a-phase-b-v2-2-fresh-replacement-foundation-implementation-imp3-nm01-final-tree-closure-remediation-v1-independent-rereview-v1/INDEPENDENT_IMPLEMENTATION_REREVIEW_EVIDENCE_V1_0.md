# IMP3-NM01 Final-Tree Closure Remediation V1 — Fresh Independent Re-review Evidence

## Review boundary

The exact reviewed Candidate is `b48f33f35d5d46824a8c4dec6b40d4a093050285` on the required full ref. The formal worktree was read-only and clean. All mutations were made in disposable detached worktrees. The executable-tree seal is `87966f118766b60aeacc51ceca61c68ea57a62cf`; every successor is a regular `100644` non-executable documentation, JSON, text, or manifest path.

Node `24.14.0` and TypeScript `5.9.3` were used through already-installed, read-only dependency links. No package manager, install, materialization, download, registry, network, Provider/API/credential, external environment, merge, Push, or phase-advance action occurred.

## Identity and supplied evidence

- Required ref, HEAD, parent, tree, seal parent/tree, failed ref, three inherited checkpoints, and frozen Tag object/peel matched exactly.
- The Candidate and a new detached exact-HEAD snapshot were clean.
- The sole Product-code delta from the failed Candidate is `scripts/verify-ai-architecture.ts`; no Schema, Migration, snapshot, journal, seed, package, lock, runtime business, Product, public, SEO, or URL file changed.
- Author report, authority, verifier, checker, immutable historical probe, controlling FAIL report/manifest, and all `20/20` manifest entries matched their fixed SHA-256 values.
- The five V3.1 proofs are canonical, their `proofHash` values independently recompute, all bind seal `87966f1...`, and all bind executable-tree SHA-256 `8c9579d3...`.
- The independently reconstructed executable membership is 477 nodes and recomputes the declared executable-tree SHA-256 exactly.

## Prior failure reproduction

The exact failed Candidate `10de3daf...` was run from both attached-equivalent and detached exact snapshots. Both real gates rejected the immutable historical Reviewer probe with `unresolved_static_edge`. This independently confirms the causal starting point.

## Exact Candidate baseline

The author verifier was Fresh-run from an independently attached local snapshot. It generated official Next type state under network denial, ran the real proof-bound checker attached and detached, and returned PASS with the exact final HEAD, seal, executable-tree hash, 20 entries, and five proof artifacts.

The real proof-bound checker independently returned:

- 657 actual final lifecycle candidates and 477 executables;
- zero unclassified and zero ambiguous nodes;
- zero current unresolved edges, 2,270 graph edges, 21 ordinary URL values;
- 69/69 graph/topology faults rejected, 10/10 positives accepted;
- 28/28 inherited mutations and 6/6 final-tree mutations rejected;
- 2 positive and 6 expected-negative type seams;
- identical executable-tree SHA-256 `8c9579d3...`.

The author capture's `654` count describes the 649-node proof source plus five proof JSON files, not the exact final lifecycle tree. The final tree adds other diagnostic documentation, and the real final gate reports 657. The evidence verifier does not assert that total. This is retained as evidence-fidelity support for the consolidated root rather than split into a second finding.

## Decisive Fresh IMP3-NM01 challenge

The corrected checker defines every `diagnostic-documentation` executable as outside the current executable graph and skips all of its acquisitions. The immutable probe is correctly tracked, byte-identical, in exactly that class, `evidence_only_not_production`, documentation-only, physically inventoried, and excluded once from outgoing current graph edges.

The Fresh challenge then added one ordinary static import from the existing Production node `src/storage/index.ts` to that exact diagnostic probe and ran the real checker without a pre-existing seal, matching the stage at which a proof would be generated. The checker exited `0`.

The cause is one-sided isolation: `enforceCapabilityEdge` rejects Production imports of synthetic/test nodes, but it has no equivalent rejection for `diagnostic-documentation`. The target probe's own historically invalid import is then suppressed by the diagnostic-source skip. Thus a Production/current node can reach evidence-only executable code while the sole architecture gate reports success.

Controls demonstrate that this is bounded, not a broad checker collapse:

- an unresolved Production target rejects with `unresolved_static_edge`;
- a post-seal executable rejects as executable-tree drift;
- a `docs/docs` compatibility target rejects;
- stale proof binding rejects;
- symlink and hard-link aliases reject;
- the exact proof-bound baseline passes.

This is the same IMP3-NM01 evidence-only/current-graph separation root. It is correction attempt 1 and remains OPEN. Attempt 2 remains available; three-strike escalation is not triggered.

## REMEDIATION_FINDINGS_REVIEW

| Boundary | Disposition | Independent basis |
| --- | --- | --- |
| IMP3-NM01 attempt 1 | **FAIL / OPEN** | Real gate accepts Production/current import of diagnostic evidence; final total is 657, not the supplied 654 exact-final expectation. |
| H-01 / M04 attempt 3 | **PASS / CLOSED** | Real checker retained 69 origin/graph faults, including ambient/non-emitting/global capability families, plus 10 positives; no H-01 weakening was found. |
| H-02 / NH01 attempt 1 | **PASS / CLOSED** | Fresh disposable PGlite real-service matrix: 2 files / 7 tests; missing, malformed, wrong-role/type/version remain non-disclosing with one read and zero lock. |
| Owner DB convergence | **PASS / CLOSED** | Exact `globalThis.cwtDatabaseConnection` member/path remains the sole exception; DB cache and architecture wrong-member/path cases pass. |
| M02 replacement | **PASS / CLOSED** | Product source bytes are unchanged; all-AI 14/163 includes the single registry, Unicode/counter and A-07/A-08 suites. |
| NM01 replacement | **PASS / CLOSED** | Product source bytes are unchanged; context-integrity and claimed-execution coverage passes within all-AI. |
| M01 reconstruction | **PASS / CLOSED** | Product source bytes are unchanged; context/provenance/binding suites pass within all-AI. |
| M03 | **PASS / CLOSED** | 2 positive / 6 negative type seams and strict typecheck pass. |
| M05 | **PASS / CLOSED** | Public/Product/SEO files are unchanged; focused frozen tests pass. |
| L01 | **PASS / CLOSED** | Production outer composition still exports availability only; no request/enqueue/Worker runtime is exposed. |
| L02 | **FAIL, same IMP3-NM01 root** | Exact-final evidence count is not asserted and the proof boundary permits the Production-to-evidence edge above. No separate finding is created. |
| Provider/Prompt/security/public/SEO/URL/Schema/Migration/package/lock/phase | **PASS / CLOSED** | Exact-empty Production Prompt and Provider registries, 21/21+96/96 mapping, Prompt/history, isolated bundle, and zero relevant diff all pass. |

## Mandatory non-regression verification

- all `src/ai`: 14 files / 163 tests PASS;
- Fresh H-02 real-service matrix: 2 files / 7 tests PASS;
- affected DB/public/SEO focus: 5 files / 12 tests PASS;
- lint with zero warnings and strict typecheck PASS;
- AI foundation verifier PASS with 40 historical artifacts;
- independent accepted profile → Drizzle → Migration extraction: `ai_model_config` 21/21/21 and `ai_runs` 96/96/96 in exact order;
- Prompt bundle and history from `9aa9735...` to exact final HEAD PASS;
- Production Prompt manifest and Provider registry exact-empty;
- official Next typegen PASS, `next-env.d.ts` 247 bytes with the fixed hash;
- isolated network-denied server/public bundle PASS: 51 server files, 16 client chunks, marker and positive-leak controls PASS.

## FULL_REVIEW_NECESSITY

**NOT_REQUIRED.** The sole Product-code delta is the architecture checker. The complete remediation series, every reasonably affected closure, and all mandatory frozen-boundary gates were reviewed first. A deterministic blocking failure was reproduced inside that exact boundary; an unrelated exhaustive application suite cannot resolve or reclassify it. The already-run all-AI, affected authorization/DB/public tests, lint, typecheck, DB, Prompt, typegen, and isolated bundle gates are proportionate and sufficient for the remaining non-regression judgment.

## Final disposition

Conclusion: **FAIL**. Counts: Blocker 0, High 0, Medium 1, Low 0, External Validation 0. No Schema, Migration, ADR, dependency, Complexity Approval, Product, public, SEO, URL, or Owner decision is required to state the finding. Exact Candidate is not Phase B implementation-gate eligible.
