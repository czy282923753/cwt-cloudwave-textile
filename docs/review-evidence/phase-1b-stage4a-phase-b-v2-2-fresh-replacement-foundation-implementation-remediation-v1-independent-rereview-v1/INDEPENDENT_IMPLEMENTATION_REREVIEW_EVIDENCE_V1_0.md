# Independent Implementation Re-review Evidence V1.0

## Subject and method

This package records the Fresh, read-only re-review of exact Candidate `49f789a9e4c5c7f2a3dbd7eaddba17e6a248832b` on `refs/heads/codex/phase-1b-stage4a-phase-b-v2-2-fresh-replacement-implementation-remediation-v1`. The review used a detached exact snapshot at `/tmp/cwt-v22-remediation-review.S2PIq6`. The formal Candidate and disposable snapshot ended clean.

The Owner-ordered sequence was followed: all repaired and preserved-closed boundaries were independently disposed under `REMEDIATION_FINDINGS_REVIEW`; only then was `FULL_REVIEW_NECESSITY` decided. Mandatory identity, supplied-evidence, frozen Schema/Prompt/Provider/security and public-boundary checks were completed regardless.

## REMEDIATION_FINDINGS_REVIEW

| Boundary | Result | Evidence |
| --- | --- | --- |
| H-01 / M04 exact global capability/member/path boundary | **FAIL / OPEN** | The exact `globalThis.cwtDatabaseConnection` exception is narrow, and five Fresh explicit-global variants failed closed. A Fresh protected-source `fetch("https://api.deepseek.com")` fault nevertheless passed the real sole gate with exit 0. |
| H-02 / NH01 authorization order/no-existence leak | **PASS / CLOSED** | Fresh real-service PGlite probe makes missing, malformed, wrong-role and unrelated Revision records indistinguishable before scope; authorized Product Editor and Admin see only their permitted downstream result. |
| Owner DB convergence | **PASS / preserved** | Only direct named `globalThis.cwtDatabaseConnection` remains in exact `src/db/client.ts`; cache tests pass; no broader DB/config/Schema change. |
| M02 replacement | **PASS / preserved** | Production M02 sources are outside the four-path delta; all AI/security/output tests pass and no second classifier was introduced. |
| M04 replacement | **FAIL / OPEN through H-01** | Same causal root as H-01: the static capability-origin language is not closed over equivalent ambient global spellings. |
| NM01 replacement | **PASS / preserved** | No changed production NM01 path; all AI context/error/order tests pass; no second mutable/raw context authority found. |
| M01 reconstruction | **PASS / preserved** | No changed M01 production path; source/target/provenance/Prompt-variable and durable reconstruction suites pass. |
| NH01 reconstruction | **PASS / CLOSED through H-02** | Actual service matrix and Fresh probe pass; exactly one select per target-kind branch, zero lock, one authorization authority. |
| M03 | **PASS / preserved** | Select-only seam unchanged; 2 positive and 6 expected-negative type probes pass. |
| M05 | **PASS / preserved** | Four public blobs are byte-identical to accepted checkpoint; pagination helper absent at both. |
| L01 | **PASS / preserved** | Production surface remains availability-only; no request/enqueue/Worker runtime. |
| L02 | **PASS / preserved** | Evidence binds code HEAD, final docs successor and exact hashes honestly. |

## Decisive H-01 evidence

The current checker explicitly rejects every identifier named `globalThis` unless it is exact direct, non-optional `globalThis.cwtDatabaseConnection` in exact `src/db/client.ts`. Fresh wrong-member, computed-member, destructuring, class/static and optional-access variants failed closed.

That rule does not close the equivalent ambient-global capability origin. The reviewer inserted only the patch in `REVIEWER_H01_AMBIENT_FETCH_FAULT_V1_0.patch` into protected Production AI source. The real gate returned `exit=0`, `ok=true`. Node 24's ambient `fetch` therefore provides a direct network/Provider-capability spelling outside the claimed fail-closed origin language. No bare `fetch`, `WebSocket` or corresponding resolved-global-symbol rule exists in the sole checker.

The complete JSON raw runs were held outside the Candidate and hashed in `REVIEWER_H01_AMBIENT_FETCH_GATE_RESULT_V1_0.txt`. The mutation was restored before any baseline/final-clean assertion.

## Decisive H-02 evidence

`REVIEWER_H02_AUTHORIZATION_ORDER_PROBE_V1_0.ts` ran against an actual migrated disposable PGlite database and the actual `createPhaseBAvailabilityServiceV1` service. It independently used a mixed-case malformed authoritative entity type, non-English locale, applied status and unrelated version. Unauthorized Product/Content actors received the same `authorization_denied` result as missing records; an authorized Product Editor received `target_version_conflict` only for the authoritative Product Revision; Admin received `target_scope_mismatch` only after authorization.

Static inspection confirms one select in the Revision branch and no lock. An attempted driver-level spy did not observe Drizzle's bound query call and was discarded rather than misreported; read-count disposition rests on the literal single statement plus the real-service behavior.

## FULL_REVIEW_NECESSITY

**NOT_REQUIRED.**

The remediation is a bounded four-path code delta. The complete closure review above found one decisive High same-root residual, so the Candidate is already ineligible. Identity, evidence integrity, all AI tests, Fresh security probes, lint, strict typing, DB/0020/Prompt gates, architecture baseline/type/fault/mutation gates and public bytes were nevertheless completed. An unrelated exhaustive application suite or optional full public build cannot change H-01's result and would not be proportional.

No unrelated full application test/build PASS is claimed.

## Process and external validation

- No package manager, install, dependency materialization, download, registry, network, Provider/API/credential, Staging/Production, Deploy, Publish, Index, merge or Push operation was performed.
- Pre-existing accepted V3.1 `tsconfig` metadata mismatch remains disclosed evidence/profile debt.
- The unchanged Google-font offline full-public-build debt remains external/local-environment debt. Public bytes are unchanged and no full-public-build PASS is claimed.

