# Independent Implementation Re-review Evidence V1.0

## Review subject and sequencing

This package reviews exact Candidate `d41c9c4bd496f4c3a612c6ac88cc5acf6ae83308` on `refs/heads/codex/phase-1b-stage4a-phase-b-v2-2-fresh-replacement-implementation-h01-m04-remediation-v2`. Candidate inspection and adversarial gates used detached exact snapshot `/tmp/cwt-h01-m04-v2-review.n05h23`; the formal worktree remained attached to the exact ref. Both ended clean.

The Owner-ordered sequence was followed. Every remediated, reasonably affected and explicitly preserved-closed item was independently disposed under `REMEDIATION_FINDINGS_REVIEW`. Only afterward was `FULL_REVIEW_NECESSITY` decided.

## REMEDIATION_FINDINGS_REVIEW

| Boundary | Disposition | Independent basis |
| --- | --- | --- |
| H-01/M04 correction attempt 2 | **FAIL / OPEN** | Literal ambient `fetch`, exact DB exception and real runtime-local shadows behave correctly, but non-emitting repository `declare` declarations disguise ambient `fetch`, `WebSocket` and `window` origins; the real gate exits 0. |
| H-02/NH01 after attempt 1 | **PASS / CLOSED** | Byte-identical actual PGlite Reviewer probe reran 1/1; all AI integration tests pass; no availability code changed. |
| Owner DB convergence | **PASS / preserved** | Exact direct named non-optional `globalThis.cwtDatabaseConnection` remains only in `src/db/client.ts`; wrong-path Fresh probe fails and cache tests pass. |
| M02 replacement | **PASS / preserved** | Production M02 authority is byte-identical; all AI/security/output tests pass; no second classifier added. |
| NM01 replacement | **PASS / preserved** | Production context/error/order authority is byte-identical; all AI tests pass; no raw or parallel authority added. |
| M01 reconstruction | **PASS / preserved** | Source/target/provenance/Prompt-variable production paths are byte-identical and focused AI tests pass. |
| M03 | **PASS / preserved** | Type gate remains 2 positive / 6 expected-negative; no select-only seam change. |
| M05 | **PASS / preserved** | Four public blobs are byte-identical and pagination helper remains absent. |
| L01 | **PASS / preserved** | Production remains availability-only; no request/enqueue/Worker surface. |
| L02 | **PASS / preserved** | Exact code head/docs-only successor and all supplied hashes are honest; attached evidence verifier passes. |
| Frozen Provider/Prompt/security/phase boundary | **PASS / preserved** | Provider registry and Production Prompt manifest exact-empty; no Phase C/D/E, Provider, network, fallback/RAG/vision/customer-support runtime added. |
| Schema/Migration/package/lock | **PASS / preserved** | Zero path change; verifier and Fresh extraction remain 21/21 and 96/96 exact order. |
| Public/SEO/URL/Product/data | **PASS / preserved** | Zero path delta and focused public tests pass. |

## H-01 causal evidence

The remediation correctly closes the previous literal witness. It also rejects a wrong-path use of the DB member and allows a real function-parameter shadow named `fetch`.

The new `ambientRuntimeCapabilityOrigin` treats any symbol with a repository declaration as a safe local declaration. TypeScript `declare` declarations in a repository `.ts` file satisfy that test but emit no runtime binding. The runtime expression therefore still resolves to the ambient global capability. The combined patch in `REVIEWER_H01_NON_EMITTING_DECLARE_BYPASS_V1_0.patch` passed the real sole gate with exit 0 for ambient `fetch`, `WebSocket` and `window.fetch` calls.

This is the same H-01/M04 semantic-origin root: the checker still equates a TypeScript symbol declaration with a concrete runtime-local origin. It is not a new feature or unrelated root.

## H-02 and preserved closures

The prior Reviewer service probe is part of the byte-identical controlling FAIL package. A disposable test entry imported and reran that exact probe: missing, malformed, wrong-role and unrelated Revision states remain indistinguishable before authorization; authorized Product Editor and Admin results remain correctly separated. Literal code inspection still shows one read and zero lock in availability.

All `src/ai` tests, selected DB/cache/schema/noindex/public tests, lint, strict typing, 0020, Prompt bundle/history and public-byte checks passed. The five author proof artifacts were hash-verified and inspected for Phase D/adapter absence, M03 type seams, public/client closure and positive leak control.

## FULL_REVIEW_NECESSITY

**NOT_REQUIRED.**

The correction changes only the non-runtime architecture checker and its sole profile fixture. The complete remediation-closure review found a decisive High same-root residual, already making the exact Candidate ineligible. Mandatory identity, manifests, security, all AI tests, type, Schema, Prompt, graph, bundle and public-boundary checks were completed. An unrelated exhaustive application suite would not exercise the semantic checker further or change the finding.

No unrelated full-suite or full-public-build PASS is claimed.

## Process notes

- The author evidence verifier requires branch attachment, so it was rerun in the exact clean formal worktree; it passed. All actual review inspection and adversarial gates used the detached exact snapshot.
- A temporary root `node_modules` symlink to already-installed Reviewer dependencies was used for test/lint/typecheck only and removed. No package manager, installation, dependency materialization, download, registry or network operation occurred.
- The unchanged accepted V3.1 historical `tsconfig` metadata mismatch and unchanged offline Google-font full-public-build debt remain External Validation items, not new Candidate code findings.

