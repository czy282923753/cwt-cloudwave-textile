CWT Stage 4A Phase D credential/redaction negative proof
Proof-bound code commit: 250b07d928fbdddc5b81c31162c601643fd0ee21

RESULTS
- Sole production credential reader expression in implementation source: 1.
- Reader location: src/integrations/ai/providers/deepseek-text-adapter.ts.
- Controlled runner, CLI, official-source preflight, composition root, repository and evidence writers contain direct credential reader expressions: 0.
- Sole isolated validation database-secret reader expression: 1, inside src/ai/testing/controlled-provider-validation.ts.
- Ordinary, disabled, local/test, PGlite, preflight-failure and Production-empty-registry test paths invoked the credential reader: 0.
- Valid injected preparation invoked the reader exactly once; invalid injected preparation invoked it exactly once and emitted no marker/fetch.
- Repeated execution of a prepared closure emitted no second credential read and no second fetch.
- Credential values, hashes, fingerprints, lengths, prefixes, suffixes and presence booleans in returned errors, durable attempt evidence, terminal projection, snapshots and logs: 0.
- Raw Provider request/response, Prompt/input/context, Authorization value, exception text and reasoning content in durable or projected evidence: 0.
- The historical first authorized invocation stopped before database-secret availability; the renewed single invocation reached the exact database guard and stopped with `isolated_database_guard_failed`. Neither invocation reached the Provider credential reader. Aggregate billable Provider POST count across both invocations: 0.
- Evidence-package scan found no credential value or credential derivative. Only fixed environment-variable identifiers and the literal emitted header name "authorization" are retained where required by the accepted proof contract.

The Provider credential itself was never inspected, printed, logged, persisted, hashed, fingerprinted, placed on a command line or copied into evidence. The renewed database secret was injected only into the controlled child environment; its value was never separately read, printed, logged, persisted, parsed outside the runner, or copied into evidence.

The final provisioning attempt generated its new database role, password, name suffix, port and URL only inside one child process. No identifier, connection value, derivative or fingerprint entered command output, Git, report, evidence or callback. Provisioning stopped before protected registration and before controlled invocation.

The separately authorized retained-resource closure uniquely resolved the exact resource without disclosing its identity, rotated the application credential through process-local stdin, and completed one HBA correction cycle. It stopped before authentication proof and protected registration with `role_semantic_proof_failed`; no resource identifier, credential, URL, port, suffix or derivative entered output or evidence.

The final role-normalization cycle replaced the protected application credential once, proved wrong-password rejection and correct-password SCRAM authentication, and updated only the protected registration. The one final controlled invocation projected only `controlled_validation_internal_failure`, two official GETs and zero billable POSTs. Credential-reader reachability is intentionally not inferred from that safe projection; no Provider or database credential value, presence flag, identifier or derivative entered evidence.
