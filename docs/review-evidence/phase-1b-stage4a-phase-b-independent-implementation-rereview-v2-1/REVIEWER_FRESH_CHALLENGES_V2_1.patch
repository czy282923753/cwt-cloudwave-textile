diff --git a/src/ai/applications/draft-assistance/context.test.ts b/src/ai/applications/draft-assistance/context.test.ts
--- a/src/ai/applications/draft-assistance/context.test.ts
+++ b/src/ai/applications/draft-assistance/context.test.ts
@@
+  it("reviewer challenge: binds Product source identity/version to the selected target", async () => {
+    const selectedSourceId = "22222222-2222-4222-8222-222222222222";
+    const policy = createDraftContextPolicy({
+      async readSelectedSource() {
+        return {
+          ok: true as const,
+          value: {
+            sourceClass: "product_structured" as const,
+            sourceIdentity: {
+              productId: "33333333-3333-4333-8333-333333333333",
+              recordVersion: 99,
+            },
+            fields: [{
+              field: "name",
+              provenance: "structural" as const,
+              value: "SYNTHETIC wrong product",
+            }],
+          },
+        };
+      },
+    });
+    const association = prepareDraftAssociationV1({
+      type: "product_draft",
+      productId: "11111111-1111-4111-8111-111111111111",
+      locale: "en",
+      expectedVersion: 7,
+    });
+    if (!association.ok) throw new Error("fixture association failed");
+    const authorized = buildAuthorizedDraftAssociationV1(association.value);
+    if (!authorized.ok) throw new Error("fixture authorization failed");
+    const built = await withReadOnlyDraftAvailabilityScope(database.db, (scope) =>
+      policy.buildReconstructibleContext({
+        actor: { principalId: "99999999-9999-4999-8999-999999999999", roleKey: "admin" },
+        command: {
+          useCase: "product_description_draft",
+          actor: { userId: "99999999-9999-4999-8999-999999999999", role: "admin" },
+          target: {
+            type: "product_draft",
+            productId: "11111111-1111-4111-8111-111111111111",
+            locale: "en",
+            expectedVersion: 7,
+          },
+          idempotencyKey: "reviewer-source-binding",
+          contextSelections: [{
+            sourceClass: "product_structured",
+            sourceId: selectedSourceId,
+            fields: ["name"],
+          }],
+        },
+        association: authorized.value,
+        scope,
+      }));
+    const encoded = built.ok ? policy.encodePreparedContext(built.value) : built;
+    console.log(JSON.stringify({ built, encoded }));
+    expect(built.ok).toBe(false);
+  });

diff --git a/src/ai/output/common.test.ts b/src/ai/output/common.test.ts
--- a/src/ai/output/common.test.ts
+++ b/src/ai/output/common.test.ts
@@
+  it("reviewer challenge: rejects equivalent prefix spam with cosmetic suffixes", () => {
+    const suffixes = [
+      "alpha", "bravo", "charlie", "delta", "echo", "foxtrot", "golf", "hotel",
+      "india", "juliet", "kilo", "lima", "mike", "maple", "oscar", "papa",
+      "quebec", "romeo", "sierra", "tango", "uniform", "victor", "whiskey", "xray",
+      "yankee", "zulu", "amber", "birch", "cedar", "dahlia",
+    ];
+    const result = output.policy.parseAndProtect({
+      rawObject: {
+        ...productCandidate("A conservative textile narrative."),
+        descriptionBlocks: suffixes.map((suffix) => ({
+          type: "paragraph",
+          text: {
+            text: `Repeated plain weave narrative ${suffix}.`,
+            sourceRefs: ["src_01:fabricStyle"],
+          },
+        })),
+      },
+      context: narrativeContext,
+    });
+    const allIndividualControlsOk = suffixes.every((suffix) => output.policy.parseAndProtect({
+      rawObject: {
+        ...productCandidate("A conservative textile narrative."),
+        descriptionBlocks: [{
+          type: "paragraph",
+          text: {
+            text: `Repeated plain weave narrative ${suffix}.`,
+            sourceRefs: ["src_01:fabricStyle"],
+          },
+        }],
+      },
+      context: narrativeContext,
+    }).ok);
+    console.log(JSON.stringify({ result, allIndividualControlsOk }));
+    expect(result.ok).toBe(false);
+  });

diff --git a/src/ai/provider-neutral-foundation.integration.test.ts b/src/ai/provider-neutral-foundation.integration.test.ts
--- a/src/ai/provider-neutral-foundation.integration.test.ts
+++ b/src/ai/provider-neutral-foundation.integration.test.ts
@@
-  aiRuns,
+  aiRuns,
+  editorialRevisions,
@@
+  it("reviewer challenge: role authorization follows the revision entity type", async () => {
+    const revisionId = "77777777-7777-4777-8777-777777777777";
+    await database.db.insert(editorialRevisions).values({
+      id: revisionId,
+      entityType: "product",
+      entityId: productId,
+      locale: "en",
+      versionNumber: 3,
+      status: "draft",
+      snapshot: { conspicuous: "SYNTHETIC TEST DATA — NOT A CWT FACT" },
+    });
+    const service = createPhaseBAvailabilityServiceV1({
+      database: database.db,
+      trustedEnvironment: { appEnvironment: "test", processFeatureAiEnabled: true },
+    });
+    const target = {
+      type: "editorial_revision" as const,
+      revisionId,
+      expectedVersion: 3,
+    };
+    const productEditor = await service.inspectDraftAssistanceAvailability({
+      useCase: "product_description_draft",
+      actor: { userId: "99999999-9999-4999-8999-999999999999", role: "product_editor" },
+      target,
+      contextSelections: [],
+    });
+    const contentEditor = await service.inspectDraftAssistanceAvailability({
+      useCase: "product_description_draft",
+      actor: { userId: "88888888-8888-4888-8888-888888888888", role: "content_editor" },
+      target,
+      contextSelections: [],
+    });
+    console.log(JSON.stringify({ productEditor, contentEditor }));
+    expect(contentEditor.ok && contentEditor.value.code).toBe("authorization_denied");
+    expect(productEditor.ok && productEditor.value.code).toBe("integration_not_ready");
+  });

diff --git a/src/app/page.tsx b/src/app/page.tsx
--- a/src/app/page.tsx
+++ b/src/app/page.tsx
@@
 export const revalidate = 3600;
 
+const reviewerComputedServerEdge = process.env.REVIEWER_AI_EDGE === "1"
+  ? "@/server/ai/phase-b-composition"
+  : "@/public-site/data";
+void import(reviewerComputedServerEdge);
+
 export async function generateMetadata(): Promise<Metadata> {
