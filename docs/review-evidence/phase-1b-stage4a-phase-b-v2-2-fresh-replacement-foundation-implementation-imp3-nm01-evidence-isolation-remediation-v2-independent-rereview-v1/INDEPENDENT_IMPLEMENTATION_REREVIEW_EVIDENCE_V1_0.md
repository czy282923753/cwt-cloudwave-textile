# IMP3-NM01 Evidence-Isolation Remediation V2 — Fresh Independent Re-review Evidence

## Boundary and process

The exact reviewed Candidate is `d60d5cc2398eeeb06263c58924896250f12f3756` on the required full ref. The formal Candidate remained attached and clean. Reviewer mutations ran only in disposable detached worktrees; a second clean exact-HEAD snapshot confirmed final identity and cleanliness.

Node `24.14.0`, TypeScript `5.9.3`, Next `16.2.12` and `tsx` `4.23.1` came from already-installed read-only dependencies. No package manager, install, materialization, download, registry, external network, Provider/API/credential, merge, Push, or phase-advance action occurred.

## Identity, history and supplied evidence

- Required ref, HEAD, parent, tree, seal, seal parent/tree, failed attempt-1 ref, three inherited checkpoints and frozen Tag matched exactly.
- The sole Product-code mutation is `scripts/verify-ai-architecture.ts`, SHA-256 `89fc60805358a3aa6e7597e68fd7607e268ea5169e39ff170c970b8ac7119a3b`.
- Every post-seal successor is a regular mode-`100644` non-executable docs/JSON/text/manifest file.
- Candidate report, authority, verifier, `20/20` manifest, controlling FAIL package and immutable historical probe matched the supplied SHA-256 values.
- No Schema, Migration, snapshot, journal, seed, package, lock, Product, public, SEO or URL path changed.
- The five V3.1 proof files independently parse, their canonical `proofHash` values recompute, and all bind seal `627779d...` plus executable-tree SHA-256 `a45c8cb0...`.

The author verifier was Fresh-run in a local attached clone under network denial. Two initial isolation-only exceptions were recorded: a detached snapshot could not satisfy its symbolic-ref assertion, and a local clone initially lacked the failed-ref name. After supplying the exact local ref without changing Candidate bytes, attached and detached verification passed. These were Reviewer process-context exceptions, not Candidate findings.

## Attempt-1 reproduction and corrected common path

The exact attempt-1 Candidate `b48f33f...` accepts the standard Production `src/storage/index.ts` import of the immutable diagnostic probe. This reproduces the one-sided boundary.

The attempt-2 checker adds a common Production target-class ceiling before the source-specific restrictions. It correctly rejects runtime and type-only imports/re-exports from `other-production-src`, `protected-ai`, `phase-b-outer-composition` and `business-consumer`, including a transitive helper. It also rejects visible and ignored untracked evidence nodes with the exact source/target class, stage, zone, rule, AST and edge diagnostics. Evidence-side review observation of Product remains accepted.

## Decisive Fresh physical-identity challenge

The real checker was challenged with two case-variant spellings on the review host's case-insensitive filesystem:

1. A Production import of the immutable executable evidence probe changed only `REVIEWER_H02...` to `reviewer_H02...`.
2. A Production import of the current `IMP3_NM01_EVIDENCE_ISOLATION_AUTHORITY_V2_0.json` changed the filename to lowercase.

For both, `stat` reports identical device/inode pairs for canonical and variant paths. `realpathSync` returns strings retaining the requested casing. The actual-tree lookup is case-sensitive, so the variant does not recover the target node and the Production target-class ceiling returns without rejecting. The real architecture gate exits `0` in both cases. The current JSON control also passes strict TypeScript, proving that historical probe content is not the cause.

The independent challenge source executes the real sole checker for every case; it does not call an extracted helper. Final result: 23 cases, 17 expected negatives, 5 legitimate Candidate positives, the prior unsafe PASS, and two unsafe Candidate acceptances. The complete machine-readable capture is included in this evidence package.

Controls reject canonical direct/re-export/type-only/transitive evidence edges, untracked/ignored targets, symlink/hard-link aliases, post-seal executable drift, stale proof binding, a `docs/docs` compatibility shim and silent historical-probe removal. Thus the defect is a physical-identity canonicalization gap at the same incoming evidence-isolation root, not a general graph collapse.

## REMEDIATION_FINDINGS_REVIEW

| Boundary | Disposition | Independent basis |
| --- | --- | --- |
| IMP3-NM01 attempt 2 | **FAIL / OPEN** | Two case-variant same-device/inode evidence targets pass the real common edge authority; one also passes strict TypeScript. |
| H-01 / M04 attempt 3 | **PASS / CLOSED** | Fresh ambient `fetch` alias rejects with capability diagnostics; runtime-emitted local shadow passes; 69 faults / 10 positives remain. |
| H-02 / NH01 attempt 1 | **PASS / CLOSED** | Fresh disposable PGlite service matrix: 2 files / 7 tests; non-disclosing one-read/zero-lock behavior retained. |
| Owner DB convergence | **PASS / CLOSED** | Exact named member and exact `src/db/client.ts` exception unchanged; wrong global/member/path cases remain rejected. |
| M02 replacement | **PASS / CLOSED** | Product bytes unchanged; all-AI security, Unicode, counters and A-07/A-08 coverage pass. |
| NM01 replacement | **PASS / CLOSED** | Product bytes unchanged; detached compiled context and claimed-execution suites pass. |
| M01 reconstruction | **PASS / CLOSED** | Product bytes unchanged; binding, provenance and Prompt-variable suites pass. |
| M03 | **PASS / CLOSED** | Strict typecheck and 2-positive/6-negative select-only seams pass. |
| M05 | **PASS / CLOSED** | Frozen public/Product/SEO files and focused tests pass. |
| L01 | **PASS / CLOSED** | Production composition remains availability-only. |
| L02 | **FAIL, same IMP3-NM01 effect** | The final proof boundary accepts the case-variant physical alias; not a separate root. |
| Frozen Provider/Prompt/security/public/SEO/URL/Schema/Migration/package/lock/phase | **PASS / CLOSED** | Exact-empty registries, mappings, Prompt/history, bundle isolation and zero relevant diff pass. |

## Mandatory verification

- source-clean architecture: 668 candidates / 478 executables / 2,269 edges;
- official-present architecture: 669 / 479 / 2,270;
- zero unclassified, ambiguous and current-unresolved; 21 ordinary URL values;
- 69/69 graph/topology faults, 10/10 positives, inherited 28/28 and closure 12/12 mutations;
- five canonical proof files and attached/detached executable identities PASS;
- all AI: 14 files / 163 tests;
- Fresh H-02 service matrix: 2 files / 7 tests;
- affected DB/public/SEO focus: 5 files / 7 tests;
- lint and strict typecheck PASS;
- DB verifier PASS, with independent 21/21/21 and 96/96/96 exact-order extraction;
- Prompt bundle/history and exact-empty Production Prompt/Provider PASS;
- official typegen PASS;
- isolated network-denied server/public bundle PASS: 51 server files / 16 client chunks.

The first isolated bundle attempt used the default Turbopack workspace-root inference and failed before compilation because the disposable dependency link sat outside the fixture root. A rerun with the same read-only dependencies and explicit webpack mode passed under network denial. No Candidate byte changed and this is a process exception, not a Candidate finding.

The final frozen-boundary probe itself returned its complete PASS payload. Its disposable wrapper then attempted to assign zsh's read-only `status` name while preparing cleanup; the already-created read-only dependency link and worktree were immediately removed explicitly. This occurred after the probe result and did not affect the Candidate or its evidence.

## FULL_REVIEW_NECESSITY

**NOT_REQUIRED.** All remediation claims and reasonably affected preserved closures were disposed before this decision. The only Product-code delta is the checker, every mandatory frozen boundary was verified, and the same-root acceptance defect is deterministic. Unrelated exhaustive application testing would not change the FAIL. The all-AI and affected/frozen gates are proportionate.

## Final disposition

Conclusion: **FAIL**. Counts: Blocker 0, High 0, Medium 1, Low 0, External Validation 0. IMP3-NM01 remains OPEN after correction attempt 2; only ordinary attempt 3 remains and three-strike escalation is not triggered. No Schema, Migration, ADR, dependency, Complexity Approval, Product, public, SEO, URL, data, or Owner decision impact exists.
