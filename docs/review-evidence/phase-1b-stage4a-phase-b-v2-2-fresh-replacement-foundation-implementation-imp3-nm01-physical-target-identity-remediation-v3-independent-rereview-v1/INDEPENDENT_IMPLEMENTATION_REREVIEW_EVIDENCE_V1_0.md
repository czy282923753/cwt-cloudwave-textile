# IMP3-NM01 Physical Target Identity Remediation V3 — Fresh Independent Re-review Evidence

## Boundary and process

The exact reviewed Candidate is `4a053c0fa9449588e88f2b8519e74e08b1b59956` on the required full ref. The formal Candidate remained attached and clean. Reviewer mutations ran only in disposable detached worktrees and a disposable local clone; no Candidate byte was changed.

Pinned Node `24.14.0`, TypeScript `5.9.3`, Next `16.2.12` and `tsx` `4.23.1` came from already-installed read-only dependencies. No package manager, install, materialization, download, registry, external network, Provider/API/credential, merge, Push, or phase-advance action occurred.

## Identity, scope and supplied evidence

- Required ref, HEAD, parent/seal, final tree, seal parent/tree, failed attempt-2 ref, inherited checkpoints and frozen Tag matched exactly.
- The only Product-code path is `scripts/verify-ai-architecture.ts`, SHA-256 `70b66c3351a1b33bac0d3dacfd119c639424fd063a85ee00c9db59c18c1955a5`.
- All ten post-seal successors are regular mode-`100644` non-executable docs/JSON/text/manifest files.
- Candidate report, authority, verifier and `22/22` manifest matched fixed hashes. The controlling FAIL package verified `7/7` byte-identically.
- No source outside the checker, Schema, Migration, snapshot, journal, seed, package, lock, Product, public, SEO or URL path changed from the failed attempt-2 Candidate.
- The five V3.1 proof files independently parse, their canonical `proofHash` values recompute, and all bind seal `9b5be579...` plus executable-tree SHA-256 `067a5e2e...`.

## Root-cause closure

The attempt-2 checker keyed the resolved target only by the resolved path string. On the required case-insensitive filesystem, a case-variant spelling resolves to the same device/inode but retains the requested casing, misses the canonical actual-tree map key and bypasses the target-class ceiling. Both the historical TypeScript witness and strict-TypeScript current JSON witness were independently reproduced as unsafe attempt-2 passes.

The corrected checker joins each local resolved target to exactly one physical actual-tree owner by device/inode, then requires exact canonical path spelling and exact Git membership before the existing class ceiling. It does not lowercase or normalize a lookup, introduce a path exception, or bypass the target class. Ambiguous owners, case/Unicode variants, symlinks, hard-links, untracked/ignored copies and non-canonical Git identities fail closed.

The independent real-gate challenge reports 27 cases: 22 expected negatives, three legitimate Candidate positives, two reproduced prior unsafe passes and zero Candidate unsafe acceptances. It includes TypeScript and JSON evidence, filename/directory/extension case, direct/side-effect/re-export/type-only/transitive/index edges, copies and physical aliases, NFC/NFD, H-01 origin, post-seal drift and stale proofs. Canonical Product imports, emitted local H-01 shadows and evidence-to-Product observation remain valid.

## REMEDIATION_FINDINGS_REVIEW

| Boundary | Disposition | Independent basis |
| --- | --- | --- |
| IMP3-NM01 attempt 3 | **PASS / CLOSED** | Physical identity, exact Git membership and class ceiling reject all Fresh aliases at the real sole checker. |
| Evidence isolation | **PASS / CLOSED** | Production/current incoming edges fail; historical evidence outgoing authority remains disabled; evidence observation remains allowed. |
| Final-tree / five proofs / L02 | **PASS / CLOSED** | Exact seal and executable-tree hashes recompute; post-seal executable drift fails; author verifier attached/detached PASS. |
| H-01 / M04 attempt 3 | **PASS / CLOSED** | Ambient capability rejects, emitted local shadow passes, DB exception stays exact. |
| H-02 / NH01 attempt 1 | **PASS / CLOSED** | Fresh disposable PGlite service matrix: 2 files / 7 tests; no-existence-leak ordering retained. |
| DB convergence | **PASS / CLOSED** | Exact named `globalThis.cwtDatabaseConnection` and exact file only. |
| M02 / NM01 / M01 | **PASS / CLOSED** | Product bytes unchanged; all-AI security/context/provenance/claimed execution coverage passes. |
| M03 | **PASS / CLOSED** | Strict typecheck and 2-positive/6-negative select-only seams pass. |
| M05 / L01 / remaining L02 | **PASS / CLOSED** | Public/Product/SEO frozen, availability-only runtime, evidence/test identity exact. |
| Frozen boundaries | **PASS / CLOSED** | Exact mappings, Prompt/history, empty registries, bundle and zero relevant diff pass. |

## Verification details

- source-clean real gate: 683 candidates / 481 executables / 2,269 edges;
- official-present real gate: 684 / 482 / 2,270;
- zero unclassified, ambiguous or current-unresolved; 21 ordinary URLs;
- 69 graph/topology faults, 10 positives, inherited 28 and final 14 mutations;
- M03 2-positive/6-negative type seams and five canonical proof artifacts PASS;
- all AI: 14 files / 163 tests;
- H-02 Fresh real-service: 2 files / 7 tests;
- DB/public/SEO focus: 5 files / 7 tests;
- lint and strict typecheck PASS;
- DB verifier and history PASS, including 21/21/21 and 96/96/96 ordered extraction;
- Prompt bundle/history and exact-empty Production Prompt/Provider PASS;
- official typegen and isolated network-denied server/public bundle PASS, 51 server files / 16 client chunks.

Process exceptions were confined to disposable Reviewer setup: wrong initial verifier cwd, a dependency-root symlink rejected before source-clean validation, a wrong Prompt-history filename extension, and two inspection-only default-Node invocations whose results were not accepted. Correct reruns used pinned Node and changed no Candidate byte.

## FULL_REVIEW_NECESSITY

**NOT_REQUIRED.** All remediation findings and preserved closures were disposed first. The checker is the sole Product-code mutation; the full architecture surface, all AI, authorization, focused DB/public/SEO, lint/typecheck, Schema, Prompt/Provider, typegen and server/public isolation gates passed. Further unrelated exhaustive application tests would be disproportionate and would not add material evidence for the static physical-target join.

## Final disposition

Conclusion: **PASS**. Counts: Blocker 0, High 0, Medium 0, Low 0, External Validation 0. IMP3-NM01 is CLOSED after correction attempt 3; three-strike escalation is not triggered. The exact Candidate is eligible only for Coordinator Phase B Implementation Gate acceptance consideration. No Schema, Migration, ADR, dependency, Complexity Approval, Product, public, SEO, URL, data, or Owner decision impact exists.
