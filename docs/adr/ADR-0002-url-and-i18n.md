# ADR-0002: URL and internationalization

Status: Accepted.

English remains at root. Future language prefixes are `/es/`, `/vi/`, `/tr/`, and `/zh-cn/`; English will not migrate to `/en/`. Routes are locale-aware from Phase 1, translations create distinct reviewed URLs, and automatic IP/language redirects are forbidden.
