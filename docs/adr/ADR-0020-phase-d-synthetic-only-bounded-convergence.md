# ADR-0020: Phase D Synthetic-Only Bounded Convergence and FONT-HERMETIC Proof Authority

Status: **Owner-approved for the S1 governance/Design line and one independent S1 Design/security proof; exact S1 remains a Candidate pending fresh independent PASS and Coordinator acceptance; Product implementation and Phase D acceptance are not authorized.**

Owner authorities:

- [Phase D Synthetic-Only Boundary Owner Decision V1.0](../PHASE_1B_STAGE4A_PHASE_D_SYNTHETIC_ONLY_BOUNDARY_OWNER_DECISION_V1_0.md)
- [FONT-HERMETIC Technical Escalation Owner Decision Amendment V1.0](../PHASE_1B_STAGE4A_PHASE_D_FONT_HERMETIC_TECHNICAL_ESCALATION_OWNER_DECISION_AMENDMENT_V1_0.md)

Exact Design Candidate: [Phase D FONT-HERMETIC Superseding Exact Design V1.0](../PHASE_1B_STAGE4A_PHASE_D_FONT_HERMETIC_SUPERSEDING_EXACT_DESIGN_V1_0.md)

Immutable source/rollback base: `fbe88cdd7639f32f48d92a0627833918b4924458` (`NOT_CANDIDATE`)

## Context

Phase D must converge the accepted DeepSeek adapter boundary to deterministic synthetic-only validation. Real/external credentials, databases, Provider/API effects, DNS/egress, live official-source reads and protected Staging remain outside Phase D. Earlier G3–G10 Design generations accumulated font mock/copy/loader proof machinery and filesystem ceremony. G10's local-font proposal remained unproven because the review harness first injected a pnpm currentness mismatch and the later diagnostic sandbox denied Turbopack's numeric-loopback IPC. Neither failure disproved `next/font/local`; neither produced a Build PASS.

A fresh authority boundary is required because proof-runner capability and forward lineage changed. The design must converge rather than layer another runner over G3–G10.

## Decision

1. **Synthetic-only Phase D.** Retain the exact synthetic-only Owner Decision. Executable controlled-real validation, live official-source access and every direct or dual real-validation acceptance route are removed in S2. Real/external Secret, credential, database, Provider/API, DNS/egress, official-source reads and protected Staging are `NOT_RUN / DEFERRED_TO_PHASE_F`, never PASS. The affected validation database credential is `ROTATION_REQUIRED_BEFORE_ANY_REUSE`.
2. **One Design/review proof authority B.** The only decisive S1 Design-proof runner is hash-bound native Node `24.14.0` invoking the exact project-local Next `16.2.12` public CLI over one verified full task-local installed dependency tree. Design proof does not use pnpm currentness, package scripts, `.bin` wrappers, internal/experimental runners, Webpack or a loader-only substitute.
3. **Exact network boundary.** The proof sandbox denies DNS, non-loopback and all external networking. It permits only runtime bind/connect to numeric `127.0.0.1` on an ephemeral port for Turbopack internal IPC. A minimal self-connect preflight must pass under the same policy before the single Build.
4. **One proof sequence.** A fresh independent reviewer performs one direct public-CLI typegen, atomically holds the generated root, performs one actual default-Turbopack Build, and only after Build PASS performs one focused native-Node public-bundle observation. Failure stops with zero Build retry, alternate runner or compiler fallback.
5. **Separate implementation authority.** S2 independently proves no-`CI`, error-mode pnpm currentness and executes the unchanged `pnpm build` at Gate 8. S1 Design proof cannot substitute for any implementation prerequisite, gate, aggregate, C2 evidence or acceptance.
6. **Local-font replacement.** S2 uses exactly `next/font/local` and two repository-contained, hash-bound Geist/Geist Mono WOFF2 files sourced only from the verified installed Next `16.2.12` package. Source fonts stay outside `public`; browser delivery is only Next-generated content-hashed `/_next/static/media/...woff2` referenced by generated CSS. No stable raw source-font route or second font authority is allowed.
7. **Retained semantic controls.** Preserve the G6 official typegen → atomic hold → fresh Build → restore lifecycle without inode/timestamp/polling ceremony, and preserve G9's synthetic profile, package/workspace/checker convergence, numeric-origin correction, exactly two bespoke runtime outputs, exactly four new Phase D mutation-probe classes, inherited controls and fresh ten-gate/no-retry sequence.
8. **Fresh linear lineage.** The only forward line is exact `fbe` → docs/evidence-only S1 → independent S1 PASS → Coordinator acceptance → fresh S2 → C2 only after full fresh PASS → different independent implementation/security review → Coordinator acceptance/checkpoint decision → pause. S1/S2 are not G11 or Attempt 4.
9. **Supersession.** G3–G10 history/evidence remains immutable. Its incidental proof mechanics and failed implementation source are not forward authority and may not be cherry-picked or copied. The superseding Design is the single implementation instruction.

## Consequences and rejected alternatives

This decision reduces proof authority to the real compiler property being tested while retaining pnpm as mandatory implementation authority. APFS CoW may transport one verified installed tree; it is not a snapshot authority. Rejected alternatives include Design-level pnpm dispatch, package-byte prerequisites unrelated to installed dependency identity, content-addressed dependency snapshots, install/download, Google mock/copy/env/server/cache/loader routes, Webpack, custom Next internals, retries, compatibility fallbacks and dual Google/local authority.

No dependency, lock, Next config, global CSS, Schema/Migration, Product/API contract, URL/Redirect, SEO metadata, publishing/Index or Product-data change is authorized. The synthetic PGlite migration is disposable validation setup only and does not change Schema.

## Security, compatibility and rollback

The deterministic adapter transport test retains exact numeric `127.0.0.1`, injected fetch and conspicuously synthetic credential semantics; it performs zero DNS/external egress and is not Provider evidence. Public/private storage, Prompt/AI protected boundaries, publishing/Index separation and all frozen CWT V1.1 invariants remain unchanged.

Rollback discards or reverts S2/C2 and then S1 in reverse authority order, returning to exact `fbe88cdd7639f32f48d92a0627833918b4924458`; historical accepted records are not rewritten. Any additional path, dependency materialization authority, font mismatch, broader network capability, external/protected input or Phase E/F request returns `NEEDS_OWNER_DECISION`.
