# Stage 6 reviewed local-stream integration report V1.0

Recorded: 2026-09-06; next-execution proposal added 2026-09-07. Status: **LOCAL INTEGRATION ACCEPTED at `c0df6907ada88133f6dfdfa62460dd0f5cc8aba9` by report-only PASS `54bcaeed8f4467ed8e717efa312cef4a02caad16`. Stage 6 remains Partial / HOLD pending the prerequisites below.**

## Inputs and integration identity

This branch starts from the final Backup Review commit `5df3701549983db4add647dd36bbc14b2e13f571`, whose parent is the accepted local Backup Candidate `14c11b9e78274f7280281418a4295a3894f40619`. It merges the final Runtime Review commit `2ad7146b48bc1930332966c47da28b8fddd923a0`, which reviews Runtime tools delivery `7e48c72940261ea264fef20d4a54355a4e14a609`. Both implementation streams originate from nonaccepted Stage 6 source `59d349f5be8bfc5ed3e9fc1157158b4140acdd08`; their implementation paths do not overlap. The source-preserving merge commit is `5eab97f8d4530ce131d4e3c2234e91408ad942c5` on `codex/stage6-local-integration`. Both reviewed ancestries and all implementation/Review reports remain unchanged and reachable.

The first integration Candidate `80fe4a79517c34def0a28f33a5feefe89ae58e97` received focused independent Review at report commit `ae26ce0b19c3a37a80270aa1e9024dc54be10868`. That Review kept both module Reviews CLOSED/PASS but failed the composition on `S6-LI-M-01`: Runtime selected `web-staging` for a protected Migration although only `scheduler-staging` owns the reviewed backup roots and shared backup/Migration mutex. This Candidate corrects only that existing caller/setup interface and the corresponding documentation/tests; it does not reopen either module body.

This merge creates no accepted tag, checkpoint protocol, Release identity, OCI digest or phase advancement. Stage 5 `a200838be34c8834a00bdcf6d1819da96e2ad26c` remains the last accepted Product baseline.

## Composed ownership and result

The source merge completed without a content conflict or merge-resolution edit. Runtime tools retain ownership of the workflow, release-header/materialization boundary, host-side Runtime validator and fixed compatibility profile. A future immutable successor subject owns this integrated `compose.yaml`, Drizzle SQL and project-relative binds; its image owns application code, `scripts/migrate.ts` and `deploy/backup`. The corrected Runtime plan selects subject Compose/SQL, the subject-owned `scheduler-staging` maintenance mount set and the image-owned `/app/deploy/backup/pre-deploy` → Migration chain. It does not overlay current files onto the historical immutable subject.

The integrated Compose authority retains ADR-0021 topology A: separate project-scoped Production/Staging outbound bridges, five approved Provider callers, dormant/internal Production AI Worker, internal database/cache/ingress networks, unchanged proxy ports and secret grants, and the **15/30** weekly connection allocation. Scheduler-only writable daily/set roots and the read-only shared backup/Migration mutex file remain enforced. The Runtime workflow retains distinct canonical sibling tools/subject checkouts, exact clean Git identities, tools-owned policy, subject-owned graph, one image-evidence authority and safe early release-header reconciliation.

The correction preserves exactly three steady services: `postgres`, `valkey-staging` and `web-staging`. Migration uses a disposable `scheduler-staging` Compose run and does not start its crontab or add a fourth steady service. Synthetic host setup now creates only the missing staging daily/set roots at UID/GID `10001:10001` with private mode and one empty root-owned shared mutex file at mode `0444`; it refuses any pre-existing CWT root. Cleanup removes consumers and networks first, then all correction-owned config, storage and lock roots, including partial-setup paths, while leaving unrelated host state outside those roots untouched.

## Proportionate local verification

- `CWT_BACKUP_TEST_IMAGE=cwt-s606-tools:local CWT_BACKUP_TEST_DEPS=cwt-s606-deps node --test deploy/scripts/release-registry-integration.test.mjs deploy/scripts/preflight-linux-runtime.test.mjs`: **25 passed**. This preserves tools/subject identity separation, workflow-selected absolute tools paths, source/profile ownership, release-header privacy, materializer transport and sole OCI authority, and adds the bounded composition proof described below.
- `CWT_BACKUP_TEST_IMAGE=cwt-s606-tools:local CWT_BACKUP_TEST_DEPS=cwt-s606-deps pnpm test:deployment`: **166 passed, 0 skipped**. The current integrated source validates dynamic/default Compose authority, topology A, outbound gateway/membership, Scheduler-only writable environment-private backup/set mounts, read-only shared mutex, denied Web/Worker backup mounts, direct Node schedules, Runtime workflow identity separation and unchanged deployment controls.
- The decisive correction test derives its caller and command from the actual Runtime plan and its security/mount boundary from actual normalized subject Compose. Against a real isolated local PostgreSQL instance, missing mutex and a held mutex both refuse before database access; the available mutex runs the exact Scheduler pre-deploy/Migration command, verifies its completed pre-deploy backup set and proves Drizzle migrations were applied.
- Two disposable Linux probes verify successful and partial synthetic-host ownership/cleanup. They confirm staging backup roots at `10001:10001/0700`, the empty root-owned mutex at `0444`, refusal without replacement when CWT state already exists, and removal of correction-owned partial state.
- `node --test deploy/scripts/preflight-image.test.mjs`: **16 passed**. Existing OCI index/child/evidence, runtime package-manager absence and Dockerfile runtime-stage source checks remain intact.
- `node --check` and targeted ESLint for the changed Runtime files passed; `pnpm typecheck` passed.
- One disposable Linux/network-none probe used two clean canonical sibling checkouts at exact merge commit `5eab97f8...`, one as tools and one as the synthetic successor subject. Actual `prepareRuntimeInputs` selected the tools profile and subject `compose.yaml`/Drizzle mount/Migration image entrypoint. The selected subject contained the approved outbound networks, both environment set roots, shared mutex mount and backup/Migration code; its Dockerfile contained PostgreSQL client acquisition, Restic/flock checks and `deploy/backup` packaging. The release header and digest were conspicuously synthetic, no evidence outcome or CWT image was emitted, and the temporary bundle/checkouts were removed.
- `git diff --check` passed. The exact PostgreSQL Bookworm pin was inspected locally and remains absent; no pull or substitute was attempted.

The local composition proof uses the already authorized shared `cwt-s606-tools:local` test utility and `cwt-s606-deps` dependency volume. It emits no CWT Product image and does not substitute for the unavailable exact pinned build input. The module-level PostgreSQL/Restic restore, daily stale-marker convergence, TLS/S3 fixture, network-peer and private storage checks remain valid independently reviewed evidence. They were not repeated as a second Product/backup Review because the correction changes none of those bodies.

## Packaging and external prerequisites

The unchanged input `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382` is not present on this host. Therefore this integration cannot execute the exact client acquisition layer, either architecture's copied shared-library closure, final slim-runtime tool smoke, linux/amd64 successor image inspection or a full integrated Runtime execution. Cached tooling from a different PostgreSQL base proves only the already-reviewed Synthetic backup behavior.

An authorized builder must first make that exact unchanged pin available, then build one coherent successor from the independently accepted integrated source, create its new real Release/evidence identity, and run the reviewed Runtime chain with tools and subject identities matching that successor. No historical Release record or OCI digest may be reused to claim this integrated source was built.

External Validation Required remains: native Tencent host/sudo/firewall/capacity/timing, live Registry and artifact custody, actual COS/private policy/provider semantics, protected Staging restore/start, teardown, independent scheduler-exit/monitoring transport and Owner-approved RPO/RTO. The external monitoring transport remains the known S6-05/Stage 7 integration item and is not implemented by this merge. O-18, 2 vCPU/4 GB/60 GB limits, the complete Migration chain and all Product/SEO/business-truth rules remain unchanged.

No Push, Registry write, CWT image build/emission, workflow dispatch, cloud/other-host access, real Provider/COS operation, production credential/data use, protected service start or deployment occurred. `S6-LI-M-01` is closed; the remaining work starts with the bounded prerequisite below and does not reopen either module Review.

## Next execution proposal

The lowest-cost sequence is **exact dependency availability → two-architecture copied-library probe → coherent successor Build Once/publication → exact-digest Runtime Validation → Provider/protected recovery evidence**. Immediately retrying the whole external chain would require a new immutable Release identity, GHCR writes, a Tencent Runner and later COS/protected access before the only current uncertainty—the unchanged PostgreSQL base and copied library closure—is isolated. The sequential path is recommended because each failure has one owner and the first two steps create no CWT Release.

### Exact-pin resolution and authorized local probe result

Local inspection has already confirmed that `Dockerfile` selects `backup-postgresql-tools` from exact `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382`, copies `pg_dump`, `pg_restore`, `psql` and their `ldd` closure into `/backup-root`, and later overlays that root onto exact `node:24.14.0-bookworm-slim@sha256:d8e448a56fc63242f70026718378bd4b00f8c82e78d20eefb199224a4d8e33d8`. The accepted `build-release-once.mjs` platform order is `linux/amd64`, then `linux/arm64`. This host does not hold the exact PostgreSQL input, so further local source reading cannot prove Registry availability or either executable closure.

The coordinator completed one anonymous Docker Hub HTTPS token/manifest read without layer download or personal credentials. HTTP `200`, response digest and calculated manifest-payload SHA-256 all matched the exact root digest. It advertised `linux/amd64` child `sha256:7e6103cf85f88f7a0eddb3ec0b1ba8940eba098ed118ade25a729ca9daee5568` and `linux/arm64/v8` child `sha256:83b9631dbb4121d9235ef910018272798b7d3041ef5b716aa481122d4c49353f`. That metadata query was not repeated. It proves current manifest visibility only, not BuildKit acquisition or executable closure.

The authorized local probe used the current Unix-socket Docker Desktop engine, Buildx `v0.35.0-desktop.2`, about 170 GiB host free space and an exact temporary archive of accepted source `c0df6907...`. Host and Docker Linux VM architecture were arm64/aarch64, so the required first `linux/amd64` run would have been emulated and could not be native target-host proof. Its one `backup-postgresql-tools` attempt used the declared exact Dockerfile frontend, exact PostgreSQL pin, `--no-cache`, one unique local acquisition tag and a 20-minute outer cap. The frontend resolved after 121.8 seconds; the PostgreSQL `FROM` metadata step then returned `DeadlineExceeded: context deadline exceeded`. The command ended NOT PASS after 365 seconds, before any Dockerfile `RUN`, layer-copy inspection, Node slim probe image or container runtime check. Under the first-failure rule, `linux/arm64` was not run.

No unique acquisition/runtime probe image or container was created. Both unique tags were absent and the exact temporary source/log root and pointer were removed. Pre-existing images and shared BuildKit/download cache were preserved; no prune or blanket cleanup ran. This result narrows the stop layer to current local BuildKit base acquisition. It is not evidence of a PostgreSQL tool/library packaging defect because none of the copy or runtime instructions executed, and it does not overturn the anonymous manifest result.

### Smallest next packaging action

The next useful action is the same dependency-only probe on an Owner-selected external trusted builder whose Docker/BuildKit path can acquire the visible exact pin. It must still run `linux/amd64` then `linux/arm64`, one attempt and at most 20 minutes per architecture, stopping on the first failure. From accepted source `c0df6907...`, build only the actual `backup-postgresql-tools` target under a run-unique local tag. A temporary Dockerfile outside the repository must use the exact pinned Node slim image and copy only that target's `/backup-root/` to `/`; after acquisition its build/run is network-none, and runtime uses numeric `10001:10001`, read-only root and no capabilities. PASS requires requested-platform inspection, no unresolved `ldd` dependency for all three PostgreSQL tools, PostgreSQL major 18 from each tool, working Node, and exact unique-resource cleanup. Native versus emulated execution must be reported.

This is now a genuinely external/other-host action and requires Owner authorization for the named builder/access path, public Docker Hub layer downloads, short-lived local non-CWT probe images/cache, up to 40 minutes of builder time and exact cleanup. It authorizes no repository edit, credential substitution, app build, CWT image/OCI/Release emission, Push, GHCR write, workflow dispatch or Provider/protected operation. The builder identity, access method, available storage and whether approved Docker Hub access requires credentials or cost are unresolved locally. Any authentication/rate-limit demand, missing platform, timeout, build/runtime mismatch or cleanup failure stops the task without retry, alternate route or pin substitution.

### Later gates and required Owner inputs

| Gate | Existing authority and decisive result | Owner input required at that gate |
| --- | --- | --- |
| Coherent successor Build Once and private publication | Dispatch `.github/workflows/cwt-release-publish.yml` from the same exact approved `release_commit`. Its first-attempt-only, 120-minute `cwt-stage6-build-once` job runs `pnpm build:release-once -- --output ...`, emits the dual-architecture `subject.oci`/`release.json`/evidence, publishes the resulting exact digest to private GHCR and retains detached evidence. Begin only after both dependency probes pass; do not create a Release merely to test the pin. | Exact successor release commit; explicit authorization for the trusted macOS ARM64 builder, workflow dispatch, external dependency downloads, image/OCI emission, GitHub Environment approval, GHCR write/artifact retention and associated resource cost. The successor digest and evidence run ID are outputs, never preselected inputs. |
| Exact-digest Linux Runtime Validation | Provision the single-use Tencent Singapore runner through the existing `deploy/runtime-validation/` invocation/scripts, then dispatch `.github/workflows/cwt-runtime-validation.yml` with exact `workflow_commit`, `release_commit`, `index_digest`, `evidence_run_id` and unique 128-bit `runner_nonce`. PASS is the existing sanitized Runtime outcome from `preflight-linux-runtime.mjs validate`, followed by required runner cleanup. | Selection/authorization of the reviewed workflow/tools commit and successor release; Tencent account, Singapore host/resource budget, sudo/firewall/egress and ephemeral-runner provisioning authority. Digest and evidence run ID must come from the successful publication. The exact host and access grants remain unresolved locally. |
| COS and protected recovery validation | Use the accepted `deploy/backup/backup-weekly`, `verify-backup-set` and `restore-empty` contracts to prove private repository policy, real object behavior, target timing/headroom and an empty isolated protected Staging restore before any Web/Worker activation. | Approved COS account/repository/bucket/region/endpoint, private policy, credentials, spend/retention authority, named recovery operator and protected Staging access/data scope. Owner-approved database-loss RPO, whole-site/host-loss RPO and RTO are required before recovery acceptance, not before dependency packaging, Build Once or Runtime image checks. |

The independent non-SMTP monitoring transport remains a **local S6-05/Stage 7 implementation gap**, not merely an external delivery test. The accepted S6-05 responsibility already provides the provider-neutral reporter, scrubbed event contract, work-health exit states, `journald` template and policy requiring an independent non-SMTP channel; it provides no transport adapter/account. After the dependency probe, this can proceed as a separate Stage 7 local task without blocking Build Once packaging. Its smallest scope is to select one Owner-approved transport, implement the single adapter/config composition plus fake-transport redaction/failure tests, and update the existing operations instructions. Actual account/credential/recipient, data-region/retention, spend and protected delivery authorization are required only for its later external proof. It must be complete before protected operational alerting or recovery acceptance is claimed.

Next gate: coordinator packaging-evidence disposition, then a concrete Owner request only for the bounded external trusted-builder dependency probe above. No successor Build Once, publication, Runtime cloud execution or later Provider permission is implied.
