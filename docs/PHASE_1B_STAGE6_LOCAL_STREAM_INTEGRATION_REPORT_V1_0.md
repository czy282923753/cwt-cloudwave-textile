# Stage 6 reviewed local-stream integration report V1.0

Recorded: 2026-09-06; Build Once execution, remediation, successful successor publication, Runtime decisions and authorized native Runtime outcomes added through 2026-09-08. Status: **LOCAL INTEGRATION ACCEPTED at `c0df6907ada88133f6dfdfa62460dd0f5cc8aba9` by report-only PASS `54bcaeed8f4467ed8e717efa312cef4a02caad16`; packaging Candidate `ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd` and index `sha256:94a89c44...` remain failed and revoked. Correction Candidate `78c882345d522d7a83cae9296c26499d49ab2521` passed focused independent Review `65bed8141c28b735c97a741587f6a81b895453b6`, and its authorized Build Once/private publication succeeded. The recovery-aware native Runtime attempt on reviewed tools `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c` returned Product `NOT_PASS` with `reasonCode=bundle_authority_failed`. The later reviewed integration source `1eecba71e6768661156d0292951f0918d29c00be` was pushed and dispatched exactly once, but its first emitted `linux/amd64` child failed the new exact-image bundle gate because the shipped checker required workspace-only files absent from the runtime image. GHCR authentication/publication did not run; the emitted index is revoked and retained only as minimal failure evidence. Technical Escalation correction `9f270171fa8e0a467c29283a70e6d91522545a39`, accepted by Review `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, passed one bounded non-release real-Next `linux/amd64` image rehearsal and then one authorized formal dual-platform Build Once. Both child checker/SBOM/Sharp/identity gates passed; private GHCR now holds exact index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` with detached evidence artifact `10025983850`. The authorized native Runtime run `34145251214` then reached the sole Product validator exactly once and returned `NOT_PASS / compose_infrastructure_up_failed`; sanitized outcome artifact `10027475980` was retained. Integrated diagnostic tools commit `cf266ae099995ddaf7a2e9572774cd49bc3c1f19` then drove one separately authorized native run `34161175361`, which reproduced `compose_infrastructure_up_failed` and retained bounded service state: PostgreSQL `running/starting`, Valkey `created`. Artifact `10032694897` was hash-verified; the one-shot attempt and temporary TCCLI authorization were cleaned up with zero observed cloud/Runner/application-grant residue. Two Operator credential-process nonconformances are recorded separately from the Product result. Reviewed Runner child-mask repair `f6703406353412400cf65536915c7a4614ab7dfe` passed independent Review `787d699ce68c562ea02ee5b632500c23fd2787e6`; native run `34165385584` then cleared the former permission boundary and reached all three Runtime services, but returned `NOT_PASS / compose_project_inventory_invalid` because Compose 5.3.1 emits multiple inventory rows as newline-delimited JSON while the validator accepts only one JSON document. Artifact `10034024882` was hash-verified; all attempt-owned Tencent resources and the ephemeral Runner were removed, with the 90-minute cleanup deadline missed and recorded as an operational nonconformance. Stage 6 remains Partial / HOLD pending a focused parser correction and independent Review.**

## Inputs and integration identity

This branch starts from the final Backup Review commit `5df3701549983db4add647dd36bbc14b2e13f571`, whose parent is the accepted local Backup Candidate `14c11b9e78274f7280281418a4295a3894f40619`. It merges the final Runtime Review commit `2ad7146b48bc1930332966c47da28b8fddd923a0`, which reviews Runtime tools delivery `7e48c72940261ea264fef20d4a54355a4e14a609`. Both implementation streams originate from nonaccepted Stage 6 source `59d349f5be8bfc5ed3e9fc1157158b4140acdd08`; their implementation paths do not overlap. The source-preserving merge commit is `5eab97f8d4530ce131d4e3c2234e91408ad942c5` on `codex/stage6-local-integration`. Both reviewed ancestries and all implementation/Review reports remain unchanged and reachable.

The first integration Candidate `80fe4a79517c34def0a28f33a5feefe89ae58e97` received focused independent Review at report commit `ae26ce0b19c3a37a80270aa1e9024dc54be10868`. That Review kept both module Reviews CLOSED/PASS but failed the composition on `S6-LI-M-01`: Runtime selected `web-staging` for a protected Migration although only `scheduler-staging` owns the reviewed backup roots and shared backup/Migration mutex. This Candidate corrects only that existing caller/setup interface and the corresponding documentation/tests; it does not reopen either module body.

This merge creates no accepted tag, checkpoint protocol, Release identity, OCI digest or phase advancement. Stage 5 `a200838be34c8834a00bdcf6d1819da96e2ad26c` remains the last accepted Product baseline.

## Composed ownership and result

The source merge completed without a content conflict or merge-resolution edit. Runtime tools retain ownership of the workflow, release-header/materialization boundary, host-side Runtime validator and fixed compatibility profile. A future immutable successor subject owns this integrated `compose.yaml`, Drizzle SQL and project-relative binds; its image owns application code, `scripts/migrate.ts` and `deploy/backup`. The corrected Runtime plan selects subject Compose/SQL, the subject-owned `scheduler-staging` maintenance mount set and the image-owned `/app/deploy/backup/pre-deploy` → Migration chain. It does not overlay current files onto the historical immutable subject.

The integrated Compose authority retains ADR-0021 topology A: separate project-scoped Production/Staging outbound bridges, five approved Provider callers, dormant/internal Production AI Worker, internal database/cache/ingress networks, unchanged proxy ports and secret grants, and the **15/30** weekly connection allocation. Scheduler-only writable daily/set roots and the read-only shared backup/Migration mutex file remain enforced. The Runtime workflow retains distinct canonical sibling tools/subject checkouts, exact clean Git identities, tools-owned policy, subject-owned graph, one image-evidence authority and safe early release-header reconciliation.

The correction preserves exactly three steady services: `postgres`, `valkey-staging` and `web-staging`. Migration uses a disposable `scheduler-staging` Compose run and does not start its crontab or add a fourth steady service. Synthetic host setup now creates only the missing staging daily/set roots at UID/GID `10001:10001` with private mode and one empty root-owned shared mutex file at mode `0444`; it refuses any pre-existing CWT root. Cleanup removes consumers and networks first, then all correction-owned config, storage and lock roots, including partial-setup paths, while leaving unrelated host state outside those roots untouched.

## Proportionate local verification

- `CWT_BACKUP_TEST_IMAGE=cwt-s606-tools:local CWT_BACKUP_TEST_DEPS=cwt-s606-deps node --test deploy/scripts/release-registry-integration.test.mjs deploy/scripts/preflight-linux-runtime.test.mjs`: **25 passed**. This preserves tools/subject identity separation, workflow-selected absolute tools paths, source/profile ownership, release-header privacy, materializer transport and sole OCI authority, and adds the bounded composition proof described below.
- `CWT_BACKUP_TEST_IMAGE=cwt-s606-tools:local CWT_BACKUP_TEST_DEPS=cwt-s606-deps pnpm test:deployment`: **166 passed, 0 skipped**. The current integrated source validates dynamic/default Compose authority, topology A, outbound gateway/membership, Scheduler-only writable environment-private backup/set mounts, read-only shared mutex, denied Web/Worker backup mounts, direct Node schedules, Runtime workflow identity separation and unchanged deployment controls.
- The decisive correction test derives its caller and command from the actual Runtime plan and its security/mount boundary from actual normalized subject Compose. Against a real isolated local PostgreSQL instance, missing mutex and a held mutex both refuse before database access; the available mutex runs the exact Scheduler pre-deploy/Migration command, verifies its completed pre-deploy backup set and proves Drizzle migrations were applied.
- Two disposable Linux probes verify successful and partial synthetic-host ownership/cleanup. They confirm staging backup roots at `10001:10001/0700`, the empty root-owned mutex at `0444`, refusal without replacement when CWT state already exists, and removal of correction-owned partial state.
- `node --test deploy/scripts/preflight-image.test.mjs`: **17 passed**. Existing OCI index/child/evidence, runtime package-manager absence and Dockerfile runtime-stage source checks remain intact; the added focused source check freezes canonical library directories, loader basenames, exact pins and the unchanged final overlay.
- `node --check` and targeted ESLint for the changed Runtime files passed; `pnpm typecheck` passed.
- One disposable Linux/network-none probe used two clean canonical sibling checkouts at exact merge commit `5eab97f8...`, one as tools and one as the synthetic successor subject. Actual `prepareRuntimeInputs` selected the tools profile and subject `compose.yaml`/Drizzle mount/Migration image entrypoint. The selected subject contained the approved outbound networks, both environment set roots, shared mutex mount and backup/Migration code; its Dockerfile contained PostgreSQL client acquisition, Restic/flock checks and `deploy/backup` packaging. The release header and digest were conspicuously synthetic, no evidence outcome or CWT image was emitted, and the temporary bundle/checkouts were removed.
- `git diff --check` passed. At the original integration gate the exact PostgreSQL Bookworm pin was absent and no substitute was attempted; the later bounded dependency probes and correction outcome are recorded below.

The local composition proof uses the already authorized shared `cwt-s606-tools:local` test utility and `cwt-s606-deps` dependency volume. It emits no CWT Product image. The later exact-pin dependency probes prove only the corrected acquisition/Node-slim closure described below. The module-level PostgreSQL/Restic restore, daily stale-marker convergence, TLS/S3 fixture, network-peer and private storage checks remain valid independently reviewed evidence. They were not repeated as a second Product/backup Review because the correction changes none of those bodies.

## Packaging and external prerequisites

The unchanged input `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382` has now been acquired through the actual corrected target, and the copied shared-library closure passes in the exact Node slim base on both architectures. This remains dependency-only evidence: it does not build or inspect a complete CWT successor image, execute the application, prove native `linux/amd64` target-host behavior or create a Release/OCI identity.

After focused independent Review of this packaging delta, an authorized builder may build one coherent successor from the reviewed integrated source, create its new real Release/evidence identity, and run the reviewed Runtime chain with tools and subject identities matching that successor. No historical Release record or OCI digest may be reused to claim this integrated source was built.

External Validation Required remains: native Tencent host/sudo/firewall/capacity/timing, live Registry and artifact custody, actual COS/private policy/provider semantics, protected Staging restore/start, teardown, independent scheduler-exit/monitoring transport and Owner-approved RPO/RTO. The external monitoring transport remains the known S6-05/Stage 7 integration item and is not implemented by this merge. O-18, 2 vCPU/4 GB/60 GB limits, the complete Migration chain and all Product/SEO/business-truth rules remain unchanged.

No Push, Registry write, CWT image build/emission, workflow dispatch, cloud/other-host access, real Provider/COS operation, production credential/data use, protected service start or deployment occurred. `S6-LI-M-01` is closed; the remaining work starts with the bounded prerequisite below and does not reopen either module Review.

## Next execution proposal

The selected sequence is **exact dependency availability → two-architecture copied-library probe → focused packaging Review → coherent successor Build Once/publication → exact-digest Runtime Validation → Provider/protected recovery evidence**. The first three gates are locally complete without creating a CWT Release: independent packaging PASS `bd83775c3f332972696e102560df89cda4bf7444` accepts exact Candidate `ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd`. Build Once readiness is recorded below.

### Exact-pin resolution and authorized local probe result

Local inspection confirmed that `Dockerfile` selects `backup-postgresql-tools` from exact `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382`, copies `pg_dump`, `pg_restore`, `psql` and their `ldd` closure into `/backup-root`, and later overlays that root onto exact `node:24.14.0-bookworm-slim@sha256:d8e448a56fc63242f70026718378bd4b00f8c82e78d20eefb199224a4d8e33d8`. The accepted `build-release-once.mjs` platform order is `linux/amd64`, then `linux/arm64`. Before the bounded probes, this host did not hold the exact PostgreSQL input, so source reading alone could not prove Registry availability or either executable closure.

The coordinator completed one anonymous Docker Hub HTTPS token/manifest read without layer download or personal credentials. HTTP `200`, response digest and calculated manifest-payload SHA-256 all matched the exact root digest. It advertised `linux/amd64` child `sha256:7e6103cf85f88f7a0eddb3ec0b1ba8940eba098ed118ade25a729ca9daee5568` and `linux/arm64/v8` child `sha256:83b9631dbb4121d9235ef910018272798b7d3041ef5b716aa481122d4c49353f`. That metadata query was not repeated. It proves current manifest visibility only, not BuildKit acquisition or executable closure.

The authorized local probe used the current Unix-socket Docker Desktop engine, Buildx `v0.35.0-desktop.2`, about 170 GiB host free space and an exact temporary archive of accepted source `c0df6907...`. Host and Docker Linux VM architecture were arm64/aarch64, so the required first `linux/amd64` run would have been emulated and could not be native target-host proof. Its one `backup-postgresql-tools` attempt used the declared exact Dockerfile frontend, exact PostgreSQL pin, `--no-cache`, one unique local acquisition tag and a 20-minute outer cap. The frontend resolved after 121.8 seconds; the PostgreSQL `FROM` metadata step then returned `DeadlineExceeded: context deadline exceeded`. The command ended NOT PASS after 365 seconds, before any Dockerfile `RUN`, layer-copy inspection, Node slim probe image or container runtime check. Under the first-failure rule, `linux/arm64` was not run.

No unique acquisition/runtime probe image or container was created. Both unique tags were absent and the exact temporary source/log root and pointer were removed. Pre-existing images and shared BuildKit/download cache were preserved; no prune or blanket cleanup ran. This result narrows the stop layer to current local BuildKit base acquisition. It is not evidence of a PostgreSQL tool/library packaging defect because none of the copy or runtime instructions executed, and it does not overturn the anonymous manifest result.

### Bounded Docker network diagnosis and smallest next action

One five-minute read-only diagnosis used cached `node:24.14.0-bookworm` with `--pull never` and one ordinary bridge-network disposable container. Host and container DNS resolved both `registry-1.docker.io` and `auth.docker.io`. Host and container TLS verification passed; Registry `/v2/` returned the expected `401` Bearer challenge pointing to `https://auth.docker.io/token`, and the Auth root returned `404`, proving HTTP reachability. The container received no `HTTP_PROXY`/`HTTPS_PROXY` environment value and connected to public remote addresses. The diagnostic container was removed automatically and no image, cache or setting was changed.

The ordinary host/container paths therefore succeed while the existing docker-driver BuildKit path stops at exact PostgreSQL base metadata. Docker Engine's reported internal `http.docker.internal:3128` proxy remains a normal observed configuration value, not a diagnosed error. The available Docker Desktop CLI exposes no read-only settings/proxy command; the previously blocked settings-file read was not repeated. Whether BuildKit's internal proxy/DNS/TLS session is stale or the Registry path has a BuildKit-specific timing failure remains unknown. No broad logs, second request sample or corrective action was used.

Immediately before the authorized maintenance action, the same local context had zero running containers, no active BuildKit history and 11 pre-existing containers all `exited` with restart policy `no`. One standard Docker Desktop restart completed in about 18 seconds. The engine returned healthy on the same `desktop-linux` context, all 11 container identities remained stopped and no setting, builder, context or cache was changed. No second restart occurred.

The sole post-restart `linux/amd64` attempt then acquired and built the actual `backup-postgresql-tools` target from accepted source in 15 seconds; this changed-state success does not prove the exact cause of the earlier BuildKit timeout. The exact Node slim metadata resolved in 1.9 seconds. The next equivalent `/backup-root/` overlay failed before its `RUN` or runtime container with `cannot copy to non-directory: .../lib`. Read-only inspection found a real `/backup-root/lib` directory containing the copied `x86_64-linux-gnu` libraries, while the exact Node slim target has `/lib -> usr/lib`; accepted `Dockerfile` line 119 uses the same `COPY --from=dependency-input /backup-root/ /` destination shape. Node/runtime, tool-version and unresolved-library checks therefore did not execute, and `arm64` was not run under the first-failure rule.

This was a copied-library path-layout incompatibility exposed only after exact dependency acquisition, not another network failure or evidence that either pin was unavailable. The bounded correction changes only the existing acquisition destination logic: it canonicalizes each source library directory through `readlink -f`, permits only the expected `/usr/lib` or `/usr/lib64` trees, and copies the dereferenced bytes under the original `ldd` basename. This preserves SONAME and ELF-interpreter lookup names while preventing a real `/backup-root/lib` directory from replacing the Node target's `/lib -> usr/lib`. All exact pins, the single dependency bundle and final `COPY --from=dependency-input /backup-root/ /` overlay remain unchanged; no fallback or second authority was added.

The corrected actual `backup-postgresql-tools` target and equivalent unchanged final overlay passed sequentially for `linux/amd64` and `linux/arm64` within their single attempts. Both exact images retained `/lib -> usr/lib`; Node remained `v24.14.0`; `pg_dump`, `pg_restore` and `psql` each reported PostgreSQL 18.4 with zero unresolved `ldd` dependencies and an existing ELF interpreter (`/lib64/ld-linux-x86-64.so.2` on amd64; `/lib/ld-linux-aarch64.so.1` on arm64). Runtime checks used network none, UID/GID `10001:10001`, read-only root, no capabilities and no-new-privileges. The amd64 run was emulated on the local aarch64 Docker VM; arm64 was native-architecture local Docker evidence, not Tencent target-host proof.

All unique acquisition/runtime tags, copy containers and temporary source/build/log roots were removed. The 11 pre-existing stopped containers and shared images/BuildKit/download cache were preserved. No personal Registry credentials, app build, CWT image/OCI/Release emission, Push, GHCR write, workflow dispatch, external host or Provider/protected operation occurred.

### Later gates and required Owner inputs

| Gate | Existing authority and decisive result | Owner input required at that gate |
| --- | --- | --- |
| Coherent successor Build Once and private publication | Dispatch `.github/workflows/cwt-release-publish.yml` from the same exact approved `release_commit`. Its first-attempt-only, 120-minute `cwt-stage6-build-once` job runs `pnpm build:release-once -- --output ...`, emits the dual-architecture `subject.oci`/`release.json`/evidence, publishes the resulting exact digest to private GHCR and retains detached evidence. | Exact successor release commit; explicit authorization for the trusted macOS ARM64 builder, source Push, workflow dispatch, external dependency downloads, image/OCI emission, GitHub Environment approval, GHCR write/artifact retention and associated resource cost. The successor digest and evidence run ID are outputs, never preselected inputs. |
| Exact-digest Linux Runtime Validation | Provision the single-use Tencent Singapore runner through the existing `deploy/runtime-validation/` invocation/scripts, then dispatch `.github/workflows/cwt-runtime-validation.yml` with exact `workflow_commit`, `release_commit`, `index_digest`, `evidence_run_id` and unique 128-bit `runner_nonce`. PASS is the existing sanitized Runtime outcome from `preflight-linux-runtime.mjs validate`, followed by required runner cleanup. | Selection/authorization of the reviewed workflow/tools commit and successor release; Tencent account, Singapore host/resource budget, sudo/firewall/egress and ephemeral-runner provisioning authority. Digest and evidence run ID must come from the successful publication. The exact host and access grants remain unresolved locally. |
| COS and protected recovery validation | Use the accepted `deploy/backup/backup-weekly`, `verify-backup-set` and `restore-empty` contracts to prove private repository policy, real object behavior, target timing/headroom and an empty isolated protected Staging restore before any Web/Worker activation. | Approved COS account/repository/bucket/region/endpoint, private policy, credentials, spend/retention authority, named recovery operator and protected Staging access/data scope. Owner-approved database-loss RPO, whole-site/host-loss RPO and RTO are required before recovery acceptance, not before dependency packaging, Build Once or Runtime image checks. |

The independent non-SMTP monitoring transport remains a **local S6-05/Stage 7 implementation gap**, not merely an external delivery test. The accepted S6-05 responsibility already provides the provider-neutral reporter, scrubbed event contract, work-health exit states, `journald` template and policy requiring an independent non-SMTP channel; it provides no transport adapter/account. After the dependency probe, this can proceed as a separate Stage 7 local task without blocking Build Once packaging. Its smallest scope is to select one Owner-approved transport, implement the single adapter/config composition plus fake-transport redaction/failure tests, and update the existing operations instructions. Actual account/credential/recipient, data-region/retention, spend and protected delivery authorization are required only for its later external proof. It must be complete before protected operational alerting or recovery acceptance is claimed.

### Build Once readiness preflight

One bounded read-only GitHub preflight confirmed that repository origin is `https://github.com/czy282923753/cwt-cloudwave-textile.git`, default branch `main` is at `506d92bf...`, and release source `ab8ed218...` is not yet remotely reachable. Remote `main` is an ancestor of the release source, which is 32 commits ahead; `main` has no branch protection or repository ruleset. The Environment custom branch policy allows only `main`. The suitable dispatch ref is therefore `main`, and the required source mutation is one normal non-force fast-forward Push using the exact refspec `ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd:refs/heads/main`. Any later report-only commit must remain outside that release identity.

GitHub registers `.github/workflows/cwt-release-publish.yml` as active on the default branch. Environment `cwt-stage6-build-once` requires reviewer `czy282923753`, permits self-review, and has no wait timer. The destination package `ghcr.io/czy282923753/cwt-cloudwave-textile` exists and is private. Repository runner inventory is the blocking readiness fact: total registered runners is zero, so there is no runner name, label set, online state or busy state that can satisfy `self-hosted`, `macOS`, `ARM64`, `cwt-trusted-build-once`. No runner was started, connected or registered, and no access setting was changed.

Before an execution approval can be acted on, the Owner must identify the exact previously approved trusted macOS ARM64 host/runner and authorize its registration or activation with all four required labels; the coordinator must then perform one read-only confirmation that the named runner is online and idle. Do not invent or automatically register a replacement. Once that prerequisite passes, the smallest authorization is: (1) one normal non-force Push of exact source `ab8ed218...` to `main`; (2) one dispatch of the active workflow on ref `main` with `release_commit=ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd`; (3) the required Environment approval; and (4) one first-attempt-only Product build/private publication capped at 120 minutes. Expected outputs are one new dual-architecture OCI identity in the existing private GHCR package plus detached `release.json`/evidence retained for 30 days. Any Push mismatch, runner drift, approval failure, build/publication failure or missing output stops the operation with no rerun, force Push, source mutation or replacement Release.

This authorization would cover only that Push, dispatch, trusted Build Once resource use, public dependency reads, Product image/OCI emission, private GHCR write and detached evidence retention. Tencent/native Runtime, COS/protected recovery, production deployment, monitoring transport and RPO/RTO remain later separately authorized gates.

At proposal time, the next gate was the Owner decision on the exact named trusted runner prerequisite and, only after its online/idle confirmation, the one coherent Build Once/private publication boundary above. The execution and resulting superseding disposition are recorded below.

## Authorized Build Once execution outcome

The Owner subsequently approved this MacBook Pro as the ephemeral trusted runner and authorized the exact bounded execution. The Operator registered one repository runner, `cwt-build-once-mac-e7da462dacd14d7b129a3807c4b50c7d`, with exactly `self-hosted`, `macOS`, `ARM64` and `cwt-trusted-build-once`; confirmed it online and idle; and confirmed zero unrelated queued, in-progress or waiting repository runs. Remote `main` was then advanced by one normal non-force fast-forward from `506d92bf396bae52d7d8e54dabc46345036e4f86` to exact release source `ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd`. A malformed local refspec was rejected before contacting or changing the remote; the corrected command performed the sole actual Push.

Exactly one `CWT authorized Build Once and private GHCR publication` workflow was dispatched on `main` with the exact release commit. [GitHub Actions run `34080416088`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34080416088), attempt `1`, used exact head SHA `ab8ed218...`; its sole `cwt-stage6-build-once` Environment deployment was approved once. The job ran on the named runner from `2026-09-07T03:40:24Z` through `2026-09-07T03:50:43Z`. Exact-source/refusal guards, pnpm `11.9.0`, Node `24.14.0`, hash-pinned ORAS, accepted tool-boundary verification and dependency installation all passed. No rerun, second dispatch or replacement runner was used.

The authoritative Build Once command emitted a real dual-architecture OCI index locally and the Actions log records index digest `sha256:94a89c44d18a38fd4a35cbb82178c4c79159cc2eb8d8e12e27d0a5c71cd62823`, amd64 manifest/config `sha256:bfa8e11fbeb61fe83b9046f7940a5097d881290d4287ac3c1be740814a31eb7e` / `sha256:703ed52a96b559b93ab47dc1267d6ad5e60458a174b2d0c0439fc8a2155fec1e`, and arm64 manifest/config `sha256:0fce7adf495553a38e9bb3312ac97686290311d9007b4ecc0184e1809a55a566` / `sha256:d8841c0616cc0fc68472530e094871fe76ea351ab38581f0b0398efc079d8b26`. These identities are failure evidence only. During the first per-child Docker Scout preparation, `dockerScoutSbom` tried to hard-link shared blob `sha256:4f4fb700ef54461cfa02571ae0db9a0dc1e0cdb5577484a6d75e68dc38e8acc1` into the same child layout more than once and failed with `EEXIST`. The causal source boundary is `deploy/scripts/build-release-once.mjs`: it iterates `child.manifestDigest`, `child.configDigest` and every `child.layers` entry without deduplicating equal digests before using exclusive `linkSync` destinations.

This is a post-emission gate failure. Under the accepted Option F rule, index `sha256:94a89c44...` and release identity `ab8ed218...` are revoked and may not be rebuilt, published, promoted or reused. GHCR authentication, exact-digest publication and evidence upload were skipped. GitHub reports zero artifacts for the run, and the private package has no version tagged `ab8ed218abef52fbc3c4b9220f0d0a15f67d24fd`. The failed output root received the script's local revocation record, but both it and the emitted subject were under `RUNNER_TEMP`; Actions removed that temporary output at job completion before any failure artifact step could retain it. The run log is therefore the surviving evidence. This exposes a second bounded custody defect: the workflow retains detached evidence only after success and has no failure-path upload for an already-emitted revoked subject.

The ephemeral runner exited and automatically deregistered. Its unique runner directory and pointer were removed after confirming no owned process or registration remained. Zero running containers, all 11 pre-existing stopped containers, 29 shared images and the shared BuildKit/download cache were preserved unchanged. No Tencent, COS, Provider, protected Runtime, production credential/data, deployment, protected service start, monitoring implementation, force Push, phase advancement or other external action occurred.

The smallest next engineering action is a new bounded remediation Candidate that deduplicates the child blob digest set before linking and adds one focused duplicate-layer regression test. The workflow also needs a narrowly scoped failure-path retention correction for the revocation record and detached identity evidence after subject emission. Both corrections require independent Review. A later Build Once requires a new source/release commit and fresh explicit execution authorization; this report grants no repair, Push, dispatch, rerun, publication, Runtime Validation or phase-advancement authority.

## Bounded local remediation Candidate

Local correction Candidate `78c882345d522d7a83cae9296c26499d49ab2521` replaces the failing copy multiplicity at its production boundary. `materializeScoutOciBlobs` now applies standard insertion-ordered `Set` semantics only to the manifest/config/layer digest collection used to populate the per-child Scout OCI blob directory. It still uses exclusive `linkSync`: a missing source blob continues to fail with `ENOENT`, and a genuinely pre-existing destination continues to fail with `EEXIST` without overwrite. The original child descriptor and its layer order/duplicates are not mutated, and OCI identity, exact pins, Docker Scout, dependency acquisition, image build, release lifecycle and publication logic are unchanged. A minimal direct-execution guard permits the production helper to be imported by the focused test while preserving the CLI's fail-closed `--output` requirement.

The release workflow now has one failure-only upload step after the existing success artifact step. It reuses pinned `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02`, retains only `${runner.temp}/cwt-release-${release_commit}/revoked/*.json` for 30 days under distinct artifact name `cwt-revoked-release-evidence-${release_commit}`, and uses `if-no-files-found: ignore`. Consequently, a post-emission failure can retain only the existing minimal `schemaVersion` / `indexDigest` / `releaseId` / `reasonCode` / `recordedAt` record; a pre-emission failure creates no record or artifact. The step has no script or synthetic-record path, does not upload the OCI subject, release record, partial SBOM/provenance/framework output, auth material or logs, and cannot convert the original job failure into success. Existing successful-release evidence naming and consumers remain unchanged.

Targeted verification passed: `node --check deploy/scripts/build-release-once.mjs`; targeted ESLint for the changed JavaScript files with zero warnings; and `node --test deploy/scripts/build-release-once.test.mjs deploy/scripts/release-registry-integration.test.mjs` with **15 passed, 0 failed**. The production copy-boundary tests use small real files and hard links to prove one destination per unique digest while preserving repeated layer order, plus unchanged missing-source and pre-existing-destination failures. Workflow checks freeze the pinned action, `failure()` condition, distinct name, exact narrow glob, 30-day retention, no-run boundary and pre-emission no-file behavior. `git diff --check` passed. `actionlint` was unavailable on this host; no substitute download or broad tool installation was introduced.

Complexity remains level: one existing loop became one named copy helper, no alternate materializer or error-swallowing path remains, and one standard artifact step closes the temporary-runner custody gap without persistent state or a second evidence authority. No Product build, OCI emission, Docker execution, Push, runner registration, workflow dispatch, Registry write, cloud/provider/protected operation, deployment, historical evidence reconstruction or phase advancement occurred. Historical run `34080416088`, source `ab8ed218...` and index `sha256:94a89c44...` remain failed and revoked. This Candidate is not an authorized Release. Next gate is focused independent Review, including the lightweight Security & Test Simplification Check; any fresh Build Once/private publication requires a distinct reviewed source and separate explicit Owner authorization.

## Reviewed successor Build Once and private publication outcome

Focused independent Review `65bed8141c28b735c97a741587f6a81b895453b6`, whose parent is exact implementation Candidate `78c882345d522d7a83cae9296c26499d49ab2521`, returned PASS. After fresh Owner authorization, the Operator reconfirmed the exact clean source, origin, normal fast-forward ancestry from remote `main` at `ab8ed218...`, required tool pins, 160 GiB free space, zero running containers, 11 pre-existing stopped containers, no active BuildKit/runner process, zero queued/in-progress/waiting repository runs and zero registered repository runners. The official macOS ARM64 Actions Runner `2.337.0` archive was verified against SHA-256 `5a2cd92908a93d7276a194e1de6008099f3e7946f3f8e14aa7a1a7b4a31fdec2`; one bounded TLS retry completed the same asset download before any runner registration. One ephemeral runner, `cwt-build-once-mac-12b59f586c08b71e48faccf4dd37aa3d`, registered with exactly the four required labels and was confirmed online and idle.

One normal non-force Push advanced remote `main` to exact source `78c882345d522d7a83cae9296c26499d49ab2521`; a prior TLS-handshake failure made no remote mutation, and API confirmation proved `main` still at `ab8ed218...` before the successful retry. Exactly one [GitHub Actions run `34082239544`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34082239544) was dispatched on `main` with the exact source input. It is `workflow_dispatch`, attempt `1`, head SHA `78c88234...`, and its one `cwt-stage6-build-once` Environment deployment was approved once. The sole job ran from `2026-09-07T04:12:53Z` through `2026-09-07T04:30:52Z` and completed `success`; every source/tool/build/auth/publish/success-evidence step passed, while the failure-only revocation upload correctly skipped.

The new retained Release is `built` and unrevoked. `release.json` binds release/source commit `78c882345d522d7a83cae9296c26499d49ab2521`, source tree `d04d8b138b04fa43b9d5dc99c13df240a1dce1ae` and OCI index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`. Platform order is `linux/amd64`, then `linux/arm64`. The amd64 manifest/config are `sha256:61b463ccb221b3383f267160eb5435793a5b2c01bebf3deab769e0a22d49c543` / `sha256:027e8326bc63ee7b14efff09ccb9a8710a8fa8fca5c137108c3255b83479c1be`; arm64 manifest/config are `sha256:809f5f5a23d6b0722404d369fb98ae246fb75295689bb899c88d284ad860ed9c` / `sha256:33cc6b062630e63839ba54f4bbeee89dedad6e8fb3094dc07ff79fdeabc9ee3f`.

The existing private GHCR package remains `private`; package version `1217211391` is named by exact index digest `sha256:fc96539e...` and has the sole new tag `78c882345d522d7a83cae9296c26499d49ab2521`. Successful evidence artifact `10004387422`, `cwt-release-evidence-78c882345d522d7a83cae9296c26499d49ab2521`, is unexpired, 9,086,518 bytes and expires `2026-10-07T04:30:37Z`. A task-owned download independently confirmed the exact source/tree/index, `built` state, platform order and child identities; all six release-declared evidence descriptor hashes matched, seven expected evidence files were present, no revocation directory was present, and the existing `check-release` authority returned PASS. The temporary verification copy was then removed; GHCR and the 30-day GitHub artifact remain the custody authorities.

The ephemeral runner exited and automatically deregistered. Its unique runner, auth and download temporary roots and pointers were removed after consumers stopped. Final local safety checks found zero running containers, the same 11 pre-existing stopped containers, the same 29 shared images, no queued/in-progress/waiting repository runs, and preserved shared BuildKit/download cache. No rerun, duplicate dispatch, replacement source, Tencent/cloud/native Runtime, COS/protected recovery, production credential/data, deployment, protected service start, monitoring implementation, retag, prune or phase advancement occurred. Historical run `34080416088`, source `ab8ed218...` and index `sha256:94a89c44...` remain failed and revoked. Next gate is coordinator evidence assessment; any Runtime Validation remains a separate explicit authorization and is not dispatched automatically.

## Decision-ready native Runtime Validation authorization scope

This section records the read-only preparation and authorization basis that preceded the attempt; the execution outcome below supersedes its unresolved current-state statements. Tools commit `65bed8141c28b735c97a741587f6a81b895453b6` is the smallest suitable distinct workflow identity: it is the direct child of release `78c882345d522d7a83cae9296c26499d49ab2521`, and its sole diff is the focused independent Review document. Every selected Runtime workflow, compatibility-profile, provisioning, registration, registry-integration and Linux-validator blob is byte-identical between the two commits. The selected Runtime workflow blob is `6a322f84c087d7783c6b3a5e38f4f605678f0d3b` / SHA-256 `cc13c607d864f7ca2ce0b3fe69a8b6e2d12e1d8d275a568ec8b47dff24f498a2`; provisioning payload is blob `367332034e2b504353c5b00c563bf27ed6f0c95c` / SHA-256 `c5ac861443d1dd741e78206ab78ab8c7e0b58f55ed87be380260bc4aa613c22e`; registration payload is blob `1281a207e5ebe58b7fb78900213a44ef26ad7e0a` / SHA-256 `7c773a341cfd629dae2985f7c51792aee6f917ca5df7733f7200d26e780da94b`. The versioned registration invocation remains blob `96cb85aa281138385652bd0ea816450107000493`; its decoded canonical wrapper is 6,894 bytes with SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`.

At proposal time, `65bed814...` was not remotely reachable and remote `main` was exact release `78c88234...`; the authorized attempt later performed the specified normal non-force fast-forward `65bed8141c28b735c97a741587f6a81b895453b6:refs/heads/main`. This made the dispatch head and `workflow_commit` distinct without rebuilding or changing the immutable Product Release. The active workflow is `.github/workflows/cwt-runtime-validation.yml`; Environment `cwt-stage6-runtime-validation` requires reviewer `czy282923753`, permits self-review, and its sole branch policy is `main`. Before the attempt, repository runner inventory and queued/in-progress/waiting runs were all zero.

The exact future workflow inputs are:

| Input | Exact decision value |
| --- | --- |
| dispatch ref / `workflow_commit` | `main` after the one tools-only Push / `65bed8141c28b735c97a741587f6a81b895453b6` |
| `release_commit` | `78c882345d522d7a83cae9296c26499d49ab2521` |
| `index_digest` | `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e` |
| `evidence_run_id` | successful Build Once run `34082239544` |
| `runner_nonce` | one fresh, execution-time 128-bit lowercase hex value; never reuse or preselect an old nonce |

The purchase boundary is one new disposable **CVM**, never the long-term Production Lighthouse `lhins-3c2vknjb` / `cwt-production-sg`: Singapore Zone 2; `SA5.MEDIUM4`; 2 vCPU / 4 GiB; current public Ubuntu Server 24.04 LTS 64-bit image providing native `amd64`; one 60 GiB general-purpose SSD system disk; one ordinary public IPv4 at 5 Mbps with traffic billing; TAT-only management; no SSH, HTTP/S, ICMP, DIND or public inbound rule. Create one attempt-named temporary security group with exactly zero inbound rules and one priority-1 IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. Select an existing non-Production Singapore VPC/subnet only after read-only confirmation; do not create, edit or delete shared network resources. All historical instance, disk, public-IP, security-group, image, VPC/subnet, nonce, token and TAT IDs are evidence only and must not be reused or assumed current.

Authenticated Tencent purchase-form preflight completed read-only at `2026-09-07 12:52:24 +0800`; no order, resource or network mutation was submitted. The current Singapore Zone 2 form explicitly shows exact `SA5.MEDIUM4` (2 vCPU / 4 GiB, AMD EPYC Bergamo) as `库存充足`, with reference compute price CNY `0.35/hour` after the displayed overseas-region 7.8-discount (CNY `0.45/hour` undiscounted). Public `Ubuntu Server 24.04 LTS 64位` is selectable as image `img-mmytdhbn`; its image entry shows 20 GiB and supplies the required public `amd64` operating system for this attempt. One 60 GiB `通用型SSD云硬盘` system disk is selectable; the exact base configuration displays CNY `0.41/hour`, so the disk component is CNY `0.06/hour` by subtraction from the displayed compute reference. A same-form 50 GiB comparison displayed CNY `0.40/hour`, consistent with a CNY `0.01/hour` incremental cost for the extra 10 GiB. These disk components are derived from the live form totals rather than separately itemized provider line items.

The same current form selects one independent ordinary BGP public IPv4, `按流量计费`, at a 5 Mbps cap and displays CNY `0.79/GB` after a CNY `0.01/GB` saving; it adds no separately displayed hourly IPv4 charge. The form states that traffic charges settle once per hour. Compute/storage are displayed per hour, but the form exposes no finer billing increment, minimum charge, tax or other short-attempt fee; therefore none is assumed. The selected base hourly total remains CNY `0.41/hour`, with outbound traffic variable at CNY `0.79/GB` and no defensible fixed all-in total.

The existing non-Production network currently offered for Singapore Zone 2 is `vpc-piootcsf | Default-VPC（默认） | 172.22.0.0/16` with `subnet-6uvqq9wg | Default-Subnet（默认） | 172.22.0.0/20`; the form reports 4,093 available subnet IPs. This read-only confirmation authorizes selection only within a later approved attempt, not modification of either shared object. The purchase form also exposes `自动化助手` as a selected free component and states that it installs the agent for command execution without SSH, confirming TAT eligibility for this exact image/shape. The form's default new-security-group choices include public inbound rules and are unsuitable; the approved attempt must instead use the separately created zero-inbound/one-ALL-outbound temporary security group defined above.

For the 90-minute resource-life cap, base compute/storage exposure is CNY `0.615`. A conservative 5 GB outbound allowance costs CNY `3.95`; 5 GB exceeds the roughly 3.375 GB theoretical transfer at a sustained 5 Mbps for the full 90 minutes. The practical Owner budget recommendation is therefore **CNY 10 maximum estimated attempt cost**, leaving about CNY `5.435` contingency above the CNY `4.565` base-plus-allowance estimate. This is an execution stop/approval budget, not an enforceable Tencent per-attempt spending cap: the purchase form shows no such cap, traffic is variable and hourly-settled, and the mandatory 90-minute teardown remains the controlling exposure boundary. Availability and price must be reconfirmed immediately before an authorized purchase if this quote is no longer fresh.

The minimal one-shot action ledger is: one tools-only Push; one fresh nonce; one temporary security group and one outbound rule; one CVM purchase including its system disk and ordinary public IPv4; one exact provisioning TAT invocation as `root` from `/root`, with no parameters or credentials, COS output disabled and timeout `600` seconds; only after terminal `SUCCESS` plus exit `0`, one repository-scoped short-lived Runner registration token; one exact versioned registration TAT invocation as `ubuntu` from `/home/ubuntu`, parameter substitution enabled, `saveCommand=false`, COS output disabled and timeout `600` seconds; one ephemeral Runner named `cwt-tencent-sg-<nonce>` with cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-<nonce>`; one workflow dispatch and one Environment approval; and one first-attempt-only Runtime job capped by its existing `60`-minute timeout. A recommended outer resource-life cap is `90` minutes from CVM creation through confirmed deletion. Any setup, registration, binding, approval, materialization, validation or cleanup failure stops the attempt with no second token, TAT invocation, Runner, dispatch, host or retry.

The only approved credential transmission should be the one short-lived GitHub repository Runner registration token through the existing TAT hidden parameter `CWT_REGISTRATION_TOKEN`. The same invocation supplies fresh nonce, exact Runner name and repository as non-secret parameters. The canonical embedded wrapper decodes and hash-checks the reviewed 3,752-byte registration payload, executes it once with tracing disabled, removes its restricted temporary file and never saves command/COS output. Do not place the token in command text, files, pipes, retained output or another channel. The Runtime workflow later uses only its GitHub-provided job token for read-only artifact and private GHCR access.

A successful actual result is one GitHub job `success` plus the validator's machine-readable `{"status":"PASS","reasonCode":null,"evidence":"cwt-runtime-outcome"}` after it verifies the exact evidence/index, selects native `linux/amd64`, confirms Docker `29.6.2` / Compose `5.3.1`, pulls only digest-qualified Product/PostgreSQL/Valkey images, verifies bundle and three-service topology, migrates an isolated Synthetic database, starts `postgres`, `valkey-staging` and `web-staging`, obtains live/readiness/root HTTP `200`, confirms root noindex, zero published ports and non-root/read-only Web, then removes its Compose consumers, networks, pulled references and Synthetic host paths. A non-PASS reason or cleanup failure remains failure; it is not repaired in place.

Mandatory outer teardown applies after success or failure: wait for the ephemeral Runner to exit and deregister; remove only a residual exact runner registration if automatic deregistration failed; destroy the one attempt CVM and its system disk with zero retained disks/EIP; verify release of its ordinary public IPv4; confirm the temporary security group has zero associations and delete it; then prove exact instance/disk/public-IP/security-group and repository-runner absence. Preserve the private GHCR Release and Build Once artifact. Do not touch Production Lighthouse, `cwt-production-sg`, shared VPC/subnet, COS, DNS, other worktrees or unrelated resources.

The Owner decision subsequently covered this exact one-shot Push, quoted CVM purchase/resource cap, temporary network boundary, at most two TAT invocations, one hidden registration-token transmission, one Runner, one Runtime dispatch/approval and mandatory teardown. It did not include a Product rebuild, Registry write, Tencent Production/Lighthouse, COS/provider recovery, protected activation, monitoring transport, RPO/RTO, deployment or phase acceptance. Stage 7 monitoring transport and Owner RPO/RTO remain later operational/recovery gates and do not block an isolated image Runtime Validation.

## Authorized native Runtime Validation attempt outcome

The Owner authorized exactly one native exact-digest Runtime Validation attempt with a CNY 10 maximum estimated cost. The frozen tuple was tools/workflow commit `65bed8141c28b735c97a741587f6a81b895453b6`, immutable release `78c882345d522d7a83cae9296c26499d49ab2521`, index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`, Build Once evidence run `34082239544` / artifact `10004387422`, nonce `8be204cb891a7c69907f870bf7ebda61`, Runner name `cwt-tencent-sg-8be204cb891a7c69907f870bf7ebda61`, and cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-8be204cb891a7c69907f870bf7ebda61`. Immediate prechecks reconfirmed the clean reviewed source, direct tools/release ancestry and byte-identical workflow, private GHCR exact index/version, unexpired detached artifact, required Environment configuration, zero repository runners and active runs, current stock and quote. One normal non-force tools-only Push advanced remote `main` to exact `65bed814...`.

The Operator created temporary security group `sg-j6sbvg5a` with zero inbound rules and exactly one priority-1 IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. One disposable Singapore Zone 2 CVM, `ins-lv1uer9i` / `cwt-tencent-sg-8be204cb891a7c69907f870bf7ebda61`, used `SA5.MEDIUM4` (2 vCPU / 4 GiB), public Ubuntu 24.04 image `img-mmytdhbn`, 60 GiB general-purpose SSD `disk-f81yg4js`, ordinary public IPv4 `43.134.10.173` at 5 Mbps traffic billing, private address `172.22.0.6`, and the approved existing VPC/subnet `vpc-piootcsf` / `subnet-6uvqq9wg`. Monitoring and security agents were disabled; TAT was selected. The displayed base rate was CNY 0.41/hour plus CNY 0.79/GB traffic.

Exactly one provisioning TAT invocation, `inv-28c8q1gtc2` using command `cmd-l5h9i8jv`, ran as `root` from `/root`, with parameters disabled, COS output disabled and timeout `600`. Before submission, its 10,206-byte payload was reconfirmed against reviewed SHA-256 `c5ac861443d1dd741e78206ab78ab8c7e0b58f55ed87be380260bc4aa613c22e`. The invocation ran from `2026-09-07 13:07:16 +0800` to `13:10:05 +0800` and ended `命令失败`, exit code `28`. Docker installation reached its exact reviewed versions, but Runner acquisition stopped at `curl: (28) Failed to connect to github.com port 443 after 132627 ms: Couldn't connect to server` while downloading the GitHub Actions Runner archive. This is a disposable-host/provider egress connectivity failure before Product Runtime execution, not evidence of a Product Runtime defect or PASS.

The one-shot stop rule then applied. No Runner registration token was generated; there was no registration TAT invocation, Runner registration, Runtime workflow dispatch, Environment approval, Product image materialization or validator execution. No retry, replacement host or second invocation was used. The attempt therefore produced no native Product Runtime result, and Stage 6 remains Partial / HOLD.

Mandatory teardown completed after the terminal failure. The CVM was destroyed with immediate resource release together with its system disk, with zero retained disks and zero EIP; Singapore CVM, cloud-disk and public-IP inventories were each confirmed empty. The temporary security group had zero associations, was deleted, and the Singapore security-group inventory was confirmed empty. GitHub repository runner inventory was reconfirmed at zero, and the Runtime workflow list contained no new run; its latest entry remained historical run `33959942641` from 2026-09-05. Remote `main` remains exact tools commit `65bed814...`. The private GHCR release and Build Once artifact remain the retained custody authorities and were not modified.

The disposable resource lived for approximately 11 minutes. Its nominal base exposure was within the approved CNY 10 boundary, with limited traffic at the displayed variable rate; the exact provider-settled charge was not yet available and is not inferred. Final disposition is **BLOCKED** for this one authorized attempt because native GitHub connectivity failed at the provisioning boundary. This report grants no retry, replacement host, additional token/TAT invocation, workflow dispatch, cloud purchase, phase acceptance or architecture change authority.

## Local provisioning download robustness Candidate

The failed invocation exposed a narrow responsibility mismatch. The outer provisioning/registration/Runtime operation is intentionally one-shot, but the sole public Runner archive transfer was also a single unbounded-connect `curl` call. A transient TCP connection stall therefore consumed approximately 133 seconds and terminated the whole purchased-host attempt before credentials, registration or Product Runtime. The destroyed host cannot establish whether the underlying route, Tencent path or GitHub availability was transient, and this local correction does not claim that it can reproduce or guarantee Singapore connectivity.

The local Candidate keeps the same endpoint, Runner version and frozen archive SHA-256, and replaces that one transfer invocation with standard bounded `curl` behavior inside the existing provisioning script. Connection establishment is capped at 15 seconds; each transfer is capped at 390 seconds; `--retry 2` permits at most three total transfers; delay is fixed at two seconds; connection-refused and curl's standard transient HTTP/timeout cases are eligible; and retries may start only within the first 45 seconds. A final transfer started inside that window is bounded to less than 435 seconds after the initial transfer began. Against the unchanged 600-second outer TAT timeout, this leaves at least 165 seconds minus earlier package-install elapsed time for hash verification, extraction, dependency installation and readiness checks. The failed live invocation reached the archive step after approximately 37 seconds, but future package timing remains external and variable.

Failed transfers remove their output, and the existing exact SHA-256 gate remains immediately before extraction. TLS failures and digest mismatches are not retryable. No mirror, proxy, fallback endpoint, alternate image, cache, custom retry loop, second payload path, token, registration, Runner, workflow or persistent state was added. The original failed payload SHA-256 `c5ac861443d1dd741e78206ab78ab8c7e0b58f55ed87be380260bc4aa613c22e` remains immutable attempt evidence; the successor local payload SHA-256 is `9fe26979d767c908dac32d55d9f6b2c71df7d094ee7ce3537756cd11c8356e50`. Runtime workflow and Product release/image bytes remain unchanged.

Focused Local/Synthetic verification uses the production download helper against a loopback HTTP fixture. It proves a transient `503` followed by exact bytes completes on the second transfer, repeated `503` responses stop after exactly three transfers and leave no output, an intentionally stalled response fails at the supplied transfer limit, and a completed wrong-digest response returns `67` and never reaches acceptance. Static checks freeze the production limits, one curl retry site, no `--retry-all-errors`, partial-output removal, unchanged 600-second TAT authority and absence of a custom loop. This is transport-behavior evidence only; no public download, Tencent probe, VM, TAT, token, Runner, workflow dispatch, Registry write or Product execution occurred.

Operational and persistent complexity remain level: one existing download call became one named helper using curl's built-in controls, while every outer one-shot stop rule remains unchanged. The next gate is focused independent Review of this tools-only Candidate. Only a reviewed successor and a new explicit one-attempt external authorization may reuse the still-valid release `78c882345d522d7a83cae9296c26499d49ab2521`, index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e` and artifact `10004387422`; this Candidate grants no external retry or execution authority.

### Reviewed tools-delivery closure

Focused independent Review `80811200b04b849b94e65eb3f967ce60b4af39cf`, whose parent is exact implementation Candidate `f63082ab6823c81c2e234a410277c47de937e384`, returned PASS with no blocking or nonblocking finding. It confirmed the finite curl semantics, final error propagation, exact digest-before-extraction boundary, Local/Synthetic transport evidence and unchanged outer one-shot authority. It did not claim Tencent reachability or authorize external execution.

Local tools-delivery merge `345f8b15637578d046a9a07c3bfa3e74c0f72e0c` has parents `80811200b04b849b94e65eb3f967ce60b4af39cf` and current remote-main source `65bed8141c28b735c97a741587f6a81b895453b6`. The sole merge conflict was the independently reviewed integration document: the `80811200...` version was retained because it contains the exact complete `65bed814...` historical Review bytes as its prefix and appends the latest provisioning-download PASS once. No executable/source conflict occurred. `65bed814...` is an ancestor of `345f8b15...`, so remote `main` can advance to it by one normal non-force fast-forward. All non-documentation bytes in the delivery tree equal reviewed Candidate `f63082ab...`; the provisioning payload remains reviewed SHA-256 `9fe26979d767c908dac32d55d9f6b2c71df7d094ee7ce3537756cd11c8356e50`.

The exact future dispatch-ready `workflow_commit` and tools delivery identity is `345f8b15637578d046a9a07c3bfa3e74c0f72e0c`, not the later report-only commit containing this closure. Product inputs remain unchanged: release `78c882345d522d7a83cae9296c26499d49ab2521`, index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`, evidence run `34082239544` and artifact `10004387422`. The next gate is one concrete fresh Owner decision for the existing CNY 10, one-host, one-attempt scope, with availability and quote refreshed immediately before purchase. No Push, cloud purchase, TAT invocation, token, Runner, workflow dispatch, Registry write, Product rebuild or phase advancement occurred during this local closure.

## Reviewed tools native Runtime Validation attempt outcome

The Owner subsequently authorized exactly one native Runtime Validation attempt for the reviewed tools delivery, within the existing CNY 10 maximum estimated cost and 90-minute resource-life boundary. The frozen tuple was workflow/tools `345f8b15637578d046a9a07c3bfa3e74c0f72e0c`, immutable release `78c882345d522d7a83cae9296c26499d49ab2521`, index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`, Build Once evidence run `34082239544` / artifact `10004387422`, nonce `8e4417cf446a20cda43fe16a1d6cfd2d`, Runner name `cwt-tencent-sg-8e4417cf446a20cda43fe16a1d6cfd2d`, and exact cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-8e4417cf446a20cda43fe16a1d6cfd2d`. Immediate prechecks reconfirmed exact ancestry and clean source, the private GHCR index/version, unexpired detached artifact, Environment controls, zero active repository runs/runners and current Tencent stock/quote. One normal non-force tools-only Push advanced remote `main` from `65bed814...` to exact `345f8b15...`; the later report-only closure commit remained local and was not pushed.

The Operator created temporary Security Group `sg-az2m8shc` / `cwt-runtime-8e4417cf446a20cda43fe16a1d6cfd2d` with zero inbound rules and exactly one priority-1 IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. One disposable Singapore Zone 2 CVM, `ins-7kjj8jd6` / `cwt-tencent-sg-8e4417cf446a20cda43fe16a1d6cfd2d`, used `SA5.MEDIUM4` (2 vCPU / 4 GiB), public Ubuntu 24.04 image `img-mmytdhbn`, 60 GiB general-purpose SSD `disk-qljqmqjq`, ordinary public IPv4 `101.32.109.111` at 5 Mbps traffic billing, private address `172.22.0.16`, and the approved existing VPC/subnet `vpc-piootcsf` / `subnet-6uvqq9wg`. Security and monitoring agents were disabled; TAT remained enabled. Creation was `2026-09-07 13:44:59 +0800` (`13:44:45` for the disk), at the displayed CNY 0.41/hour base plus CNY 0.79/GB traffic.

Exactly one reviewed provisioning TAT invocation, `inv-v8ca4n0x3h` / task `invt-v8ca4n0x3i`, ran as `root` from `/root`, with parameters disabled, COS output disabled and timeout `600`. Its reviewed payload was reconfirmed at SHA-256 `9fe26979d767c908dac32d55d9f6b2c71df7d094ee7ce3537756cd11c8356e50` before execution. The invocation ran from `13:54:53` to `13:56:07 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_PRE_REGISTRATION_OK os=ubuntu-24.04 arch=amd64 docker=29.6.2 compose=5.3.1 runner=2.337.0 runner_tree_owner=ubuntu diag_write_probe=PASS docker_user_probe=PASS`.

Only after that PASS, one repository-scoped short-lived Runner registration token was generated and transmitted through the approved hidden TAT parameter; it was never placed in command text or a retained local/remote file. Exactly one reviewed registration invocation ran as `ubuntu` from `/home/ubuntu`, with parameter substitution enabled, COS output disabled and timeout `600`. Its decoded canonical wrapper was reconfirmed at SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`. It ran from `13:59:49` to `13:59:55 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_REGISTRATION_MATERIALIZATION_PASS`. Repository runner ID `30` appeared online and idle with the exact name and six labels.

Exactly one [Runtime workflow run `34088960229`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34088960229) was dispatched on `main`, with job `101638403163`, event `workflow_dispatch`, attempt `1` and head SHA `345f8b15...`; its one Environment deployment was approved once. The job ran from `2026-09-07T06:00:38Z` through `06:05:54Z` and completed `failure`. Exact tools and release checkouts, Node installation, unique Tencent runner binding, detached artifact download and release-header reconciliation all passed. The artifact download identified artifact `10004387422`, size `9,086,518` bytes and expected/computed SHA-256 `49ac1a4e33d6e1efe3e20281e71a142cac735a993243d613a2b799246a898e55`; the existing release-header checker returned `{"status":"PASS"}`.

The next step, `Install hash-pinned patched ORAS`, attempted the sole reviewed URL `https://github.com/oras-project/oras/releases/download/v1.3.3/oras_1.3.3_linux_amd64.tar.gz` and failed at `2026-09-07T06:05:47Z` with `curl: (28) Failed to connect to github.com port 443 after 136440 ms: Couldn't connect to server`. This was a public dependency transport failure before credentials or Product Runtime. GHCR authentication, exact-digest OCI materialization and the sole accepted Linux Runtime validator were skipped. The job therefore produced no Product Runtime PASS or FAIL result and does not establish any Product defect. The one-shot rule was honored: no retry, rerun, replacement host, second token, TAT invocation, Runner or workflow dispatch occurred.

The ephemeral runner exited and automatically deregistered; final repository runner inventory is zero. Mandatory outer teardown then destroyed `ins-7kjj8jd6` with immediate resource release, including `disk-qljqmqjq`; the destruction summary retained zero disks and zero EIP. Singapore CVM, cloud-disk and public-IP inventories each displayed zero entries. Security Group `sg-az2m8shc` was separately confirmed with zero associations, deleted, and the Security Group table displayed zero entries. Shared VPC/subnet, Production Lighthouse/security group, private GHCR Release and Build Once artifact were preserved. The local provisioning payload copy was removed. Remote `main` remains exact `345f8b15...`.

The disposable resource lived for approximately 31 minutes, within the approved time and CNY 10 exposure boundary; the exact provider-settled charge is not yet available and is not inferred. Final disposition is **BLOCKED** for this single authorized attempt because the sole validator did not run. A later coordinator steering allowing bounded same-host recovery for a pre-registration public dependency failure arrived only after this formal Runtime run had already failed and teardown had completed; its explicit no-recreation rule therefore applies. This report grants no retry, replacement host, additional token/TAT invocation, Runner, workflow dispatch, cloud purchase, phase acceptance or architecture change authority.

## Recovery-aware reviewed native Runtime Validation outcome

The Owner approved one complete recovery-aware outer execution for reviewed workflow/tools commit `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c`, immutable Product release `78c882345d522d7a83cae9296c26499d49ab2521`, index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`, Build Once evidence run `34082239544` / artifact `10004387422`, nonce `ce054bf9055f2bd04481692a936033ba`, Runner name `cwt-tencent-sg-ce054bf9055f2bd04481692a936033ba`, and exact cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-ce054bf9055f2bd04481692a936033ba`. The accepted recovery ceiling allowed at most one same-host preparation recovery only before GHCR authentication, OCI materialization and Product-validator start; it did not permit a new host or a second workflow dispatch.

Immediate prechecks reconfirmed the exact tools/release/evidence tuple, unexpired detached artifact, protected Environment configuration, zero repository runners and active runs, and current Tencent Singapore capacity and quote. One normal non-force tools-only Push advanced remote `main` to exact `da2fa43d...`. The displayed disposable-host rate was CNY 0.41/hour plus CNY 0.79/GB ordinary public traffic, within the CNY 10 cap.

The Operator created temporary Security Group `sg-7l0v73tq` / `cwt-runtime-ce054bf9055f2bd04481692a936033ba` at `2026-09-07 15:16:41 +0800`. It had zero inbound rules and exactly one priority-1 IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. One disposable Singapore Zone 2 CVM, `ins-9kbfacma` / `cwt-tencent-sg-ce054bf9055f2bd04481692a936033ba`, used `SA5.MEDIUM4` (2 vCPU / 4 GiB), public Ubuntu 24.04 image `img-mmytdhbn`, 60 GiB general-purpose SSD `disk-8yf3topa`, ordinary public IPv4 `43.173.164.140` at 5 Mbps traffic billing, private address `172.22.0.4`, and approved existing VPC/subnet `vpc-piootcsf` / `subnet-6uvqq9wg`. Security and monitoring agents were disabled; TAT remained enabled. The disk was created at `15:20:09` and the instance at `15:20:27 +0800`.

Exactly one provisioning TAT invocation, `inv-v8ccpg0ie0` / task `invt-v8ccpg0ie1`, ran as `root` from `/root`, with parameters disabled, COS output disabled and timeout `600`. Its exact 19,915-byte reviewed payload was reconfirmed at SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0`. The invocation ran from `15:23:13` to `15:25:05 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_PRE_REGISTRATION_OK os=ubuntu-24.04 arch=amd64 docker=29.6.2 compose=5.3.1 runner=2.337.0 preparation=fresh runner_tree_owner=ubuntu diag_write_probe=PASS docker_user_probe=PASS`. No preparation recovery was needed.

Only after that PASS, one fresh repository-scoped short-lived Runner registration token was generated and transmitted through the approved hidden TAT parameter. The local UI bridge temporarily held it in one mode-`0600` file and served it only on loopback; both the file and loopback helper were removed immediately after registration, and the token entered no retained output, Git file or TAT command text. This deviated from the stricter preferred clipboard-only local handling and is recorded as an operator-process finding; it did not create a remote credential file or a second registration path. Exactly one registration TAT invocation, `inv-s8ccsx0q8f` / task `invt-s8ccsx0q8g`, ran as `ubuntu` from `/home/ubuntu`, with parameter substitution enabled, COS output disabled and timeout `600`. Its decoded 6,894-byte canonical wrapper was reconfirmed at SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`. The invocation ran from `15:26:55` to `15:27:01 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_REGISTRATION_MATERIALIZATION_PASS`. Repository Runner ID `31` appeared online and idle with the exact name, OS and six labels.

Exactly one [Runtime workflow run `34095608690`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34095608690) was dispatched on `main`, with job `101658484898`, event `workflow_dispatch`, attempt `1` and head SHA `da2fa43d...`; its one Environment deployment was approved once. The job ran from `2026-09-07T07:28:33Z` through `07:31:20Z` and completed `failure`. Exact tools and release checkouts, Node installation, unique Tencent Runner binding, detached artifact download, release-header reconciliation, hash-pinned ORAS installation and private GHCR authentication all passed. Exact-digest OCI materialization also returned `PASS` for the frozen release and index.

The sole accepted Product validator then executed exactly once and returned `{"status":"NOT_PASS","reasonCode":"bundle_authority_failed","evidence":"cwt-runtime-outcome"}` with process exit code `1`. This is a formal Product Runtime NOT PASS at the bundle-authority boundary. It is more decisive than the earlier public-download blocks, but the sanitized outcome does not by itself identify a repair. Because GHCR authentication, OCI materialization and the validator had started, the recovery admission was closed. No provisioning recovery, second token, second registration, workflow rerun, new dispatch, new host, Product rebuild, Registry write or in-place repair was used.

The ephemeral Runner exited and automatically deregistered; repository Runner inventory returned zero. After explicit action-time Owner confirmation, mandatory outer teardown immediately destroyed `ins-9kbfacma` and released `disk-8yf3topa` and ordinary public IPv4 `43.173.164.140`, retaining zero disks and zero EIP. Singapore CVM, cloud-disk and public-IP inventories each displayed zero entries. Security Group `sg-7l0v73tq` was confirmed at zero associations, deleted, and the Singapore Security Group inventory displayed zero entries. Shared VPC/subnet, Production Lighthouse/security group, private GHCR Release and Build Once artifact were preserved.

The disposable CVM lived for approximately 15 minutes, below the 80-minute teardown-start and 90-minute final-lifetime ceilings and within the approved estimated cost boundary; exact provider-settled cost is not yet available and is not inferred. Final disposition is **NOT PASS / HOLD**. The next gate is root-cause analysis and a reviewed correction for `bundle_authority_failed`, followed by an independently authorized future Build/Runtime decision as required by the correction scope. This report grants no retry, new cloud purchase, Product repair, Registry write, deployment, phase acceptance or architecture change authority.

## Local root-cause diagnosis of `bundle_authority_failed`

Bounded read-only diagnosis identifies one deterministic startup failure before any Product bundle assertion. At exact tools commit `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c`, `verifyBundle` runs the immutable Product subject as `node /app/scripts/check-public-bundle.mjs` with `CWT_BUILD_DIR=/app/.next/standalone/.next`, `linux/amd64`, no network, a read-only filesystem, UID/GID `10001:10001`, all capabilities dropped and `no-new-privileges`. At exact Product release `78c882345d522d7a83cae9296c26499d49ab2521`, that checker statically imports `typescript` before it reads the build or evaluates an assertion. The release declares `typescript@5.9.3` only in `devDependencies`; its Docker build replaces the build dependency tree with `pnpm install --prod` and copies that production-only `/app/node_modules` into the runtime image.

The concrete failing invocation is `docker run --rm --pull never --platform linux/amd64 --network none --read-only --user 10001:10001 --cap-drop ALL --security-opt no-new-privileges:true --env CWT_BUILD_DIR=/app/.next/standalone/.next --entrypoint node ghcr.io/czy282923753/cwt-cloudwave-textile@sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e /app/scripts/check-public-bundle.mjs`. Its first unavoidable failing operation is ESM resolution of the static `import ts from "typescript"`; no Product bundle assertion runs.

The detached Build Once artifact `10004387422` binds the same release and index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e`. Its release-declared amd64 and arm64 SBOM files independently contain one `next@16.2.12` package and one `tsx@4.23.1` package but zero `typescript` packages. Their computed SHA-256 values are the exact release descriptors `feeb2e82b70078f21d014d45fda0ca497e196206785845e504ee7629758ef471` and `a7e54edddbd0b23d50614c5cccb024f7429c78`. A reproducible Local/Synthetic check archived the exact release tree to a fresh temporary directory and ran `pnpm install --prod --offline --frozen-lockfile --trust-lockfile --ignore-scripts`; `next` and `tsx` resolved, `node_modules/typescript` was absent, and `node scripts/check-public-bundle.mjs` exited `1` with Node `ERR_MODULE_NOT_FOUND: Cannot find package 'typescript'`. It failed during ESM module resolution, before `.next` access. The scratch dependency tree was then removed.

The failed cloud process's raw child stderr was not retained, so this diagnosis does not claim recovery of its byte-for-byte exception. Run `34095608690` logged only the validator's sanitized `bundle_authority_failed`, and its artifact inventory is exactly zero. The validator's generic process wrapper captures child stdout/stderr but converts every nonzero child status into the same broad reason and discards the captured detail; it did write `linux-runtime-validation.json` and its checksum under the ephemeral Runner temporary directory, but the workflow has no post-failure upload step and host teardown removed that directory. Nevertheless, exact source, exact-image SBOM and exact production-lock reproduction establish that the shipped checker cannot start in either emitted child. This distinguishes a **bad Product image dependency closure** from a validator OS, Docker, user, filesystem, network or bundle-content mismatch. No bundle-content assertion was reached, so the failure is not evidence that the underlying compiled public bundle violates an assertion.

The defect escaped because each existing check observes a different, weaker boundary. `pnpm check:bundle` and its focused tests invoke the checker from the development workspace where dev dependencies are installed. `pnpm build` is only `next build`, so the Docker build never invokes the checker after its production-only reinstall. Build Once loads each emitted child for SBOM and runs the exact-image Sharp smoke, but does not run the bundle checker. The Runtime validator test freezes the checker path in source text but does not execute that command against a production-only image. All of those tests can pass while the shipped checker is unstartable.

The smallest complete correction is a new Product Candidate with these bounded changes:

1. Move the already locked exact `typescript@5.9.3` entry from `devDependencies` to `dependencies` in `package.json` and the root importer in `pnpm-lock.yaml`, because the checker is intentionally shipped and executed from the runtime Product image.
2. Add the matching exact-version presence assertion to the `Dockerfile` runtime stage alongside the existing Node and `tsx` assertions.
3. Extend the existing Build Once post-emission child gate in `deploy/scripts/build-release-once.mjs` to execute `/app/scripts/check-public-bundle.mjs` against each loaded child with the same no-network, read-only, non-root and privilege-drop envelope used by Runtime, and add a focused rejection case in `deploy/scripts/build-release-once.test.mjs` for a child missing the checker's runtime dependency.

This path preserves the existing TypeScript AST authority and has the smallest behavioral surface. Its cost is that the compiler becomes part of the runtime image and SBOM. Pre-bundling the checker into a self-contained validation artifact could reduce runtime dependency surface, but it introduces another build artifact and equivalence boundary; removing the runtime check or replacing the AST logic with text matching would weaken the accepted bundle authority. The direct production-dependency correction is therefore recommended for this bounded remediation.

Because `78c882...` is immutable and both children lack the required package, it cannot be repaired or reused as the Product subject. The correction requires normal implementation, focused Review, a new Build Once execution, new release/index identities, and successful exact-image bundle evidence before any future native Runtime decision. Existing artifact `10004387422` remains useful only as retained failure/diagnostic evidence. This diagnosis authorizes no implementation, Push, Product rebuild, Registry write, cloud purchase, workflow dispatch or retry.

After the bundle gate, the current validator immediately starts digest-pinned PostgreSQL and Valkey, runs the selected migration, starts Web, checks the exact three-service set and container identity/security boundaries, and then probes live, readiness and root behavior. Compose normalization, topology validation, dependency digest checks, exact Product pull and image identity all necessarily completed before the recorded bundle reason, but infrastructure start and every later boundary were not reached. Before another one-shot native run, the new Build Once gate must prove both emitted children can complete the checker; a non-authoritative isolated amd64 compose/migration/Web smoke against the emitted Candidate should also be run where the build host can support it, reusing the production compose and probe logic rather than creating a second authority. Native Ubuntu/amd64 runtime and teardown remain the final authority for host-specific behavior.

Future failed runs should preserve bounded, sanitized diagnostics without retaining raw child output. `scripts/check-public-bundle.mjs` should emit a strict, allowlisted machine reason code for dependency bootstrap and each assertion class; `deploy/scripts/preflight-linux-runtime.mjs` should accept only that code, record it as a separate detail field while retaining `bundle_authority_failed` as the boundary reason, and map malformed/unknown output back to the broad fail-closed reason. Raw stderr, paths, matched needles, bundle excerpts and environment values must remain excluded. An `if: always()` step in `.github/workflows/cwt-runtime-validation.yml` should upload only the already generated `linux-runtime-validation.json` and checksum when present, with bounded retention. This reuses the existing outcome rather than adding a second evidence format and makes the next failure distinguishable without weakening sanitization.

The mode-`0600` local token bridge used during the completed attempt was an operator-process nonconformance with the explicit credential contract, which prohibited placing the short-lived Runner token in files, pipes or another channel. Loopback binding, restrictive mode, immediate file/helper removal, one successful registration and final zero-runner inventory bound the observed exposure and left no known retained residue, but they do not retroactively prove that no other local process could read the temporary value. This finding is independent of the Product failure and does not change its causal diagnosis. A future attempt must transfer the token directly through the authenticated UI's hidden parameter using an OS/browser clipboard cleared immediately after paste, or require direct user paste if automation cannot preserve that boundary; it must not recreate a file, local HTTP helper or pipe bridge.

## Local dependency-closure remediation Candidate

Following the accepted diagnosis, local code Candidate `d283e459a18a503119a28715572596b524209fbb` closes the causal Product boundary without changing checker semantics. Exact `typescript@5.9.3` moved from development to production in `package.json` and only the root dependency classification moved in `pnpm-lock.yaml`; no package version or resolved snapshot changed. The `Dockerfile` runtime stage now asserts that exact TypeScript version beside the existing Node and `tsx` assertions. This preserves the intentionally shipped TypeScript AST authority and makes its required runtime dependency explicit.

Build Once now executes the shipped checker against every loaded `linux/amd64` and `linux/arm64` child before accepting its SBOM/Sharp evidence. The command uses `--pull never`, no network, a read-only filesystem, UID/GID `10001:10001`, all capabilities dropped, `no-new-privileges:true`, the exact child platform and the same in-image build root and checker path used by Runtime. The child tag and existing `finally` cleanup are reused. A nonzero checker status throws inside the existing post-emission gate, removes the temporary loaded image, and reaches the existing `post_emission_gate_failed` revocation record rather than a successful release. The pinned runtime-package SBOM check now also requires `typescript@5.9.3`.

Runtime observability remains deliberately narrow. `scripts/check-public-bundle.mjs` emits exactly one fixed JSON detail on failure: `bundle_dependency_bootstrap_failed` when TypeScript import cannot start, or `bundle_assertion_or_unknown_failed` for every later uncaught failure. It retains its prior stderr and assertion behavior locally, but the Runtime process wrapper never forwards that stderr. `deploy/scripts/preflight-linux-runtime.mjs` accepts a detail only when the complete stdout is at most 256 bytes, parses as an exact two-field schema and contains one of those two allowlisted codes. Every unknown code, extra field, appended output, malformed JSON or oversized value becomes `failureDetailCode: null`; a nonzero child always remains boundary `reasonCode: bundle_authority_failed` and cannot produce PASS. The additive outcome field contains only that fixed scalar or null.

The Runtime workflow now uses the already pinned `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` in an `always()` step after the sole validator. It uploads only the existing `linux-runtime-validation.json` and `linux-runtime-validation.json.sha256` exact paths when present, with 30-day retention, no overwrite and no hidden files. It cannot upload raw stderr, command output, paths outside the existing outcome root, OCI, release evidence, Registry auth or Runner token; absence is ignored when validation fails before outcome creation. A successful validator followed by upload failure remains a failed job under normal Actions semantics.

Focused verification completed without a Product build:

- Lockfile policy and frozen production classification passed with `pnpm install --lockfile-only --offline --frozen-lockfile --ignore-scripts`; all 715 locked entries remained accepted.
- `deploy/scripts/build-release-once.test.mjs` passed 6/6. Its fresh production-only offline install runs the real shipped checker far enough to reach the Synthetic missing-build assertion without `ERR_MODULE_NOT_FOUND`, and its orchestration test freezes both child commands and proves a nonzero checker is rejected.
- `src/public-site/public-bundle-check.test.ts` passed 157/157 compiled-fixture checks, including fixed bootstrap and later-failure detail codes; existing bundle semantics remained intact.
- `deploy/scripts/preflight-linux-runtime.test.mjs` passed 15 tests with its one existing environment-dependent Scheduler Migration case skipped. It proves strict detail acceptance, malicious/extra output rejection, unchanged boundary failure and exact two-file workflow retention.
- `deploy/scripts/release-registry-integration.test.mjs` passed 11/11, preserving manual workflow separation, exact checkouts, read-only Runtime Registry permission and pinned Actions.
- Focused ESLint for all touched JavaScript/TypeScript files, syntax checks for the three changed executables, workflow YAML parsing and `git diff --check` passed.

No new immutable Product was built, loaded or published, so this Candidate does not claim an exact-image PASS, new SBOM, compose startup or native Runtime result. The current macOS/ARM64 host can exercise the production dependency install, Synthetic compiled bundle fixtures and both platform command orchestration, but it cannot satisfy the accepted native Ubuntu/amd64 host profile; running a full local Product smoke would also require the prohibited new Product build. After independent Review and a separately authorized Build Once, both emitted children must first pass the new in-image bundle gate. Before a later one-shot native decision, any feasible non-authoritative amd64 rehearsal must reuse `createRuntimeCommandPlan`, `validateComposeGraph`, the selected production Migration, exact service-set/container checks and `probeApplication` from `deploy/scripts/preflight-linux-runtime.mjs`; it must not introduce an alternate compose, migration or probe implementation. The accepted native validator remains the sole authority for Ubuntu/amd64 host behavior and teardown.

Rollback is one normal revert of `d283e459...`, which restores the previous dependency classification, Docker assertion, Build Once gate, broad-only Runtime outcome and no-upload workflow. That rollback also restores the known defect and cannot make release `78c882345d522d7a83cae9296c26499d49ab2521` valid. The immutable old release/index `sha256:fc96539ee4c51895c1c1fedc8ef873e2fc92b5898c55f273f03d81656d779f0e` and artifact `10004387422` remain retained historical failure evidence; they are unsuitable as a future Runtime subject because their shipped checker cannot start. No remote content was deleted or revoked, and historical Build Once success facts remain unchanged.

The next gate is separate focused independent Review of code Candidate `d283e459...`, including the production dependency surface, exact child gate, fail-closed detail parser and two-file artifact boundary. Only an accepted successor may define a future new Product release/index through a separately authorized Build Once. No Push, Registry write, Product build/rebuild/publication, workflow dispatch, cloud action, permission-policy change or phase acceptance occurred in this remediation.

## `S6-BD-M-01` Runtime outcome ownership correction

Independent Review `07b504619fc05b46a79624ec1fdebbefc3f0ba94` accepted every dependency, Build Once, checker-detail and sanitized-retention boundary in `d283e459...`, but correctly rejected the producer/consumer DAC mismatch as `S6-BD-M-01`: the validator ran as root and left a root-owned `0700` evidence directory containing root-owned `0400` files, while `upload-artifact` runs as the non-root Runner identity. Local correction Candidate `896dff79fd74ac69cce25c1ca4635fe05060c851` changes only `deploy/scripts/preflight-linux-runtime.mjs` and its focused test; the workflow and all previously accepted Product/checker files remain byte-unchanged from the reviewed Candidate.

The existing writer now derives the uploader only from the established root/SUDO_UID-to-subject-repository-owner identity check; no CLI UID, second command, copy or permission helper was added. Before writing, it requires the canonical evidence output to be one empty root-owned `0700` directory and requires its canonical parent to belong to that verified originating Runner UID with owner traversal. After writing the existing JSON and checksum, it requires exactly those two names, two canonical non-symlink regular files owned by root at `0400`, and an exact SHA-256 sidecar match. Only then does it transfer ownership of the two files to the verified UID and transfer the directory last, retaining file mode `0400` and directory mode `0700`. Root-owned non-sudo operation resolves to UID 0 and retains the original ownership. A handoff failure exits fail-closed as `outcome_handoff_invalid`; it cannot convert an earlier Product NOT PASS or a successful validator into workflow PASS.

The focused suite passed **16/17 with one unchanged environment-dependent Scheduler Migration skip**. Its real Linux isolation probe executes the actual writer as root, then drops the same process to UID/GID 1000: the consumer traverses the `0700` directory, reads exactly the JSON and checksum, parses the exact outcome and recomputes the matching digest. Both files are UID 1000 regular files at `0400`; there are no additional files. A sibling root-owned `0400` file retains its exact owner, group, mode and size and remains unreadable with `EACCES`. The same probe rejects a missing sudo-origin identity, a mismatched evidence-parent owner and a symlink output before any ownership mutation, and proves the UID-0 path remains root-owned. The existing mixed-revision actual CLI test now models its evidence parent as Runner-owned, reads the completed outcome successfully, and separately proves a root-owned parent produces exit `1`, empty stdout and `outcome_handoff_invalid`. Focused ESLint, Node syntax and diff hygiene passed.

This correction does not claim a live GitHub artifact upload, new Product image/SBOM, compose startup or native Runtime PASS. Those remain later externally authorized evidence. Rollback is a normal revert of `896dff79...`; doing so reopens deterministic uploader `EACCES` and is not an operationally valid release path. The next gate is the same independent Reviewer performing a narrow re-review of `S6-BD-M-01` only. No Push, Registry write, Product build, workflow run, cloud operation, permission-policy change or phase acceptance occurred.

## Authorized successor Build Once ancestry preflight outcome

Independent re-review delivery `c38b073a89802d0c53bcc7c61d4c95d292e94212` closed `S6-BD-M-01`, and the Owner authorized it as the exact new Product Build Once source, dispatch head and `release_commit`. Immediate read-only preflight confirmed a clean local source and accepted local tool boundary: macOS ARM64, Node `24.14.0`, pnpm `11.9.0`, Docker Engine `29.6.2`, Buildx `v0.35.0-desktop.2`, running Docker Desktop builder with both `linux/amd64` and `linux/arm64`, 155 GiB available disk, zero running containers, only pre-existing stopped containers/images, zero registered repository Runners, zero active repository workflow runs, the required `cwt-stage6-build-once` Environment/reviewer/branch policy, and a private `cwt-cloudwave-textile` GHCR package.

The mandatory remote-ancestry check did not pass. After a fresh fetch, remote `main` was exact `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c`; the merge-base of `da2fa43...` and authorized `c38b073...` was only `345f8b15637578d046a9a07c3bfa3e74c0f72e0c`, and `git merge-base --is-ancestor origin/main c38b073...` returned `1`. The branches therefore contain separate accepted deltas after `345f8b15...`. Publishing exact `c38b073...` to `main` would require a prohibited non-fast-forward/force update, while merging `da2fa43...` would create a new source/tree/release identity outside the exact reviewed and authorized tuple. The execution stopped before mutation rather than discarding either line of work.

No Push, registration-token acquisition, Runner registration, Environment approval, workflow dispatch, Product build/emission, GHCR write, artifact creation or new scratch occurred. Repository Runner and active-run inventories remained zero, and no owned cleanup was necessary. The authorized Build Once was **BLOCKED before gate-open**, so there is no Product success/failure or revocation claim. The next gate is a reviewed integration commit that preserves both `da2fa43...` and `c38b073...` histories, followed by a concrete Owner/coordinator decision updating the exact source/dispatch/release identity; the already approved one-run operational scope otherwise remains unconsumed.

## Runtime recovery and Product correction integration merge

Local dispatch-ready merge `1eecba71e6768661156d0292951f0918d29c00be` has exact parents `092555326f86c79fffb7eaa515b7176d38f19e6d` and remote-main `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c`; their merge-base is `345f8b15637578d046a9a07c3bfa3e74c0f72e0c`. Individually captured `git merge-base --is-ancestor da2fa43... 1eecba71...` and `git merge-base --is-ancestor c38b073... 1eecba71...` each returned exit `0`. This preserves the accepted recovery history, the complete dependency/outcome correction through independent re-review `c38b073...`, and the blocked-preflight record without rewriting either line.

There was no manual executable conflict. Git's three-way merge combined the two non-overlapping Runtime workflow changes automatically: the result is byte-equal to the mechanical merge of `c38b073...`, common base `345f8b15...` and `da2fa43...`, retaining both the bounded ORAS download flags and the sanitized two-file outcome upload. The sole manual conflict was the independent Review Markdown file because both branches appended sections at the same base location; it was resolved as exact common-base bytes followed by the complete `da2fa43...` recovery Review suffix and the complete `09255532...` dependency/handoff Review suffix. No text from either append was discarded or rewritten.

All single-sided executable/source blobs were compared directly with their accepted head and matched exactly: dependency, Docker, Build Once, checker and Runtime validator files equal `c38b073...`; recovery provisioning, registry integration and operations files equal `da2fa43...`; the principal blocked report remained equal to `09255532...` before this closure. The combined release-registry/workflow contract suite passed **13/13**, including bounded ORAS acquisition and the two-file retention contract. Workflow YAML parsing and staged diff hygiene passed. No Product build, Push, Runner/token, Environment approval, workflow dispatch, Registry write or cloud action occurred. The next gate is coordinator assessment of exact merge `1eecba71...` and explicit update of the still-unconsumed authorized Build Once tuple to that concrete identity.

## Authorized integrated-source Build Once outcome

After the coordinator explicitly updated the authorized tuple to exact source, dispatch head and `release_commit` `1eecba71e6768661156d0292951f0918d29c00be`, the one-shot preflight was repeated against the current external state. Local HEAD was clean at report-only commit `45b6232808910e1b234703f2d042731b74c33969`; remote `main` was `da2fa43dd9c4a1b2b669766b8afeecee8ebaa95c`, and its fast-forward ancestry to the authorized source passed. The macOS ARM64 host retained Node `24.14.0`, pnpm `11.9.0`, Docker Engine `29.6.2`, Buildx `v0.35.0-desktop.2`, both required build platforms and 155 GiB free space. There were zero running containers, 11 pre-existing stopped containers, zero repository Runners and zero active workflow runs. The private GHCR package and required `cwt-stage6-build-once` Environment reviewer/main policy remained in force.

The official macOS ARM64 Runner archive matched SHA-256 `5a2cd92908a93d7276a194e1de6008099f3e7946f3f8e14aa7a1a7b4a31fdec2`. Exactly one ephemeral repository Runner was registered as `cwt-build-once-mac-c44d1859278ad615c23f7634f4939b8d`, Runner ID `32`, with exact labels `self-hosted`, `macOS`, `ARM64` and `cwt-trusted-build-once`. The short-lived registration token stayed in operator-process memory and was passed directly to the official registration process; no token file, pipe, loopback bridge or retained output was created. One normal fast-forward Push advanced remote `main` from `da2fa43...` to exact `1eecba71...`.

Exactly one [Build Once workflow run `34132330763`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34132330763) was dispatched, with job `101775159015`, event `workflow_dispatch`, attempt `1` and exact head `1eecba71...`. Its one required Environment deployment was approved once under the Owner's explicit authorization. The job ran from `2026-09-07T14:20:00Z` through `14:27:15Z` and completed `failure`. Exact checkout/refusal gates, pnpm, Node, hash-pinned ORAS, accepted tool-boundary verification and exact dependency installation all passed. The sole Build Once authority then built and emitted an OCI subject far enough to load its `linux/amd64` child and write an SBOM containing 517 packages.

The new exact-image Product bundle gate returned `bundle_assertion_or_unknown_failed`. Its retained workflow log identifies the first decisive exception: `Error: ENOENT: no such file or directory, stat 'pnpm-lock.yaml'` at `assertFreshBuild` in `/app/scripts/check-public-bundle.mjs`. The checker treats `pnpm-lock.yaml` as one of the required freshness inputs, while the final runtime image copies `package.json`, `tsconfig.json`, `scripts` and `src` but does not copy `pnpm-lock.yaml`. Build Once therefore refused the emitted amd64 child as `linux/amd64 emitted Product bundle authority failed`, removed its temporary loaded image and exited `1`. The second `linux/arm64` child was not evaluated after the first failure. This is a deterministic Product packaging/checker contract mismatch; it is not evidence of a bundle-content assertion failure.

GHCR authentication, exact publication and detached success-evidence retention were all skipped, so this attempt created no new private GHCR release contract. The post-failure path uploaded only artifact `10022838887`, `cwt-revoked-release-evidence-1eecba71e6768661156d0292951f0918d29c00be`, size 517 bytes, retained for 30 days. Its sole JSON record binds release `1eecba71...` and emitted index `sha256:5ea0592ea6dbebcb26efd744e087cd4d7f527ca82e10dc3509f4342ef52bff93`, with `schemaVersion: 1`, `reasonCode: post_emission_gate_failed` and `recordedAt: 2026-09-07T14:26:57.457Z`. That index is revoked failure evidence and must not be used as a Runtime subject.

The ephemeral Runner completed the failed job, removed its own credentials/configuration, exited cleanly and automatically deregistered. Its owned local root, work tree, credentials, downloads and release scratch were removed. Final repository inventories showed zero Runners and zero queued/in-progress workflow runs; remote `main` remained exact `1eecba71...`; Docker retained zero running and the same 11 stopped containers. Shared images and caches were preserved, and the temporary Build Once amd64 tag was absent. No rerun, repair, second registration/token, extra Environment approval, Tencent Runtime, deployment or production operation occurred.

Final disposition is **Build Once NOT PASS / HOLD**. The next gate is bounded root-cause correction of the runtime-image/checker freshness-input contract, focused verification and independent Review. Any successor Build Once requires a new exact Candidate and separate explicit authorization; this report grants no retry, Push, Registry write, workflow dispatch, cloud purchase, Runtime execution, deployment or phase acceptance authority.

## Reviewed correction local `linux/amd64` rehearsal

Technical Escalation plan `7abb965529e4f3a3e72f0274efdef4c103809ec8` replaced the mixed workspace/artifact freshness contract at its causal boundary. Code Candidate `9f270171fa8e0a467c29283a70e6d91522545a39` deletes the incomplete source-mtime walk from the immutable-image checker, anchors shipped layout inputs to the checker's own root and moves fresh workspace sequencing into the existing build-then-check command. Independent Review `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641` accepted the correction and is code-identical to that Candidate for `Dockerfile`, `scripts/check-public-bundle.mjs`, `deploy/scripts/build-release-once.mjs`, `package.json` and `pnpm-lock.yaml`; the last file and the production Build Once implementation remain unchanged.

One disposable non-release rehearsal used an exact `git archive` of Review source `68c15e6b...`, tree `ba209c586704b157e8901496cfc0ac8c69f52d93`, with `CWT_RELEASE_ID=68c15e6b...` and commit epoch `1788794283`. The existing Docker Desktop engine `29.6.2`, Buildx `v0.35.0-desktop.2` and `desktop-linux` builder supplied emulated `linux/amd64`; the host began with 153 GiB free space, zero running containers, the same 11 historical stopped containers and no conflicting CWT build process. No daemon, builder, context or tool configuration was changed.

The existing Dockerfile `dependency-bundle` target was built once for `linux/amd64` with `--no-cache`, exact pinned inputs and its normal read-only dependency downloads. Its exported context contained only the expected `backup-root`, `bin`, `pnpm` and `pnpm-store` roots. The existing final `runtime` target was then built once with `--no-cache`, `--network=none`, disabled attached provenance/SBOM and that exact dependency context, and loaded only under unique local tag `cwt.local/rehearsal-6ad3f2e1`. Real Next `16.2.12`/Turbopack production compilation, TypeScript checking, static generation, production-only dependency replacement, Sharp runtime smoke and final package/tool assertions all passed. The resulting local image was `linux/amd64`, default UID/GID `10001:10001`, source label exact `68c15e6b...`, and temporary image ID `sha256:02fd59241ba2a87bc0f016ea7a417718156e3d13c91514daa94774902bfb53e3`.

The actual shipped checker then ran exactly once as `node /app/scripts/check-public-bundle.mjs` with `CWT_BUILD_DIR=/app/.next/standalone/.next`, `--pull never`, platform `linux/amd64`, network none, read-only root, UID/GID `10001:10001`, all capabilities dropped and `no-new-privileges:true`. It exited `0` and reported the complete public-bundle boundary verified: 400 eligible server runtime JavaScript files, required AI/server/File Scanner/Rate Limiter/Turbopack evidence, 20 public page manifests, 7 root chunks, 8 manifest chunks and 15 distinct chunk files. No lock/config/workspace-metadata lookup failed, and no content assertion failed. This closes the known real-Next/runtime-image checker contract for this single emulated platform.

After the checker stopped, the unique rehearsal tag/image and complete task-owned archive, dependency tar/context, metadata and build scratch were removed. Final state retained zero running and the same 11 stopped containers; the rehearsal tag was absent. Shared BuildKit/download caches and every prior image were preserved, with no prune. No `release.json`, lifecycle state, OCI release, detached release evidence, SBOM claim, GHCR authentication/write, Push, GitHub Runner/workflow/Environment action, cloud operation, Compose/Migration/Web start, deployment or production operation occurred.

Disposition is **LOCAL REHEARSAL PASS with accepted limits**. This proves one emulated `linux/amd64` real-Next image/checker path only. It does not prove native Ubuntu/amd64 behavior, `linux/arm64`, dual-platform identity/SBOM/private custody, full Runtime or Stage 6 acceptance. The next gate is coordinator assessment and a concrete, separately authorized formal dual-platform Build Once from exact accepted source; this local evidence grants no such external action authority.

## Authorized corrected-source formal Build Once and private publication

After the Owner explicitly authorized the concrete corrected-source scope, volatile preflight reconfirmed clean local report HEAD `7209aa88c874be3e5b7cba26fe8a04967665b68e`, exact source `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, tree `ba209c586704b157e8901496cfc0ac8c69f52d93`, remote `main` at ancestor `1eecba71e6768661156d0292951f0918d29c00be`, zero active repository workflows/Runners and zero running containers. The required `cwt-stage6-build-once` Environment retained its sole `main` branch policy and required Owner reviewer; the existing GHCR package remained private. Docker Engine `29.6.2`, Buildx `v0.35.0-desktop.2`, BuildKit `v0.31.2`, both target platforms and 149 GiB free disk were available without daemon/tool changes.

One normal fast-forward Push advanced remote `main` from `1eecba71...` to exact `68c15e6b...`. The verified official macOS ARM64 Runner `2.337.0` archive retained exact SHA-256 `5a2cd92908a93d7276a194e1de6008099f3e7946f3f8e14aa7a1a7b4a31fdec2`. Exactly one ephemeral Runner was registered as `cwt-build-once-mac-bb8f144a259b4b1aad65edc17cd2f111`, ID `33`, with exact labels `self-hosted`, `macOS`, `ARM64` and `cwt-trusted-build-once`. Its fresh short-lived registration token stayed in one operator process and entered no file, pipe, loopback helper or retained output. One [workflow run `34139907082`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34139907082) was dispatched from `main` with exact `release_commit=68c15e6b...`, event `workflow_dispatch`, attempt `1`, head `68c15e6b...` and job `101799329245`. An initial approval API request was explicitly rejected `422` before state change because the Environment ID was encoded as a string; the corrected integer JSON request then created exactly one approved deployment `6312010730` for the already authorized Environment.

The job ran from `2026-09-07T15:46:08Z` through `16:03:15Z` and completed `success`. Exact checkout/refusal, pnpm `11.9.0`, Node `24.14.0`, hash-pinned ORAS `1.3.3`, accepted Build Once tool boundary and exact dependency install all passed. The sole Build Once authority completed from `15:46:50Z` through `15:56:18Z`. Its immutable record binds source archive SHA-256 `9e68c8b274b4a1f13fe90ccce3cff611b3d1865fc743a93784d3377b006b7a75`, release/tree/epoch `68c15e6b...` / `ba209c58...` / `1788794283`, platform order `linux/amd64`, `linux/arm64`, and index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` in state `built` with no revocation record.

Both actual emitted-image Product gates passed under the accepted `--pull never`, no-network, read-only, UID/GID `10001:10001`, capability-drop and no-new-privileges envelope. The loaded `linux/amd64` child reported the complete 400-file/20-public-manifest bundle boundary verified at `15:54:38Z`; the loaded `linux/arm64` child independently reported the same complete boundary verified at `15:55:34Z`. Both temporary loaded tags were then removed. The release record binds:

| Platform | Manifest digest | Config digest | Scan result |
| --- | --- | --- | --- |
| `linux/amd64` | `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c` | `sha256:dda7ba8093547fc8e17d7f478ed48a451ee6f5ebacd8bba7b6cac4c24e3042e5` | 518-package evidence; pinned runtime packages, package-manager absence, Sharp `0.35.3` smoke and zero business/framework evidence leakage matches passed |
| `linux/arm64` | `sha256:e3a389bf29196337926654cdf1c06c3dc56cc13ee5e3b45f9ffeb8d3f368ae1c` | `sha256:0a484167fbffdb21b9107ff7592508c524d99f5dd7ae9e60786c769dfe6ccd76` | 518-package evidence; pinned runtime packages, package-manager absence, Sharp `0.35.3` smoke and zero business/framework evidence leakage matches passed |

Private publication ran once from `15:56:19Z` through `16:02:05Z` and returned `PASS` for `ghcr.io/czy282923753/cwt-cloudwave-textile@sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, with `mutableTagIsAuthority: false`. GitHub's private package inventory records version ID `1219717121` named by that exact index digest and tagged only `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`; tagless child versions `1219717035` and `1219716887` match the amd64 and arm64 manifests above.

The sole retained success artifact is `10025983850`, `cwt-release-evidence-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, 9,090,474 bytes with 30-day retention. Its downloaded ZIP SHA-256 `83f8d4a3e9255695b3252ada0c9839ab5f7279db337fffe43fe2be59fc6fe59b` exactly matches the upload log. It contains only `release.json`, sanitized build metadata and the six declared detached platform evidence files. Recomputed descriptor hashes all match the immutable release record:

| Platform | SBOM | Scan | Provenance |
| --- | --- | --- | --- |
| `linux/amd64` | `88a02bb51d49761b54f7cc3cde455d534085465b23edb7efad20df6eaa16df92` | `79c84a2dd395107be7ee6de86fe87089a60ec4cae3f097ac523047765505bf87` | `9c86cce05f41c98408f0f09f4c980d9fece1e8cc0885b188b8eca10dcfe4b41e` |
| `linux/arm64` | `eb5c38909e6b5826f565856f3ff093ea6158e79af31c1b9320751cee962cf5bb` | `26ec5cc2fb40aa90b69b71bcd3f8c51fae5fe5419a37d26f7a69753cab51b10d` | `0235156097de3c3eb80e20b59baad4a04883b7fa06dfefa47d7acbf8fbddca78` |

The existing `check-release` authority is byte-identical between the local report branch and exact release source. Its first local invocation correctly rejected the lexical `/tmp/...` artifact path as `path_invalid` because macOS resolves `/tmp` through `/private/tmp`; rerunning the same downloaded file at its canonical `/private/tmp/...` path returned `{"status":"PASS"}` for exact release `68c15e6b...` and index `sha256:c8d4d1c3...`. This was a local path-canonicalization check, not a Product/artifact failure or external retry.

The ephemeral Runner exited after one successful job, removed its credentials/configuration and automatically deregistered. Its complete owned root, work/auth/download/release scratch and the local artifact/log verification copies were removed after evidence capture. Final inventories retain zero repository Runners, zero queued/in-progress workflows, zero running and the same 11 stopped containers; shared Docker images and BuildKit/download caches remain preserved. Remote `main` remains exact `68c15e6b...`; local report history remains separate and unpushed. No rerun, second dispatch/build/registration, Tencent purchase, native Runtime, Compose/Migration/Web start, deployment, production/COS/DNS operation or permission-policy change occurred.

Disposition is **FORMAL BUILD ONCE AND PRIVATE PUBLICATION PASS**. This establishes the exact dual-platform Product/evidence/private-custody tuple above. It does not establish native Ubuntu/amd64 Runtime, deployment or Stage 6 acceptance. The next gate is coordinator evidence assessment and a separate Owner decision on the native Runtime operation; there is no automatic continuation.

## Decision-ready corrected-release native Runtime preparation

This section prepares one fresh Owner decision for the corrected Release and records no Runtime execution authority. The dispatch-ready tools/workflow identity is local merge `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2`, with exact parents report lineage `748c7b966e4583633b616d0ca9826893ceb15100` and reviewed Release source / current remote `main` `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`. The normal local merge completed without conflict. Independently captured ancestry checks from both parents returned exit `0`; the complete non-documentation tree equals `68c15e6b...` with exit `0`; the principal report at the merge equals `748c7b96...` with exit `0`; the independent Review and Technical Escalation documents equal `68c15e6b...` with exit `0`; and the selected Runtime workflow, provisioning, validator, Registry integration, Compose, Dockerfile and bundle-checker modules equal `68c15e6b...` with exit `0`. This preserves both accepted histories while making one distinct workflow commit a normal fast-forward successor of remote `main`. The report-only successor containing this section does not replace the dispatch identity and is not part of the proposed Push.

The exact dispatch and immutable Product tuple is:

| Boundary | Exact value |
| --- | --- |
| one proposed future Push / dispatch ref | `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2:refs/heads/main`; normal non-force fast-forward only |
| `workflow_commit` | `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2` |
| `release_commit` | `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641` |
| source tree | `ba209c586704b157e8901496cfc0ac8c69f52d93` |
| `index_digest` | `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` |
| `evidence_run_id` / job / attempt | `34139907082` / `101799329245` / `1`, completed `success` |
| detached evidence artifact | `10025983850`, `cwt-release-evidence-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, 9,090,474 bytes, expires `2026-10-07T16:02:05Z` |
| evidence ZIP SHA-256 | `83f8d4a3e9255695b3252ada0c9839ab5f7279db337fffe43fe2be59fc6fe59b` |
| private Registry subject | `ghcr.io/czy282923753/cwt-cloudwave-textile@sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` |
| GHCR version / convenience tag | `1219717121` / exact Release commit `68c15e6b...` |
| `runner_nonce` | one fresh execution-time 128-bit lowercase hexadecimal value; never preselect or reuse a historical nonce |

At exact reviewed source `68c15e6b...`, the execution contracts are:

| Contract | Git blob | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| `.github/workflows/cwt-runtime-validation.yml` | `02fcbb36b14ff2405f35351624399f704f060218` | 8,932 | `a7a0fb31dd1045044df5833a5596d5d933e58284c1154f56065e2f288ec99d23` |
| `linux-amd64-compatibility.v1.json` | `8d18157bd68eb2dcff8ca7c88a66c8309a2d08fe` | 454 | `5e33bd99e412d35a7e0cffb6a1c379f0decf7f8f816cb087dc162906977514f0` |
| `provision-ubuntu-amd64-runner.sh` | `428b7f6b4c3b9d2a2938b1a3a4b83d58dd2e490f` | 19,915 | `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0` |
| `tencent-tat-provisioning-invocation.v1.json` | `60e534b810c81c6147991ec23f53258f8488fb35` | 421 | `98d67f285e0fd5afb57a6a4b7227c57b6214cdb8992d44a552d4059ba81e41b1` |
| `register-and-start-ephemeral-runner.sh` | `1281a207e5ebe58b7fb78900213a44ef26ad7e0a` | 3,752 | `7c773a341cfd629dae2985f7c51792aee6f917ca5df7733f7200d26e780da94b` |
| `tencent-tat-runner-registration-invocation.v1.json` | `96cb85aa281138385652bd0ea816450107000493` | 9,502 | `0184e5108a4ddd81dbd8edc5060c4c4369e65edd1fbead2c9f2a733179016839` |

The provisioning payload is submitted unchanged as one zero-argument Bash payload. Its versioned invocation requires Tencent Automation Assistant `ExecuteCommand`, timeout `600` seconds, retained ordinary output ceiling 24,576 bytes, success budget 1,024 bytes and failure diagnostic tail 4,096 bytes. Completion authority is terminal `SUCCESS` plus process exit `0`; `CWT_PRE_REGISTRATION_OK` corroborates but never replaces those facts. No credential or registration parameter exists in this step.

The registration invocation is the unchanged versioned JSON contract: Tencent Automation Assistant `RunCommand`, `SHELL`, parameter substitution enabled, user `ubuntu`, working directory `/home/ubuntu`, timeout `600`, `saveCommand=false` and COS output disabled. Its exact Base64 `content` decodes to one 6,894-byte wrapper with SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`. That wrapper embeds and verifies the exact 3,752-byte registration payload above. Its four one-time parameters are hidden `{{tat-hidden:CWT_REGISTRATION_TOKEN}}`, plus non-secret `CWT_RUNNER_NONCE`, `CWT_RUNNER_NAME` and `CWT_GITHUB_REPOSITORY`. The canonical Runner name is `cwt-tencent-sg-<nonce>` and its cumulative labels are `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-<nonce>`.

Read-only GitHub and custody readiness was refreshed at `2026-09-08 00:28:01 +0800`. Runtime Environment `cwt-stage6-runtime-validation`, ID `21083845147`, retains required-reviewer rule `64408756` for Owner `czy282923753`, permits self-review, and retains custom-branch rule `64408757` with the sole branch policy `main`, ID `58917416`. Repository inventory showed zero registered Runners and zero queued, waiting, pending, requested or in-progress workflow runs. The original Build Once run remains attempt `1`, `workflow_dispatch`, exact head `68c15e6b...` and completed `success`. Artifact `10025983850` remains unexpired and its run/head join remains exact. Package `cwt-cloudwave-textile` remains private; version `1219717121` remains named by the exact index digest and carries only the exact Release-commit tag. These are volatile preflight facts and must be refreshed before any authorized execution.

Authenticated Tencent Cloud purchase-form preflight completed read-only on `2026-09-08 +0800`; the page was left before confirmation and no order or resource mutation was submitted. Singapore Zone 2 offered exact `SA5.MEDIUM4` (`标准型SA5`, 2 vCPU / 4 GiB, AMD EPYC Bergamo) as `库存充足`, displaying CNY `0.35/hour` after the shown 7.8 discount and CNY `0.45/hour` as its crossed reference. Public `Ubuntu Server 24.04 LTS 64位`, image `img-mmytdhbn`, and one 60 GiB `通用型SSD云硬盘` system disk produced an exact current base configuration price of CNY `0.41/hour`, with CNY `0.10/hour` displayed saving. The selected network was existing non-Production `vpc-piootcsf | Default-VPC（默认） | 172.22.0.0/16` and Zone-2 `subnet-6uvqq9wg | Default-Subnet（默认） | 172.22.0.0/20`, with 4,093 free addresses. One independent public IPv4, ordinary `BGP`, `按流量计费`, and a 5 Mbps bandwidth cap displayed CNY `0.79/GB`, with CNY `0.01/GB` saving and hourly settlement. The current public price table lists CNY `0.80/GB` for Singapore before the form's displayed saving; the transaction form remains the price authority immediately before purchase.

Tencent's official network contract defines traffic billing as hourly-settled **public outbound** traffic. Traffic entering the CVM, including the CVM downloading public dependencies, is not charged as outbound traffic; traffic sent by the CVM to GitHub/GHCR or other public endpoints is chargeable. For a purchased cap at or below 10 Mbps, Tencent currently assigns 10 Mbps inbound capacity, while the chosen 5 Mbps setting remains the outbound cap. The authoritative references are [公网计费模式](https://cloud.tencent.com/document/product/213/10578), [网络计费问题](https://cloud.tencent.com/document/product/213/17282), [公网带宽上限](https://cloud.tencent.com/document/product/213/12523) and [公网网络费用](https://cloud.tencent.com/document/product/213/113026).

The recommended whole future authorization is one operation with a **CNY 10 maximum estimated exposure**, subject to immediate price/stock refresh and stop-before-purchase if the quoted shape, image, disk, BGP mode or price changes materially. One disposable host is the absolute ceiling: Singapore Zone 2, exact `SA5.MEDIUM4`, public Ubuntu 24.04 native `amd64`, one 60 GiB general-purpose SSD, one ordinary BGP public IPv4 at 5 Mbps traffic billing, TAT management, and one attempt-specific temporary security group with zero inbound rules and exactly one IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. Do not use or alter Production Lighthouse `lhins-3c2vknjb`, `cwt-production-sg`, the existing VPC/subnet, DNS, COS or any other shared resource. The current 90-minute base exposure is CNY `0.615`; even a conservative 5 GB outbound allowance makes CNY `4.565`, leaving CNY `5.435` contingency under the Owner budget. The budget is an operator stop boundary rather than a Tencent-enforced cap.

The authorized operation would include exactly one normal non-force Push of `91334b31...` to `main`; creation of the one temporary security group/outbound rule and one disposable CVM; at most two provisioning TAT invocations and at most two registration TAT invocations only under the single recovery rule below; acquisition of a fresh repository-scoped registration token for each admitted registration; one ephemeral Runner identity; exactly one Runtime workflow dispatch with the tuple above; required approval of `cwt-stage6-runtime-validation` for attempt `1` and, only if GitHub requests it for an admitted same-run recovery, attempt `2`; no more than two workflow attempts of the same run; at most one actual Product validator start; and mandatory outer cleanup. The existing workflow job timeout remains 60 minutes. Teardown must start by host age 80 minutes and finish by host age 90 minutes. No replacement host, second dispatch, rebuilt Product, alternate Release, Registry write, Product repair, protected activation, deployment or phase advancement is included.

There is at most one shared same-host preparation recovery for the host lifetime. It is admissible only after a terminal preparation failure before private-GHCR authentication and before the Product validator starts; immutable evidence must show neither boundary started and no validator outcome exists; the repository Runner record, local Listener and Worker must be absent; the prior job token must be expired; exact trusted Ubuntu/Docker/Compose identities must still match; there must be no containers, CWT networks, `/etc/cwt`, `/srv/cwt`, `/run/lock/cwt`, GHCR authentication, materialized OCI, Runtime evidence or untrusted mutation; cleanup must have succeeded; the CNY 10 envelope must remain sufficient; and at least 45 minutes must remain before teardown starts. A recovered registration uses a fresh token while retaining the original nonce, Runner name, labels and GitHub run. If attempt `2` is needed, rerun only the failed job of that same run. Any ineligible state, second preparation failure, GHCR authentication, OCI materialization, validator start, Product result, insufficient time/budget or cleanup failure closes recovery and proceeds directly to teardown. The Product validator may start once at most across both attempts.

For the future registration token, use the authenticated GitHub UI to acquire the short-lived value and transfer it directly through clipboard/user paste into Tencent TAT's hidden `CWT_REGISTRATION_TOKEN` parameter field. Clear the system clipboard immediately after the hidden field has accepted the value and before continuing. Do not place the token in a file, shell argument, command body, pipe, loopback service, retained browser field, output capture, report or chat. The token must never be read back or echoed; after registration it is allowed to expire and a recovery, if admitted, acquires a new token through the same direct hidden-field path.

Mandatory cleanup applies after success, failure, timeout or abandoned setup: wait for the ephemeral Runner process to exit and deregister; remove only its exact residual GitHub Runner record if automatic deregistration failed; destroy the one attempt CVM and its system disk with no retained disk; verify release of its public IPv4; prove the temporary security group has zero associations and delete it; remove only task-owned TAT commands/invocations if the accepted operating procedure requires it; clear any remaining clipboard value; and independently prove absence of the exact instance, disk, public IP, temporary security group and repository Runner. Preserve the immutable private GHCR Release, original Build Once artifact, shared VPC/subnet and unrelated historical resources.

The Owner's standing instruction now expressly authorizes purchase and destruction/cleanup of task-owned disposable resources without another confirmation when an otherwise authorized task remains within its agreed scope, budget and resource count. For this Runtime proposal, that standing authority covers the purchase-form final confirmation for the one disposable host within the CNY 10 boundary, its required temporary security group/disk/public IP, mandatory deletion after success or failure, and bounded reconciliation after a failed cleanup attempt. It does not expand authority to Production Lighthouse, shared or unrelated resources, a second host, budget overrun, repeated Product validation or another workflow dispatch. It also does not independently start this preparation-only assignment. A later coordinator-approved execution should not ask the Owner again merely to confirm the in-budget purchase or required owned teardown; authentication/MFA or a hard platform/tool restriction remains a distinct factual blocker.

No part of that future operation occurred during this preparation. There was no Push, Tencent order, instance/disk/IP/security-group creation, TAT command or invocation, registration-token acquisition or transmission, Runner registration, Runtime workflow dispatch, Environment approval, GHCR authentication, OCI materialization, validator, deployment or cleanup mutation. The complete concrete scope is **READY FOR COORDINATOR ASSESSMENT**. Any execution instruction should name exact tools commit `91334b31...`, the frozen Release/index/evidence tuple, CNY 10 maximum estimated exposure and the single same-host recovery rule; the standing purchase/cleanup authority above then applies without a repeat Owner prompt.

## Corrected-release native Runtime execution outcome

The coordinator subsequently authorized exactly one recovery-aware native Runtime operation for workflow/tools commit `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2`, immutable Product release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, Build Once evidence run `34139907082` / artifact `10025983850`, nonce `69decf905b1fed44f720d4a5f5fba2e9`, and Runner name `cwt-tencent-sg-69decf905b1fed44f720d4a5f5fba2e9`. One normal non-force Push advanced remote `main` from exact Release commit `68c15e6b...` to exact tools commit `91334b31...`; the preparation report commit `4bf41d8c52c225d77e21cffba202028f1463b7ce` remained local and unpushed.

Immediate authenticated Tencent purchase refresh retained the approved shape and quote: Singapore Zone 2, `SA5.MEDIUM4` (2 vCPU / 4 GiB), public Ubuntu 24.04 image `img-mmytdhbn`, one 60 GiB general-purpose SSD system disk, ordinary BGP public IPv4 at 5 Mbps traffic billing, CNY `0.41/hour` base and CNY `0.79/GB` public outbound traffic. The Operator created temporary Security Group `sg-84od3k9m` / `自定义模板-20260908003959329` with zero inbound rules and exactly one IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule carrying attempt remark `cwt-runtime-69decf905b1fed44f720d4a5f5fba2e9`. One disposable CVM, `ins-003pumdi`, used the approved existing VPC/subnet `vpc-piootcsf` / `subnet-6uvqq9wg`, system disk `disk-c927v3r8`, ordinary public IPv4 `43.134.94.126` and private IPv4 `172.22.0.14`. The instance was created at `2026-09-08 00:40:23 +0800`; Tencent security and monitoring agents were disabled and TAT remained enabled.

Exactly one provisioning TAT invocation, `inv-28cv5h00c1` / task `invt-28cv5h00c2`, ran as `root` from `/root`, with parameters and COS output disabled and timeout `600`. The submitted 19,915-byte reviewed payload matched SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0`. It ran from `00:44:18` through `00:45:39 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_PRE_REGISTRATION_OK os=ubuntu-24.04 arch=amd64 docker=29.6.2 compose=5.3.1 runner=2.337.0 preparation=fresh runner_tree_owner=ubuntu diag_write_probe=PASS docker_user_probe=PASS`. No preparation recovery was needed.

Only after that PASS, one repository-scoped short-lived registration token was selected from the authenticated GitHub UI and copied directly into Tencent TAT's hidden `CWT_REGISTRATION_TOKEN` parameter. It entered no file, shell, pipe, loopback helper, command text, retained output, report or chat. The clipboard was overwritten with a non-secret sentinel immediately after paste and before command submission. Exactly one registration invocation, `inv-x8cvcxgxum` / task `invt-x8cvcxgxun`, ran as `ubuntu` from `/home/ubuntu`, with parameter substitution enabled, COS output disabled and timeout `600`. The unchanged 6,894-byte decoded wrapper matched SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`. It ran from `00:52:15` through `00:52:21 +0800`, ended `命令成功`, exit code `0`, and emitted `CWT_REGISTRATION_MATERIALIZATION_PASS`. Repository Runner ID `34` appeared online and idle with the exact name, OS `Linux` and cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, `cwt-job-69decf905b1fed44f720d4a5f5fba2e9`.

Exactly one [Runtime workflow run `34145251214`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34145251214) was dispatched on `main`, with event `workflow_dispatch`, attempt `1`, exact head `91334b31...` and job `101815782493`. The already authorized Environment deployment `6313012980` for `cwt-stage6-runtime-validation` was approved once. The job ran from `2026-09-07T16:53:21Z` through `16:56:11Z` and completed `failure`. Exact tools and Release checkouts, Node installation, unique Tencent Runner binding, detached Build Once evidence download, release-header reconciliation, hash-pinned ORAS installation, private GHCR authentication and same-digest read-only OCI materialization all passed.

The sole accepted Linux Runtime validator then executed exactly once and returned `{"status":"NOT_PASS","reasonCode":"compose_infrastructure_up_failed","evidence":"cwt-runtime-outcome"}` with process exit code `1`. The always-run retention step succeeded and created artifact `10027475980`, `cwt-runtime-outcome-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, 2,339 bytes. Its downloaded `linux-runtime-validation.json` has SHA-256 `2066efda2ec51ff65aacfcec1d5e582e5d77fa65c69d1e8dfee101adad93cdd5` and records schema `1`, tools/profile identities `91334b31...` / `5e33bd99e412d35a7e0cffb6a1c379f0decf7f8f816cb087dc162906977514f0`, exact Release/index/selected-amd64 child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, `status=NOT_PASS`, `reasonCode=compose_infrastructure_up_failed`, `failureDetailCode=null` and `runtime=null`. It also records successful cleanup of Compose consumers, Compose networks, pulled references and host paths, no credential values, synthetic data only and no automatic retry.

This is a formal Product Runtime NOT PASS at the Compose infrastructure-start boundary. The sanitized result does not identify which service or dependency stopped startup, and no unsupported inference is recorded. Because private GHCR authentication, OCI materialization and the Product validator had already started, the same-host recovery admission was closed. No recovery, rerun, second workflow attempt, second dispatch, second registration, new token, replacement host, Product rebuild, Registry write or in-place repair occurred.

The ephemeral Runner exited and automatically deregistered; the exact-runner query and final repository Runner inventory returned zero. Mandatory outer cleanup selected immediate CVM destruction and immediate resource release. Tencent required a fresh account identity check in the deletion flow, so the final absence proof was delayed beyond the planned host-age ceiling. At `2026-09-08 02:33 +0800`, the Singapore inventories authoritatively displayed zero CVMs (`还未选购云服务器`), zero public IPs, zero cloud disks and zero Security Groups. Before deletion, Security Group `sg-84od3k9m` displayed zero associated instances; after confirmation its inventory displayed `0`. Thus `ins-003pumdi`, `disk-c927v3r8`, `43.134.94.126`, `sg-84od3k9m` and Runner ID `34` leave zero observed residue. Shared VPC/subnet, private GHCR Release, Build Once artifact and unrelated resources were preserved. The downloaded sanitized outcome verification copy was removed locally.

The final cleanup proof occurred approximately 112 minutes after instance creation, so the accepted 80-minute teardown-start / 90-minute final-lifetime operating ceiling cannot be claimed as met. The console did not expose a stable destruction-completion timestamp in the captured evidence. The known base-rate exposure through final verification is approximately CNY `0.77`, plus unsettled public outbound traffic at the quoted CNY `0.79/GB`; exact provider-settled cost is not yet available and is not inferred. This timing nonconformance did not widen resource count, validation count, Registry authority or Product scope, and final residue is zero.

Final disposition is **NOT PASS / HOLD**. The next gate is bounded root-cause analysis of `compose_infrastructure_up_failed` using the exact retained outcome, immutable Release/evidence and accepted source, followed by normal implementation and independent Review if a causal correction is required. Any future Build Once, Release/index identity, native Runtime attempt, cloud purchase, workflow dispatch, deployment or Stage acceptance requires separate authority. This record grants none of those actions.

## Bounded pre-teardown infrastructure failure-state Candidate

Accepted local analysis could not reproduce the native failure: one exact combined PostgreSQL Bookworm plus digest-qualified Valkey `linux/amd64` cold Compose rehearsal passed, while run `34145251214` retained only broad `compose_infrastructure_up_failed` and the original host state and logs were correctly destroyed. Code Candidate `c9bc37dbec143df3f8335b42a6d29fbec5ccf287` therefore does not speculate about journald, resource pressure, a particular dependency or a Product correction. It closes only the existing evidence gap at the responsibility boundary where infrastructure `up` has already returned nonzero but cleanup has not started.

On that one boundary, the validator now executes exactly one bounded, read-only `docker compose ps --all --format json` against the same unique project, project directory, Compose file, staging profile, Docker Unix socket and sanitized Compose environment. It uses the existing command runner with a 10-second timeout and 16 KiB output ceiling. A nonzero collector status, spawn error, signal, timeout, oversized output, malformed JSON or ambiguous duplicate fixed service returns `null`; collector failure never replaces the original `compose_infrastructure_up_failed`, skips teardown, adds a retry or changes `NOT_PASS` to `PASS`.

The existing schema-version-1 outcome gains `infrastructureFailureServices` only for `compose_infrastructure_up_failed`. Its value is either `null` or an exact object keyed only by `postgres` and `valkey-staging`. Each fixed entry contains only `present`, allowlisted `state`, allowlisted `health`, and a finite safe-integer `exitCode` or `null`. Valid inventory absence records `present=false` with the other values `null`; unavailable or unparseable inventory leaves the whole field `null`. Container IDs/names, commands, image names, environment, mounts, ports, labels, health output, Docker `State.Error`, raw stdout/stderr/logs and arbitrary dynamic strings are discarded. Other failures and PASS outcomes do not contain the field. The existing checksum, ownership handoff and always-run two-file artifact upload remain unchanged.

Local Docker Compose `5.3.1` was verified directly with two unique, no-published-port, cached-Node synthetic services. Its actual `ps --all --format json` output was NDJSON with numeric `ExitCode`, lowercase `State` and empty/starting `Health`; the parser accepts that real shape and the JSON-array variant without retaining unrelated fields. The same focused test proves an exited PostgreSQL-shaped service with exit `7` and a running/starting Valkey-shaped service are reduced to the fixed schema, while hostile extra fields, unknown state/health, unsafe exit codes, malformed data, duplicate fixed services and over-limit output fail safely.

Final focused verification passed: Node syntax checks for both changed files; targeted ESLint for both changed files; `node --test deploy/scripts/preflight-linux-runtime.test.mjs` **19 passed, 1 unchanged environment-dependent Scheduler/PostgreSQL test skipped, 0 failed**; and `git diff --check`. The first targeted run exposed one test-only sampling race in which the intentionally exiting synthetic container was still momentarily `running`; the test was corrected to wait for that exact container to exit before sampling, with no production-code change, and the final run passed. Synthetic containers, network and temporary Compose file were removed after each run.

Complexity remains local and bounded: no Product/configuration/privilege/timeout change, persistent state, schema-version increase, framework, taxonomy, retry, prerequisite probe, second validator or alternate evidence writer was added. The existing `up` failure branch, command plan, process runner, cleanup path, outcome writer/checksum and workflow uploader are reused. The only new runtime branch collects one fixed snapshot before the unchanged failure and teardown path.

This Candidate improves the next failure's evidence but does not diagnose the destroyed prior host or prove native Runtime success. No Product build/rebuild, GHCR authentication/write, CWT Compose startup, Push, cloud/TAT/Runner/workflow action, deployment or Stage acceptance occurred. The next gate is one independent Review of exact code Candidate `c9bc37db...` plus this report-only closure; any later Runtime remains a separate coordinator and Owner decision with no automatic execution.

## Native Runtime infrastructure startup failure — bounded local diagnosis

This section is a report-only diagnosis of a new Runtime infrastructure failure. It does not reopen the closed bundle/image escalation, whose accepted correction and formal Build Once remain PASS. The exact Runtime tools/workflow tree is merge commit `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2`; the immutable Product remains release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, private index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` and selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`. Native workflow run `34145251214`, job `101815782493`, attempt `1` returned `compose_infrastructure_up_failed`. Preserved artifact `10027475980` has a verified sanitized outcome and checksum: release and bundle authorities passed, Runtime remained `null`, `failureDetailCode` remained `null`, all inner cleanup booleans were true and artifact upload succeeded. Migration, Web and application probes did not run. Outer VM deletion is a separate already-controlled lifecycle and is not part of this diagnosis.

### What the current evidence establishes

The accepted validator normalizes and validates the complete Compose graph, pulls the exact Product/PostgreSQL/Valkey references, verifies Product identity and bundle, and then invokes one combined command: `docker compose ... up --detach --wait --wait-timeout 180 --no-deps --pull never --no-build postgres valkey-staging`. Its generic process wrapper captures child stdout/stderr in memory but, for any nonzero status, replaces them with only `<label>_failed`. The catch path then runs Compose teardown and residue checks before writing the retained outcome. Consequently the retained evidence proves the failure boundary but cannot identify whether PostgreSQL, Valkey, their healthcheck, the shared `journald` logging prerequisite or Compose orchestration caused it. No remaining container or network can now answer that question. The actual root cause is therefore **undetermined**, not a diagnosed Product defect.

Static inspection found no focused test that starts the two exact Compose infrastructure services together. The Compose graph tests validate normalized declarations. The Scheduler/Migration test starts a separately constructed trust-authenticated PostgreSQL container, manually creates its role/database and does not exercise the accepted PostgreSQL entrypoint, initialization script, Valkey, `journald` or the combined `--wait` boundary. The synthetic-host test prepares fixed paths inside a helper container without exercising Compose. This leaves the exact failure seam operationally uncovered even though its individual declarations are strongly checked.

### Bounded local evidence and its ceiling

The local Docker Desktop engine reports Linux/aarch64 Docker `29.6.2` with Compose `5.3.1`; it is not the accepted native Ubuntu 24.04/amd64 host. A disposable `journald` probe failed at container creation with `journald is not enabled on this host`. Docker Desktop nevertheless lists `journald` among known log drivers, so driver inventory is not a readiness proof. This is only a known local platform difference and does not rank `journald` as the native lead cause.

Exact source identity was preserved: `compose.yaml`, `deploy/postgres/init-environments.sh` and `deploy/valkey/entrypoint.sh` have identical Git blobs at Product source `68c15e6b...` and tools merge `91334b31...`, respectively `7e98b127863694259b701fe06f7be701cf9c30d5`, `e8c6d7b106b4bfa07d9f66787871110134eb6305` and `97087671f7f4816e31815a8f42fd2f071ef14960`. The public exact PostgreSQL reference `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382` was pulled for `linux/amd64`; its selected manifest was `sha256:7e6103cf85f88f7a0eddb3ec0b1ba8940eba098ed118ade25a729ca9daee5568`. The exact Valkey index `valkey/valkey:8.1.9@sha256:f0ba225266310efba5fb33383e21c64fbd07907304224786c780606e7ebd7327` already had its `linux/amd64` variant cached. No Trixie or native-arm image substituted for either exact probe.

The probe used one unique Compose project and a temporary override derived from the accepted Compose file. Every override was bounded and recorded: explicit `platform: linux/amd64` for the two selected services; `json-file` in place of the locally unavailable `journald`; and task-owned PostgreSQL data and four Synthetic secret-file sources in place of `/srv/cwt` and `/etc/cwt`. No image, command, entrypoint, user, read-only setting, capability, security option, limit, tmpfs, healthcheck, service network, init-script mount or secret destination changed. Normalized configuration confirmed neither service had ports or dependencies.

First, exact PostgreSQL alone completed a cold Compose `up --detach --wait --wait-timeout 180 --no-deps --pull never --no-build`. It was healthy with zero restarts, zero exit status, no OOM, no port binding, `999:999`, read-only root, all capabilities dropped, `no-new-privileges`, the accepted memory/CPU/tmpfs/command/mount/network set and container-visible `x86_64`. `PGDATA` was `999:999` mode `0700`; the accepted init script created both `cwt_production` and `cwt_staging` roles and databases.

After removing that task-owned initialized data, a second cold invocation used the validator's exact combined orchestration and service order: `... up --detach --wait --wait-timeout 180 --no-deps --pull never --no-build postgres valkey-staging`. The command exited `0`; Compose reported both exact services created, started and healthy. The resulting project service set was exactly `postgres,valkey-staging`. Both containers were `x86_64`, healthy with zero restarts, zero exit status and no OOM or published port; all accepted service controls remained present. PostgreSQL again initialized both roles/databases, and the accepted Valkey ACL user returned authenticated `PONG`. Sanitized log filtering retained only initialization/readiness markers and exposed no secret value. This clears a reproducible common dependency-version, entrypoint, init-script, ACL, resource-limit, healthcheck and combined `--wait` configuration failure under the stated local ceiling.

A later explicit scope extension authorized read-only local materialization of the existing private Product index and one complete local operational chain. The exact `linux/amd64` Product was initially absent. Existing GitHub authentication identified active account `czy282923753` with `read:packages`; no token value was printed or placed on a command line. The first task-private Docker login had returned a transient Registry `EOF`. Under the clarified finite transport boundary, the next login succeeded through `password-stdin`. The first pull invocation used the restricted config without Docker Desktop's context metadata and therefore failed locally against absent `/var/run/docker.sock` before contacting Registry; binding the same task-private config to the already-active Desktop socket corrected only that invocation wiring and changed no daemon or global context. One exact pull then completed without resume. Inspection bound index `sha256:c8d4d1c3...` to selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, user `10001:10001` and revision `68c15e6b...`. The credential config was mode `0600`, logged out and removed before any container ran.

The single operational chain used a fresh unique project and complete task-owned Synthetic Staging host material. Its 43-key `runtime.env` compared exactly with production `runtimeEnvironment("staging", releaseId)` semantics; all required protected secret files, matching database credentials, three media roots, two backup roots, maintenance lock and PostgreSQL data path were present at the accepted container destinations. Secret files were mode `0444`, `runtime.env` was mode `0400`, and container-visible storage ownership/modes matched their accepted service users. The four selected services retained their exact commands, users, read-only roots, capability/security restrictions, limits, tmpfs, healthchecks, secrets and networks. The only environment-specific changes were explicit emulated `linux/amd64`, task-owned host sources and the known Desktop `json-file` exception for `journald`; no port or dependency was added.

Fresh PostgreSQL and Valkey again passed the exact combined infrastructure command. The selected one-off `scheduler-staging` then ran exactly `/app/deploy/backup/pre-deploy node --import=tsx /app/scripts/migrate.ts` with the existing read-only Drizzle mount. It exited `0` with `Database migrations applied`; the database recorded 23 migration rows, one sealed pre-deploy backup set contained `complete.json`, no partial set remained, and the `--rm` Scheduler container was absent afterward. PostgreSQL emitted only non-fatal SQLSTATE `42622` notices for truncating long generated identifiers; there was no Migration error or retry.

`web-staging` subsequently passed its exact `--detach --wait --wait-timeout 180 --no-deps --pull never --no-build` start. The production `probeApplication` source was extracted from the existing validator and executed unchanged inside Web: live, readiness and root each returned HTTP `200`; all three carried `x-robots-tag: noindex, nofollow, noarchive`, and root contained meta noindex. Final Compose inventory contained exactly `postgres`, `valkey-staging` and `web-staging`; all were healthy with zero restart, exit `0`, no OOM and zero published ports. Web retained the exact index/child labels, `10001:10001`, read-only root, dropped capabilities, `no-new-privileges`, exact four networks and three writable `10001:10001` mode-`0700` storage mounts. No persistent Scheduler, AI or Production service and no external email, AI or scanner action was invoked.

All task containers, networks, data, temporary configuration and private Docker auth material were removed after consumers stopped. The newly acquired Product and PostgreSQL references were removed; the pre-existing Valkey variants, shared layers/caches, other images and all 11 historical stopped containers were preserved. Final state had zero running containers and zero task project containers/networks. Prior outer Runtime cleanup is separately confirmed complete by Operator report commit `5b9abaaf3ca384e98fedb68a0aa69f85b205d1c9`; no cloud evidence was accessed or recreated. No Product build/publication, Registry write, workflow, cloud, remote mutation, deployment, protected restore, full formal validator or release action occurred.

### Remaining hypotheses and minimum safe native evidence

The exact full local chain PASS removes ordinary PostgreSQL/Valkey/Product version, accepted Compose service configuration, pre-deploy backup/Migration, Web startup and application probe composition from the leading hypotheses. The surviving hypotheses are native-only and cannot be ordered with present evidence: native `journald` availability or interaction; native Linux bind-mount ownership/filesystem behavior; a transient Docker daemon or Compose `--wait` condition in the failed attempt; or another host-specific create/start/health condition. Desktop's `journald` limitation is not evidence for any one of them. Because prior teardown and outer cleanup are complete, the failed host contains no recoverable discriminator. The SQL identifier notices are a bounded schema-maintenance observation, not a failed Runtime gate or an identified cause here.

A separate Developer owns any diagnostic implementation. The smallest fallback remains reuse of non-sensitive service state at the existing `infrastructureUp` failure boundary: before unchanged teardown, parse one bounded `docker compose ps --all --format json` snapshot and retain only the two fixed service names, whether each is present, its reported state, health and exit code. Do not add a separate prerequisite-probe container or classification framework, and do not retain raw Compose stdout/stderr, logs, health output, `State.Error`, environment, mounts, labels, container IDs, secret paths/values or arbitrary daemon text. If the snapshot itself fails, preserve the existing broad failure without inventing detail.

Only an execution on the accepted native host can supply the missing host-specific evidence; the minimum useful change is the one pre-teardown service-state snapshot at the existing command boundary, not another Product build or expanded Runtime path. This analyst section implements no diagnostic code and authorizes no automatic native execution, Push, workflow, cloud or phase action. The next gate is coordinator integration of this local full-chain PASS with the separately owned diagnostic deliverable.

## Integrated diagnostic tools preparation

Local integration Candidate `cf266ae099995ddaf7a2e9572774cd49bc3c1f19` retains accepted diagnostic Review `d4fb6119b4fdc7166e60f39da23b15286e00dc99` and the complete local-chain Analyst delivery `59788d6d04b1f1eccf2cd92b619ace6ff617165b` as separate Git ancestors; both ancestry checks returned exit `0`. The Review branch fast-forwarded from report delivery `d0d71566160e1ac35c2780b1f8d023727dc34995`. The Analyst branch was then merged normally, with its sole principal-report conflict resolved by retaining both the accepted bounded diagnostic Candidate record and the Analyst's native-failure/local-full-chain evidence. Review Low finding `S6-ID-L-01` is accepted as a bounded documentation observation and creates no repair loop.

The Candidate's complete non-documentation tree equals Review `d4fb6119...` with `git diff --quiet` exit `0`. Relative to prior tools authority and current fetched `origin/main` `91334b31bc83fb99c486a07c77d9ee6fdbb3a5b2`, the only non-documentation changes are `deploy/scripts/preflight-linux-runtime.mjs` and its existing `deploy/scripts/preflight-linux-runtime.test.mjs`; no Product, workflow, configuration, privilege, timeout or recovery-policy file changed. `origin/main` is an ancestor of the Candidate with exit `0`, so it remains eligible for normal non-force fast-forward integration. The independently reviewed focused result is reused without a redundant build or full suite: syntax and targeted ESLint passed, and the focused Node test recorded 19 pass, one unchanged environment-dependent skip and zero failures. This report-only preparation did not run the Product, formal validator or cloud Runtime.

The immutable Product tuple is unchanged: source/release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, Build Once run `34139907082` and retained artifact `10025983850`. Read-only GitHub refresh confirmed the run remains completed/success at exact head `68c15e6b...`; the artifact remains available and unexpired through `2026-10-07T16:02:05Z`. Repository Actions remains enabled with allowed actions `all` and SHA pinning required. No token was retrieved or registered.

Authenticated Tencent read-only refresh found the main-account session usable and the Singapore CVM inventory at zero. The current purchase form retained Singapore Zone 2 exact `SA5.MEDIUM4` at 2 vCPU / 4 GiB and `库存充足`, compute CNY `0.35/hour`, selectable public Ubuntu Server 24.04 LTS 64-bit image `img-mmytdhbn`, one 60 GiB general-purpose SSD, and base configuration CNY `0.41/hour`. The existing `vpc-piootcsf` / `subnet-6uvqq9wg` remained selectable with 4,093 available IPs. One ordinary BGP public IPv4 at 5 Mbps remained selectable under traffic billing at CNY `0.79/GB`, and Automation Assistant remained an enabled free component. The purchase flow was not advanced to confirmation and no Tencent object or setting was created or changed.

The prior run's quasi-real-time detailed bill now exposes CNY `0.46394637` for exact instance `ins-003pumdi` and CNY `0.10696560` for exact system disk `disk-c927v3r8`, a confirmed compute-plus-disk subtotal of CNY `0.57091197`. Querying public address `43.134.94.126` returned no billing row and CNY `0.00000000`, but the console explicitly warns that detailed billing may lag; no independently attributable public-traffic record was available. Exact provider-settled all-in cost therefore remains unknown, and no budget-compliance claim is inferred from the subtotal.

The earlier cleanup nonconformance remains explicit: instance creation to final cross-inventory absence proof was approximately 112 minutes, exceeding the accepted 80-minute teardown-start / 90-minute finish boundary; the console did not expose a stable deletion-completion timestamp, and provider identity verification delayed final cleanup proof. Current authenticated access reaches the zero-instance inventory, purchase configuration and billing views without MFA. That read-only success does not prove a future destructive confirmation will be MFA-free; the provider may again require an interactive identity check. The existing one-host, CNY 10 maximum estimated exposure, 60-minute job, teardown-by-80, finish-by-90 and bounded same-host preparation-recovery rules remain unchanged, with no automation, persistent guard, provider-setting change or new prerequisite gate added.

Production Lighthouse `lhins-3c2vknjb` / `cwt-production-sg`, shared VPC/subnet and every unrelated resource remain outside scope and untouched. No Push, purchase, Security Group/TAT/Runner action, workflow dispatch, Registry write, Product rebuild, validator, deployment or cleanup mutation occurred. This integrated tuple is ready for coordinator verification. The next gate is the coordinator's decision on bounded native execution under the existing standing in-budget purchase and owned-resource cleanup authority; this preparation performs no automatic execution.

## Integrated diagnostic native Runtime execution outcome

The coordinator and Owner subsequently authorized one bounded native execution on integrated diagnostic workflow/tools commit `cf266ae099995ddaf7a2e9572774cd49bc3c1f19`, immutable Product release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, private index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, Build Once run `34139907082` / artifact `10025983850`, and nonce `3fa227131a83773fedb61ee2b1268d05`. Remote `main` resolved exactly to `cf266ae...` before dispatch. The report-only commit remained local and no report commit was pushed.

One attempt-specific Security Group, `sg-10pl3n2s` / `cwt-runtime-3fa227131a83773fedb61ee2b1268d05`, was created at `2026-09-08 03:44:53 +0800` with zero inbound rules and exactly one IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule recorded at `03:45:38`. One disposable Singapore Zone 2 CVM, `ins-6gu4t4fi` / `cwt-tencent-sg-3fa227131a83773fedb61ee2b1268d05`, used exact `SA5.MEDIUM4` (2 vCPU / 4 GiB), public Ubuntu 24.04 image `img-mmytdhbn`, 60 GiB general-purpose SSD `disk-9mthp5k8`, ordinary public IPv4 `43.163.119.55`, private IPv4 `172.22.0.9`, and the accepted existing VPC/subnet. The disk was created at `03:56:27` and the instance at `03:56:42 +0800`. The displayed quote remained CNY `0.41/hour` plus CNY `0.79/GB` public traffic at 5 Mbps. Security and monitoring add-ons were disabled and the purchase form selected the free Automation Assistant component.

The new instance nevertheless remained reported as not having a TAT client. The same host was retained inside the existing one-host lifecycle while completing initial client readiness before any provisioning invocation. This was not an admitted preparation-recovery event: no terminal provisioning failure had occurred, and no 45-minute reserve admission was checked. The Owner used Tencent VNC with the existing generated Ubuntu credential; no credential value was recorded and no password reset, public inbound rule, shared-network change or replacement host occurred. A first download command was visibly truncated by the VNC remote-command helper and failed against the incomplete host `mirrors.tenc`; it installed nothing. The official Tencent installer was then downloaded byte-completely from `https://mirrors.tencentyun.com/install/tat_agent/tat_agent_installer.sh` and executed on the VM. TAT Agent `1.2.2` started at approximately `04:44:44 +0800`; its process was present, WebSocket connection was established, `CheckUpdate` returned `NeedUpdate=false`, and `DescribeTasks` succeeded. A later authenticated API query reported the exact instance Agent `Online` with a fresh heartbeat. This initial-readiness completion changed only the missing official management client on the same disposable host.

Exactly one provisioning TAT invocation, command `cmd-d3xi61oh`, invocation `inv-s8d4dw00kn`, task `invt-s8d4dw00kp`, ran as `root` from `/root`, with parameter substitution disabled, `saveCommand=false`, COS output disabled and timeout `600`. The exact 19,915-byte payload matched SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0`. It ran from `2026-09-08 04:52:12` through `04:53:23 +0800`, ended `SUCCESS`, exit code `0`, and emitted `CWT_PRE_REGISTRATION_OK os=ubuntu-24.04 arch=amd64 docker=29.6.2 compose=5.3.1 runner=2.337.0 preparation=fresh runner_tree_owner=ubuntu diag_write_probe=PASS docker_user_probe=PASS`.

Only after that PASS, one repository-scoped short-lived GitHub Runner registration token was generated. Exactly one registration TAT invocation, command `cmd-5bx7s8ud`, invocation `inv-28d4fu00ua`, task `invt-28d4fu00ub`, ran as `ubuntu` from `/home/ubuntu`, with the unchanged invocation file SHA-256 `0184e5108a4ddd81dbd8edc5060c4c4369e65edd1fbead2c9f2a733179016839`, parameter substitution enabled, `saveCommand=false`, COS output disabled and timeout `600`. It ran from `04:54:17` through `04:54:23 +0800`, ended `SUCCESS`, exit code `0`, and emitted only `CWT_REGISTRATION_MATERIALIZATION_PASS`. Repository Runner ID `35` appeared `online`, idle and named `cwt-tencent-sg-3fa227131a83773fedb61ee2b1268d05`, with exact labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, and `cwt-job-3fa227131a83773fedb61ee2b1268d05`.

The Operator credential path had two bounded nonconformances and must not be represented as satisfying the accepted direct hidden-field contract. To avoid the console URL restriction after TAT recovery, the Owner explicitly approved a temporary official TCCLI OAuth grant whose page disclosed full account read/write scope. The no-browser verification value containing OAuth material was rendered in tool output and echoed into the local TCCLI PTY. Separately, the one GitHub Runner registration token was held in process memory but was transiently materialized inside a mode-`0600` local JSON request file for the TAT API call. The registration token value was not printed or placed in repository/evidence, and the request file was deleted immediately after the API response, but the local file still violated the explicit no-file token boundary. After cloud cleanup, `tccli auth logout --profile cwt-runtime` deleted `/Users/calvin/.tccli/cwt-runtime.credential`; the Owner separately confirmed revocation of the Tencent Cloud Command Line Tool application grant, and the live authorization inventory then displayed zero authorized application services. These controls bound retained access and known residue but do not retroactively erase either process nonconformance. They are independent of the Product Runtime result.

Exactly one [Runtime workflow run `34161175361`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34161175361), attempt `1`, was dispatched from `main` at exact head `cf266ae...`. Environment deployment `6316049330` for `cwt-stage6-runtime-validation` was approved once. Job `101863168686` ran from `2026-09-08 04:55:05` through `04:57:53 +0800` and completed `failure`. Both exact checkouts, Node installation, unique Runner binding, detached evidence download, release-header reconciliation, hash-pinned ORAS installation, private GHCR authentication and same-digest read-only OCI materialization passed. The materializer reported exact Release/index identity PASS before the sole Product validator started.

The sole accepted Linux Runtime validator executed exactly once and returned `{"status":"NOT_PASS","reasonCode":"compose_infrastructure_up_failed","evidence":"cwt-runtime-outcome"}` with process exit code `1`. The always-run evidence step succeeded and retained artifact `10032694897`, `cwt-runtime-outcome-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, 2,624 bytes, unexpired through `2026-10-07T20:57:44Z`. The downloaded `linux-runtime-validation.json` and sidecar matched; the JSON SHA-256 is `ea7125c67c5b607a744e353b50561b80307450813b1c28035625a3a84e830039`.

The new diagnostic field narrowed the native failure state without exposing uncontrolled output. At the nonzero combined infrastructure `up --detach --wait` boundary, `postgres` was present, `state=running`, `health=starting`, `exitCode=0`; `valkey-staging` was present, `state=created`, `health=null`, `exitCode=0`. The outcome otherwise retained exact tools/profile, Release/index/selected-child identities, `runtime=null`, `failureDetailCode=null`, all four inner cleanup booleans `true`, no credential values, Synthetic data only and no automatic retry. This reproduces the prior formal `compose_infrastructure_up_failed` boundary and proves the bounded collector worked. It does not establish why Compose returned nonzero, why Valkey had not started, or whether PostgreSQL later would have become healthy; no raw Compose error, health output or service log survived. Root cause therefore remains undetermined and no Product repair is inferred.

The one-shot stop rule closed the attempt immediately after the Product validator started and returned NOT PASS. There was no recovery, retry, rerun, second dispatch, second Runner, second token, replacement host, Product rebuild, Registry write, deployment or phase action. The ephemeral Runner deregistered automatically and the final repository Runner query returned no matching record. Tencent termination request `7df07689-44eb-4304-9126-c2297ad7d97b` removed exact VM `ins-6gu4t4fi`; the subsequent instance inventory query returned total `0`, and exact disk `disk-9mthp5k8` returned total `0`. Exact Security Group `sg-10pl3n2s` was then deleted under request `4742f40f-883f-44b3-8dd0-678d73cd79fb`; a subsequent exact lookup returned `ResourceNotFound`. The ordinary public IPv4 was attached to the destroyed instance and was not retained as an elastic address. Cleanup began immediately after workflow terminal state and completed before `2026-09-08 05:00:21 +0800`, within both the host-age 80-minute teardown-start and 90-minute finish ceilings. Production Lighthouse `lhins-3c2vknjb`, `cwt-production-sg`, shared VPC/subnet, private GHCR Release, Build Once artifact and unrelated resources were preserved.

Final disposition is **NOT PASS / HOLD with zero observed attempt resource residue and two bounded Operator credential-process nonconformances recorded**. The formal native evidence remains `compose_infrastructure_up_failed`, now with PostgreSQL `running/starting` and Valkey `created` at the failure boundary. The next gate is coordinator disposition and independently reviewed root-cause work; this record grants no retry, new VM, token, Runner, workflow dispatch, Build Once, Registry mutation, deployment, S6-06, S6-07, Stage acceptance or later-phase action.

## Native Runner umask-to-bind causal diagnosis

This report-only analysis starts from exact native tools `cf266ae099995ddaf7a2e9572774cd49bc3c1f19`, unchanged Product `68c15e6b...` / index `sha256:c8d4d1c3...`, run `34161175361`, job `101863168686`, and verified outcome artifact `10032694897` / JSON SHA-256 `ea7125c67c5b607a744e353b50561b80307450813b1c28035625a3a84e830039`. The decisive native state is PostgreSQL `running/starting/0` while Valkey is `created/null/0` at the nonzero combined infrastructure-up boundary. The destroyed host retained no source mode or daemon error text, so historical-host attribution cannot be direct.

The accepted TAT invocation file is byte-identical between native tools and this report tree, SHA-256 `0184e5108a4ddd81dbd8edc5060c4c4369e65edd1fbead2c9f2a733179016839`; its decoded 6,894-byte wrapper is SHA-256 `d12e5186d507eebc75fd751b493475034b39d76b740d0fa881c45d9d98f702e7`. It begins with `umask 077`, creates and verifies the embedded payload under that mask, then executes it without restoring the prior mask. The payload configures the Runner and removes the registration token before `cwt_launch_runner`, but that launcher invokes `nohup env -u CWT_REGISTRATION_TOKEN "$runner_root/run.sh"` without a child-scoped mask. `env` and `exec` preserve the process umask.

Primary-source inspection used exact GitHub Actions Runner `v2.337.0` tag commit `397b032cbf865e9c3ddfab89d533ec19325e1273` and workflow-pinned `actions/checkout v6.1.0` commit `d23441a48e516b6c34aea4fa41551a30e30af803`. Runner `run.sh`, `run-helper.sh.template`, Worker/SDK source and checkout source contain no `umask` reset. Runner starts actions as ordinary child processes; checkout copies the inherited environment and invokes `git checkout --progress --force` without a permission repair. The exact source therefore contains no boundary that would normally stop inherited `077` from reaching Git.

Two fresh local Git checkouts of exact `cf266ae...` differed only by checkout-process mask. Both indexes retained tracked mode `100755`; Valkey and PostgreSQL script blobs and SHA-256 values were identical. Under `umask 077`, `deploy/valkey/entrypoint.sh` and `deploy/postgres/init-environments.sh` landed owner-current-user mode `0700`. Under `022`, the same files landed mode `0755`. This proves Git mode `100755` does not itself override the inherited creation mask.

Docker Desktop bind mounts remapped each host source owner to the selected container UID, so its direct bind test allowed both `0700` and `0755` to start. That is an explicit local-platform ceiling, not contrary evidence for native Linux bind ownership. A second minimal control placed the same exact checked-out bytes on the Docker Linux filesystem with native-runner-shaped owner `1000:1000`, preserving only the causal mode difference. Both exact Valkey `linux/amd64` containers used UID/GID `999:999`, read-only root, all capabilities dropped, `no-new-privileges`, accepted limits/tmpfs/health command, no published port, the actual entrypoint command and a Synthetic secret; `json-file` remained the sole Desktop logger exception.

With source mode `0700`, OCI start failed exactly with `exec: "/opt/cwt/valkey/entrypoint.sh": permission denied`. The focused Compose invocation returned nonzero and its actual `ps --all --format json` row was `Service=valkey-staging`, `State=created`, empty/null health and `ExitCode=0`, exactly matching the retained native diagnostic shape; lower-level inspect recorded exit `126` and the permission error. With only source mode changed to `0755`, UID `999` read and executed the script, Valkey became healthy and authenticated `PONG`. Under `0700`, the same UID could neither read nor execute the PostgreSQL init script; under `0755`, both checks passed. PostgreSQL being concurrently `running/starting` in the native snapshot is consistent with Valkey failing at OCI exec before PostgreSQL completed initialization, but it does not prove how PostgreSQL ultimately would have resolved.

The causal mechanism is therefore **reproduced and strongly consistent with the native failure**, while the destroyed host's actual checkout modes and raw start error remain unobserved. It is not represented as mathematical certainty about that host. The earlier exact local full-chain PASS used a normal local checkout with other-user-readable scripts and therefore did not exercise this launcher-to-checkout-to-bind seam.

The smallest coherent repair belongs at the boundary where protected registration ends and the Runner child begins. Keep `umask 077` for the outer TAT wrapper, embedded payload, registration token handling and `config.sh` materialization; after the token has been cleared, launch only the Runner process in a subshell with conventional source-creation mask `022`:

```bash
(
  umask 022
  exec /usr/bin/nohup /usr/bin/env -u CWT_REGISTRATION_TOKEN "$runner_root/run.sh"
) </dev/null >/dev/null 2>&1 &
```

Because the subshell immediately `exec`s, `$!` remains the launched process identity and the parent registration path retains `077`. This replaces the leaked process state at its causal boundary. It does not chmod the checkout after creation, touch secret/config paths, add privilege, change Compose/Product behavior or create a second authority.

Focused implementation evidence should prove: parent/materialization umask remains `077`; payload and existing Runner credential/config files retain their restrictive modes; the launched Runner child observes `022` with the registration token absent; a fresh tiny Git checkout through that child produces tracked executable `0755` and ordinary public source readable by container UID `999`; a `077` negative control reproduces the created/permission-denied shape; and no recursive or broad chmod is introduced. Existing direct-launch PID/liveness, token removal and deterministic wrapper/payload checks must remain green.

All diagnostic checkouts, primary-source clones, containers, Compose project, internal network, Linux volumes, Synthetic secret and scratch data were removed. Final Docker state retained zero running and zero task-owned containers/networks/volumes, the same 11 historical stopped containers and all pre-existing images/caches. No implementation, private Product pull, GHCR authentication, Runner registration, workflow/cloud action, Product build, Registry write, formal validator or deployment occurred. The next gate is coordinator acceptance of this concrete repair plan, followed by the original Developer and independent Reviewer as appropriate; this Analyst does not self-implement or self-approve it.

## Runner child umask repair Candidate

The coordinator accepted the preceding diagnosis and delegated the smallest causal repair on top of report commit `57aba3d9ba63167a2c3d1ee5edd2be74a9f2a74a`. The implementation changes only `cwt_launch_runner` in `deploy/runtime-validation/register-and-start-ephemeral-runner.sh`: its background child enters a subshell, sets `umask 022`, and immediately `exec`s the existing `nohup env -u CWT_REGISTRATION_TOKEN "$runner_root/run.sh"` launch. Redirections remain `</dev/null >/dev/null 2>&1`; the parent still captures `$!`, waits one second and checks that exact process identity with `kill -0`. The registration token has already been cleared before this function is reached, and `env -u` remains the final launch guard.

The outer TAT wrapper still begins with `umask 077`, creates one temporary payload with explicit mode `0700`, verifies its exact byte count and SHA-256, and deletes it. Registration and `.runner` creation remain under the protected parent mask. The parent mask is not restored or globally widened: only the exec-replaced Runner child receives `022`. No `chmod`, privilege wrapper, new exposure, daemon, retry, persistent state, secondary launcher, Product, Compose or validator change was introduced.

The regenerated authoritative registration payload is 3,785 bytes, Git blob `e0b1e4564fa67f3210cc89dc018903cae14033f9`, and SHA-256 `8722709d99d918ea3c52d020971b8a2c80cbc993b2b0df0ce0cf5c1d5ac0cf0d`. The decoded wrapper is 6,938 bytes with SHA-256 `a3309b5ea90710f44ff75ec1d75289f932753e1f81050dbffa22e10d9bb2168f`; the complete invocation file SHA-256 is `ef0c788e8f38ffe0bbae2f8568de73d317b62077c3d2dc62e791799085c53396`. Its operational envelope is unchanged: `RunCommand` / `SHELL`, parameter substitution enabled, hidden `CWT_REGISTRATION_TOKEN`, user `ubuntu`, working directory `/home/ubuntu`, timeout 600 seconds, `saveCommand=false`, and COS output disabled.

The focused launcher test now starts the actual production launcher from a `077` parent and proves that the parent remains `077`, a protected state file and synthetic `.runner` state remain mode `0600`, the child observes `022`, and the registration token is absent. Through that child it performs a real fresh local Git checkout: a tracked executable lands `0755` and ordinary public source lands `0644`. Existing wrapper materialization, deterministic byte/hash, direct registration, PID/liveness, failure, identity, label and token-custody assertions remain active.

The registration and adjacent provisioning Node tests recorded 35 pass, zero failures and zero skips on the development host. The 14-test registration suite also passed in an existing public `node:24.14.0-bookworm` container with its scratch checkout on Docker's Linux filesystem, no network and a read-only repository mount. Targeted ESLint, Bash syntax for both registration and provisioning scripts, Node syntax for both focused test files, and `git diff --check` passed. A separate owned Docker Linux-filesystem control reused the existing public `ubuntu:24.04` and exact `valkey/valkey:8.1.9` images with Synthetic credentials. With the actual production source installed owner `1000:1000` mode `0700` and mounted at `/opt/cwt/valkey-entrypoint.sh`, service UID/GID `999:999` failed OCI execution with permission denied and exit `126`; mode `0755` alone made the same read-only, capability-dropped container start and return authenticated `PONG`. The test container and three task-owned volumes were removed.

Changed files are limited to the authoritative registration launcher, its existing embedded Tencent TAT invocation, its focused test, and this existing report. No full Product suite or build was run because the Product and Compose graph are unchanged. The fresh-checkout launcher test passed on both the local macOS filesystem and Docker's Linux filesystem, while the separate Linux-volume control exercised the exact UID and bind target; these cover the causal boundaries but do not replace a future independently authorized native Tencent Runner validation. No cloud resource, OAuth grant, credential, TAT invocation, Runner, workflow, Registry, private Product, deployment or phase action was created or used. This Candidate requires independent Review before any native rerun or later gate; it does not self-approve or authorize one.

## Post-Review native validation operation preparation

Independent Review commit `787d699ce68c562ea02ee5b632500c23fd2787e6` returned PASS for code Candidate `f6703406353412400cf65536915c7a4614ab7dfe`, including the single Security & Test Simplification Check. The delivery branch fast-forwarded from the exact Candidate to that Review commit without conflict. Review changed only the existing independent-review report; comparison with the Candidate confirmed no executable-tree change. This preparation selects exact `787d699ce68c562ea02ee5b632500c23fd2787e6` as both the proposed workflow/tools identity and the future dispatch head. This principal-report update remains local evidence and is not part of that selected dispatch identity.

| Proposed field | Exact value |
| --- | --- |
| Workflow and tools commit | `787d699ce68c562ea02ee5b632500c23fd2787e6` |
| Future dispatch ref | `main`, only after remote `main` is advanced to exact `787d699c...` |
| Runtime workflow | `.github/workflows/cwt-runtime-validation.yml` |
| `workflow_commit` | `787d699ce68c562ea02ee5b632500c23fd2787e6` |
| `release_commit` / immutable Product | `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641` |
| `index_digest` | `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4` |
| Selected `linux/amd64` child | `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c` |
| `evidence_run_id` / retained artifact | `34139907082` / `10025983850` |
| Proposed fresh nonce | `1119c49b7a0c600841b6b61a6224d80b` |
| Runner name | `cwt-tencent-sg-1119c49b7a0c600841b6b61a6224d80b` |
| Unique job label | `cwt-job-1119c49b7a0c600841b6b61a6224d80b` |

An independent remote read at `2026-09-08 05:40 +0800` resolved `origin/main` to `cf266ae099995ddaf7a2e9572774cd49bc3c1f19`. The selected `787d699c...` is an ordinary fast-forward descendant, five commits ahead and zero behind. The exact future source update is therefore `787d699ce68c562ea02ee5b632500c23fd2787e6:refs/heads/main`; the later local report-only commit must never be substituted. No Push occurred in this preparation.

Fresh GitHub read-only checks found Build Once run `34139907082` still `completed/success` at exact Product head `68c15e6b...`. Artifact `10025983850`, named `cwt-release-evidence-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, remains unexpired through `2026-10-07T16:02:05Z`. Repository Actions remains enabled with allowed actions `all` and SHA pinning required. The authenticated repository identity currently has `ADMIN`, there are zero registered repository Runners and zero queued, in-progress or waiting runs. Environment `cwt-stage6-runtime-validation` still has required-reviewer and branch-policy protection. Previously accepted Product header, index and child byte verification is reused; no artifact download, private Product pull, build or broad test rerun occurred.

The reviewed registration binding remains exact: launcher 3,785 bytes and SHA-256 `8722709d99d918ea3c52d020971b8a2c80cbc993b2b0df0ce0cf5c1d5ac0cf0d`; decoded wrapper 6,938 bytes and SHA-256 `a3309b5ea90710f44ff75ec1d75289f932753e1f81050dbffa22e10d9bb2168f`; complete invocation file SHA-256 `ef0c788e8f38ffe0bbae2f8568de73d317b62077c3d2dc62e791799085c53396`. The unchanged provisioning script remains SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0`, and its existing invocation contract file remains SHA-256 `98d67f285e0fd5afb57a6a4b7227c57b6214cdb8992d44a552d4059ba81e41b1`. Review's 35/35 focused registration/provisioning tests and 14/14 Linux-filesystem launcher tests are reused without another broad suite.

Tencent readiness could not be freshly authenticated in this preparation. At `05:40 +0800` the Mac was locked and the approved UI-control path could not unlock it. The prior OAuth application grant remains revoked. No callable `tccli` entrypoint, importable Tencent SDK, credential file or matching Keychain item is currently available; the two retained `.configure` files contain ordinary CLI service configuration rather than SecretId/SecretKey, access token or refresh token. No credential was retrieved or refreshed, and no new OAuth grant was created. The latest authenticated same-day evidence therefore remains the earlier zero-CVM inventory and purchase-form quote: Singapore Zone 2 exact `SA5.MEDIUM4`, 2 vCPU / 4 GiB, public Ubuntu Server 24.04 image `img-mmytdhbn`, one 60 GiB general-purpose SSD, existing `vpc-piootcsf` / `subnet-6uvqq9wg`, ordinary BGP IPv4 at 5 Mbps, CNY `0.41/hour` base plus CNY `0.79/GB` outbound traffic, and free Automation Assistant selected. It is carried-forward evidence, not a fresh transactional quote. An unlocked, authenticated Tencent console and an immediate read-only inventory/stock/price refresh are concrete prerequisites before purchase; any required provider login or MFA is an access step rather than a new scope decision. If the exact shape, image, network selection, disk, public-IP mode or quote changes materially, stop before purchase.

The credential-safe registration path is the already accepted direct provider hidden-parameter flow. GitHub documents that a repository Runner registration token is created only for a repository administrator and expires after one hour. Tencent's current [RunCommand contract](https://cloud.tencent.com/document/api/1340/52676) accepts Base64 `Content`, parameter substitution and a JSON-encoded `Parameters` value; its documented `{{tat-hidden:key}}` form substitutes the real value while recording `**`. The Owner or authorized Operator must use an unlocked authenticated GitHub repository administration page to generate exactly one token only after provisioning PASS, copy it directly in the browser, and paste it into the Tencent TAT execution form's `CWT_REGISTRATION_TOKEN` value corresponding to `{{tat-hidden:CWT_REGISTRATION_TOKEN}}`. The three non-secret values are the proposed nonce, exact runner name and `czy282923753/cwt-cloudwave-textile`. After submission, replace the clipboard with the non-secret nonce before any further capture. Do not use Codex screen/accessibility capture while the token is visible.

This browser-to-hidden-field transfer creates no token file, argv value, printed output, pipe, loopback bridge, background process, helper or credential store. It also avoids the revoked TCCLI OAuth route. The local CLI/SDK route is unavailable and is not a fallback; no official SDK/API credential method is proposed, so no Synthetic SDK mutation rehearsal is needed. If the authenticated TAT console does not expose the direct parameter form without rendering or retaining the secret outside the provider's documented hidden handling, stop before token generation rather than switching methods. Standard interactive GitHub and Tencent sessions, plus MFA if demanded, are the only actual access prerequisites. The already granted unchanged in-budget purchase and owned-resource destruction authority requires no repeat generic approval.

The executable operation is:

1. Unlock the Mac, establish ordinary authenticated GitHub and Tencent console sessions, and satisfy any direct provider MFA. Recheck remote `main=cf266ae...`, its fast-forward ancestry to `787d699c...`, zero repository Runners/active runs, Artifact availability, zero Singapore attempt-owned resources, the approved shared network selection and the exact live quote.
2. Under the later coordinator go-ahead, advance remote `main` once by normal non-force fast-forward to exact `787d699c...`; reread it as exact before any host purchase. Never Push the local report-only successor as the dispatch head.
3. Create one nonce-named temporary Security Group with zero inbound rules and exactly one IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. Purchase at most one exact approved CVM. Start the single lifecycle clock from instance creation and never reset its age, cost estimate or attempt counters.
4. After the instance reports running, explicitly query TAT Agent status. The purchase checkbox establishes requested installation only, not online readiness. If the initial client is absent, use only the existing permitted same-host VNC path and official Tencent installer `https://mirrors.tencentyun.com/install/tat_agent/tat_agent_installer.sh`, then require exact Agent Online before provisioning. This is completion of initial host readiness before any provisioning invocation; it is not the admitted preparation-recovery branch and does not consume or reset that branch, the lifecycle clock or deadlines. If it cannot complete inside the remaining envelope, destroy the owned resources.
5. Run exactly one existing provisioning TAT invocation as `root` from `/root`, with no secret present. Require terminal `SUCCESS` and process exit `0`; the bounded marker is corroboration only. Only a later terminal provisioning failure may be considered for the single same-host recovery, and only after checking every existing admission condition, including no private-GHCR or Product-validator start and at least 45 minutes remaining before teardown starts. A second failure or failed admission requires cleanup; initial TAT bootstrap must never be mislabeled as this recovery.
6. After provisioning PASS, generate one repository-scoped registration token and submit the reviewed registration invocation once through the direct hidden-field path: exact Base64 `content`, `SHELL`, parameter substitution enabled, `ubuntu`, `/home/ubuntu`, timeout 600, `saveCommand=false`, COS output disabled, and the four exact parameter values. Clear the clipboard and require the exact Runner name, six cumulative labels, online and idle state.
7. Dispatch `.github/workflows/cwt-runtime-validation.yml` from remote `main` only when both remote and input `workflow_commit` equal `787d699c...`, using the exact Product/index/evidence/nonce tuple above. Approve the existing protected Environment once. The 60-minute job, one accepted Product validator start and one-shot outcome remain authoritative; no automatic retry or second dispatch is allowed.
8. At terminal outcome, begin cleanup immediately. Verify the ephemeral Runner record is absent, terminate the exact owned CVM and confirm its disk and ordinary address are gone, then delete the exact temporary Security Group and prove absence. Begin teardown by host age 80 minutes and finish by 90 minutes. The one-host CNY 10 maximum estimated exposure, immediate terminal cleanup and existing recovery ceilings remain unchanged; owned destruction does not wait for another generic approval.

No cloud/API mutation, OAuth/credential action, token, TAT/Runner, Push, workflow dispatch, Registry operation, private Product pull/build, formal validator or deployment occurred in this preparation. The next gate is coordinator verification of this exact operation and acknowledgement of the concrete unlocked-console/session/MFA prerequisite. This report neither requests general Owner permission nor starts paid execution.

## Post-Review native Runtime validation outcome

The coordinator and Owner authorized exactly one native Tencent Singapore validation attempt using reviewed workflow/tools commit `787d699ce68c562ea02ee5b632500c23fd2787e6`. Remote `main` was advanced once by ordinary fast-forward from `cf266ae099995ddaf7a2e9572774cd49bc3c1f19` to exact `787d699c...`, then reread as the dispatch head. The immutable Product remained release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, and Build Once run `34139907082` / artifact `10025983850`. The unique attempt nonce was `1119c49b7a0c600841b6b61a6224d80b`.

That Push automatically triggered ordinary `CWT quality gates` run [`34164993698`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34164993698). It was a Push event, not a second native Runtime dispatch. Dependency security passed, while three existing CI paths failed before their downstream checks: Homebrew supplied PostgreSQL `18.6` while the workflow required an exact `18.4` Cellar path and version; the public-bundle build rejected its non-40-character `CWT_RELEASE_ID`; and the Browser web server imported `server-only` through `src/uploads/scanner-factory.ts` from a Client Component path. These failures are separate from the reviewed Runner child-mask change and native Runtime result. They remain visible quality-gate failures and are not represented as green, but this bounded Runtime operation did not repair or rerun them.

One temporary Security Group, `sg-4iixds1o` / `cwt-runtime-1119c49b7a0c600841b6b61a6224d80b`, was created with zero inbound rules and exactly one IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. One disposable Singapore CVM, `ins-rqemd1pm` / `cwt-tencent-sg-1119c49b7a0c600841b6b61a6224d80b`, used Ubuntu 24.04, exact `SA5.MEDIUM4` at 2 vCPU / 4 GiB, one 60 GiB general-purpose SSD, ordinary public IPv4 `43.134.189.33`, private IPv4 `172.22.0.2`, 5 Mbps traffic billing and the accepted shared VPC/subnet. The instance detail recorded creation at `2026-09-08 05:58:19 +0800`.

The purchased host again did not initially expose a usable TAT Agent. The official Tencent installer was downloaded byte-completely from `https://mirrors.tencentyun.com/install/tat_agent/tat_agent_installer.sh` through VNC and installed on the same disposable host. TAT Agent `1.2.2` started, established its Tencent WebSocket connection and successfully returned `DescribeTasks`. No inbound rule, password reset, replacement host or alternate remote-access path was introduced.

Exactly one provisioning TAT invocation, `inv-28d6dg0uqn` / task `invt-28d6dg0uqp`, ran from `06:00:01` through `06:01:14 +0800`, returned command success and exit code `0`, and used the exact 19,915-byte provisioning payload with SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0`. Its bounded marker was `CWT_PRE_REGISTRATION_OK os=ubuntu-24.04 arch=amd64 docker=29.6.2 compose=5.3.1 runner=2.337.0 preparation=fresh runner_tree_owner=ubuntu diag_write_probe=PASS docker_user_probe=PASS`.

Exactly one registration TAT invocation, `inv-x8d6h4gck7` / task `invt-x8d6h4gck8`, ran from `06:03:54` through `06:03:59 +0800`, returned command success and exit code `0`, and emitted `CWT_REGISTRATION_MATERIALIZATION_PASS`. The reviewed launcher remained 3,785 bytes with SHA-256 `8722709d99d918ea3c52d020971b8a2c80cbc993b2b0df0ce0cf5c1d5ac0cf0d`; its decoded wrapper remained 6,938 bytes with SHA-256 `a3309b5ea90710f44ff75ec1d75289f932753e1f81050dbffa22e10d9bb2168f`. The repository token moved directly from the authenticated GitHub browser page to Tencent's documented hidden parameter field. It was not printed, saved to a file or placed on a command line, and the browser clipboard was overwritten with the non-secret nonce immediately after submission.

Before dispatch, repository Runner inventory contained exactly one online, idle Runner, `cwt-tencent-sg-1119c49b7a0c600841b6b61a6224d80b`, with the exact cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use` and `cwt-job-1119c49b7a0c600841b6b61a6224d80b`. Workflow run [`34165385584`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34165385584), job `101875288060`, dispatched from `main` at exact head `787d699c...`. The existing protected environment `cwt-stage6-runtime-validation` was approved once. The job ran from `06:04:56` through `06:08:00 +0800` and completed `failure`; no retry or second dispatch occurred.

Steps through both exact checkouts, Node installation, unique Runner binding, detached Build Once evidence download, release-header reconciliation, hash-pinned ORAS installation, credential-argument-free private GHCR authentication and exact OCI materialization all passed. OCI materialization again bound the authorized index to the exact selected `linux/amd64` child. The sole Runtime authority ran from `06:06:09` through `06:07:51 +0800` and was the only failed step. Its sanitized artifact `10034024882`, `cwt-runtime-outcome-68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, contains `linux-runtime-validation.json` with SHA-256 `972b7c7c541dd74340d0c923bc901b307c5d6977e9a5ee2c112de23c762e8a69`. It records `status=NOT_PASS`, `reasonCode=compose_project_inventory_invalid`, null `failureDetailCode`, the exact reviewed tools/Product/Runner profile, and successful cleanup of Compose consumers, networks, pulled references and host paths. Credential recording remained false, Synthetic-only data remained true and automatic retry remained false.

The failure is a deterministic Docker Compose 5.3.1 output-compatibility defect in the validator, not a recurrence of the repaired checkout permission boundary. `validateProjectServiceSet` currently passes the complete stdout from `docker compose ... ps --all --format json` to one `JSON.parse`. A bounded task-owned local control on exact Compose `v5.3.1` showed that one running service produces one JSON object, while two running services produce two individually valid newline-delimited JSON objects; parsing the complete two-line value as one JSON document fails. The native attempt reached the inventory check only after the exact two-service infrastructure `up --wait`, one-off Migration and `web-staging up --wait` had all succeeded. The retained `compose_project_inventory_invalid` therefore matches this multi-row parser incompatibility exactly and does not indicate that the three-service set differed. The earlier Runner child `umask 022` repair is operationally corroborated because checkout scripts executed through infrastructure and Web startup instead of failing at OCI entrypoint permission.

Cleanup began after the terminal outcome but required interactive Owner identity validation before Tencent accepted destruction. The instance was then destroyed, its associated system disk and ordinary public address were released, and Security Group `sg-4iixds1o` was deleted after its association count reached zero. Authenticated Singapore inventories subsequently showed zero CVMs, zero cloud disks, zero public IPs and zero Security Groups. GitHub repository Runner inventory also returned zero. Final absence proof completed by `2026-09-08 07:49:12 +0800`. This was approximately 111 minutes after instance creation and exceeded both the accepted teardown-by-80-minute and finish-by-90-minute limits. It is recorded as an operational nonconformance; provider identity validation and the interactive handoff do not reset or excuse the lifecycle clock. Exact provider-settled cost was not yet available and no cost-compliance claim is made.

The attempt is **NOT PASS** and creates no Stage acceptance, deployment or promotion authority. The native host and all attempt-owned cloud resources are gone, so there is no recoverable raw service inventory. The minimum next implementation is to replace the single-document parser at the existing bounded inventory boundary with strict support for Compose 5.3.1's one-object or newline-delimited-object output, while retaining the exact three-service equality check and sanitized failure behavior. That change requires focused tests and independent Review before any separately authorized native rerun. This execution report performs no such implementation, rerun, Product rebuild, Registry write, deployment or phase action.

## Technical Escalation causal convergence and replacement repair plan Candidate V1.0

Status: **ANALYSIS COMPLETE / REPAIR PLAN CANDIDATE / HOLD — the failed remediation loop is frozen; no implementation, Push, rebuild, Registry mutation, native retry, deployment or phase action is authorized by this section.** Plan identity: `S6-TE-RP-V1.0`. Analysis baseline: report/evidence commit `78596cf51ea67cc43a7999ebe91b3413f54c3ed4`, whose parent is preparation-only commit `4e0b754ff548392d85cd19f19d4e1214505d0c14`; reviewed executable tools remain exact `787d699ce68c562ea02ee5b632500c23fd2787e6`. This section is the sole versioned Technical Escalation analysis/plan addition and does not replace the immutable evidence already recorded above.

### Frozen evidence and causal conclusion

The prior corrections were causally valid at their own boundaries, but each successful correction exposed a later contract that had not been executed before the next paid native attempt. This is not evidence that the dependency-closure, outcome-handoff or Runner-child-umask corrections regressed. The recurring system-level cause is **continuation-proof fragmentation**: real command contracts were parsed or launched through multiple caller-specific paths, and the paid one-shot native run remained the first coherent execution of the newly reachable downstream path. The current source demonstrates the same pattern outside the native validator: three Compose inventory consumers use two incompatible parsing policies; CI asks a rolling Homebrew formula for an exact patch release; builds require a release identity that CI never binds; and server-only CLI callers do not consistently select the already established Node `react-server` condition.

The replacement direction is therefore one bounded convergence, not a fourth symptom patch:

1. one strict Compose-`ps` row parser serves every current consumer;
2. CI inputs become immutable or source-derived at their existing authority boundaries;
3. every direct server-only CLI caller uses the repository's existing Node export-condition convention;
4. the exact Product/runtime continuation and all three ordinary CI paths must pass before any future native run can be considered;
5. the cloud lifecycle clock and credential-handling timestamps are recorded as separate events instead of being inferred from final absence.

| Frozen fact | Evidence-backed result |
| --- | --- |
| Native authority | GitHub run [`34165385584`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34165385584), job `101875288060`, one `workflow_dispatch`, exact head `787d699c...`; checkouts, Runner binding, detached evidence, release reconciliation, ORAS, GHCR authentication and exact OCI materialization passed. |
| Sole native failure | The Runtime authority ran from `06:06:09` through `06:07:51 +0800` and returned `NOT_PASS / compose_project_inventory_invalid`; retained artifact `10034024882` contains `linux-runtime-validation.json` at SHA-256 `972b7c7c541dd74340d0c923bc901b307c5d6977e9a5ee2c112de23c762e8a69`. |
| Product binding | Release `68c15e6bcd2900f5e634f6c6c6e3b8b3f5550641`, index `sha256:c8d4d1c3148670a683a3509bafb7d832afc6fbe3bb811e2de19b27ae63540ce4`, selected `linux/amd64` child `sha256:6016047d8e2801e1971000d93046575d9e1c19d34bd1a06b1fee52c12418d57c`, Build Once run `34139907082` / artifact `10025983850`. No Product byte changed during the tools/Runner repairs. |
| Reached Product path | Exact infrastructure `up --wait`, the one-off Scheduler Migration and `web-staging up --wait` necessarily completed before the inventory parser ran. Artifact cleanup records all four existing cleanup classes true. |
| Compose contract | Docker's current [`docker compose ps` reference](https://docs.docker.com/reference/cli/docker/compose/ps/) expressly defines `--format json` as JSON Lines, one object per line. The current success-path validator nevertheless applies one `JSON.parse` to the complete multi-row stdout. |
| Local continuation control | Exact local Compose `5.3.1` produced three NDJSON rows. Whole-document parsing refused them, while row parsing yielded exact services `postgres`, `valkey-staging`, `web-staging`, three unambiguous service-ID lookups, container inspections, in-container live/readiness/root `200` probes with noindex, and cleanup to zero task-owned consumers and networks. This is non-authoritative local evidence, but it proves the deterministic continuation mechanism. |
| Product drift check | `68c15e6b...787d699c` changes only the Runner launcher/binding, Runtime preflight/test and existing reports. There is no application, schema, URL, SEO, publishing, storage or business-data change in that interval. |

The native failure is thus caused by a tools-side framing defect, not by a missing service, Product startup failure, Docker-in-Docker, Registry identity mismatch or recurrence of the Runner umask fault. The retained sanitized artifact cannot prove the raw three rows byte-for-byte because raw output was intentionally not retained, but exact source control flow, the successful preceding steps, Docker's documented output contract and the exact-version local reproduction are jointly decisive.

### Replacement A — one strict Compose inventory parser

The implementation must export one small `parseComposePsRows` helper from the already authoritative `deploy/scripts/preflight-compose-graph.mjs` and import it into `deploy/scripts/preflight-linux-runtime.mjs`. No new parser file, wrapper, command runner, evidence format, reason-code taxonomy or validation framework is allowed.

The helper contract is:

- input must be a string no larger than `64 KiB`; maximum parsed row count is `128`;
- an empty trimmed value yields an empty row set;
- documented JSON Lines is primary behavior, while a single JSON object and a JSON array remain accepted for compatible Compose versions and existing tests;
- malformed JSON, internal blank records, primitives, nested arrays, nulls, oversized input and excessive rows fail closed;
- every row must be a non-array object whose `Service` value is a string before any caller filters or maps it;
- the helper returns only validated rows or `null`; it does not log, retain raw output, classify Product state or convert any failure to PASS.

That helper replaces all three current policies:

| Consumer | Required post-replacement behavior |
| --- | --- |
| Runtime infrastructure-failure diagnostic | Keep its stricter existing `16 KiB` / 10-second collection envelope and safe-field sanitizer. Invalid shared-parser output remains `null` and never replaces the original `compose_infrastructure_up_failed`. Unknown services with valid string names remain ignored; duplicate target rows remain rejected. |
| Runtime success inventory | Invalid framing/schema keeps exact reason `compose_project_inventory_invalid`. Valid rows must contain exactly one each of `postgres`, `valkey-staging` and `web-staging`; any missing, extra or duplicate service keeps exact reason `runtime_service_set_mismatch`. No weakening to subset membership is permitted. |
| Protected Staging pre/post state | Replace its direct `JSON.parse`/array-only assumption with the shared helper. Invalid rows keep the existing fail-closed project-state error; the existing unique-service `Map`, Production/staging state, pause, health and memory predicates remain unchanged. |

This same change closes accepted Low `S6-ID-L-01`: `{}` and `{ "Service": 1 }` must now produce unknown/invalid `null`, never a synthetic report that both target services are absent. This is required because the parser is being changed; it must not become a separate remediation loop.

Allowed implementation scope for Replacement A is exactly:

- `deploy/scripts/preflight-compose-graph.mjs`;
- `deploy/scripts/preflight-compose-graph.test.mjs`;
- `deploy/scripts/preflight-linux-runtime.mjs`;
- `deploy/scripts/preflight-linux-runtime.test.mjs`.

Focused proof must cover documented multi-row JSON Lines, CRLF, single-object and array compatibility; empty output; bounds; malformed/primitive/nested/null rows; missing and non-string `Service`; duplicate/missing/extra exact Runtime services; unchanged diagnostic sanitization; and an actual disposable Compose `5.3.1` three-service inventory followed by service lookup, container inspection, application probes and zero-residue cleanup. The protected pre/post consumer must have a focused regression that proves it consumes the same helper. The implementation may factor a pure exact-service predicate only if required for direct testing; it may not add a second parser or state authority.

### Replacement B — repair all three ordinary CI failure paths

Automatic Push-triggered run [`34164993698`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34164993698) used exact head `787d699c...`. Dependency security job `101874173710` completed successfully as non-applicable for that unchanged dependency scope. The three substantive paths failed independently before their downstream gates. None is evidence that the native Product startup failed.

#### B1. Exact PostgreSQL without a rolling package-manager contradiction

Job `101874173766` installed Homebrew `postgresql@18` `18.6` and then required the literal nonexistent `/opt/homebrew/Cellar/postgresql@18/18.4/bin`; the step failed before any project migration or test. The current [Homebrew formula](https://formulae.brew.sh/formula/postgresql@18) is a rolling major-version formula, so it cannot be the authority for exact patch `18.4`.

Replace the macOS/Homebrew provisioning body, rather than discovering a mutable Cellar path or broadening acceptance to arbitrary `18.x`:

- run `quality-postgres` on the stable GitHub-hosted `ubuntu-24.04` `linux/amd64` label;
- define one job service from the already frozen production index `postgres:18.4-bookworm@sha256:882236b897e39051d2368c5ccc6cda944904723506b2dfc97f2a8f5bc9afa382`;
- retain Synthetic-only values: `POSTGRES_USER=cwt_ci`, `POSTGRES_DB=cwt_ci`, `POSTGRES_HOST_AUTH_METHOD=trust`;
- map only `55432:5432`, use `pg_isready -U cwt_ci -d cwt_ci` as the bounded service health check and retain the existing `CWT_PHASE_C_POSTGRES_URL`;
- delete the Homebrew install, handwritten init/start, path/version exception, log-path and stop steps. GitHub manages the service container lifecycle and network; its documented contract requires an Ubuntu runner and supports host port mapping ([GitHub PostgreSQL service guide](https://docs.github.com/en/actions/tutorials/use-containerized-services/create-postgresql-service-containers), [workflow service syntax](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax)).

The digest, not a mutable tag or downloaded package-manager path, is the PostgreSQL patch-level authority. Existing fresh Migration and full PostgreSQL-enabled test steps remain the behavioral proof. Do not change the frozen production PostgreSQL pin, application schema, database driver predicates or test expectations.

#### B2. Bind the exact checked-out source identity to every CI build

Job `101874173873` reached `next build` and failed because `next.config.ts` correctly rejects an empty/non-40-character `CWT_RELEASE_ID`. The workflow checks out `${{ github.event.pull_request.head.sha || github.sha }}` but never exports that same identity to the build.

Add one workflow-level value, `CWT_RELEASE_ID: ${{ github.event.pull_request.head.sha || github.sha }}`, so build and Browser paths consume the same exact source selected by checkout. Keep the lowercase 40-character check and `generateBuildId`; do not insert a zero value, short SHA, branch name, fallback inside `next.config.ts` or test-only exception. Focused proof must show empty input still refuses, an exact 40-character head is accepted and the produced `.next/BUILD_ID` equals that head.

#### B3. Preserve `server-only`; correct the server CLI entrypoints

Job `101874173833` failed while Playwright's web-server command executed `tsx scripts/seed-fixtures.ts`, before Next build or browser tests. The emitted `server-only` message says “Client Component,” but the stack is an ordinary Node CLI stack through `scripts/seed-fixtures.ts` into `src/uploads/scanner-factory.ts:1`. The package exports its throwing default under ordinary Node and its empty marker under the `react-server` condition. A bounded local reproduction therefore refuses the current plain command and imports the same factory successfully with `node --conditions=react-server --import=tsx`.

The `server-only` marker is the correct security boundary and must remain. Converge the four actual direct CLI consumers on the repository's existing convention:

- change `db:seed:fixtures` to `node --conditions=react-server --import=tsx scripts/seed-fixtures.ts`;
- change `assets:rescan-legacy` to `node --conditions=react-server --import=tsx scripts/rescan-legacy-assets.ts`;
- change the two Playwright direct invocations for `scripts/seed-e2e-retryable-asset.ts` and `scripts/seed-e2e-block-projection.ts` to the same conditioned Node form;
- leave `scripts/seed-e2e-editorial-roles.ts` and other entrypoints unchanged unless direct import evidence places them in the same boundary.

Do not remove or mock `server-only`, move protected scanner composition into client-safe code, weaken public-bundle checks, add a loader wrapper, or create duplicate package scripts merely to hide the condition. Focused regression must freeze the direct consumer set and exact conditioned caller strings. A real fresh Synthetic fixture seed must pass under the conditioned entrypoint while the ordinary unconditioned import continues to refuse.

Allowed implementation scope for Replacement B is:

- `.github/workflows/ci.yml`;
- `package.json`;
- `playwright.config.ts`;
- `deploy/scripts/build-release-once.test.mjs`, which already owns a CI build-caller contract;
- `src/uploads/scanner-factory.test.ts`, which already owns the server-only scanner composition boundary.

`pnpm-lock.yaml` must remain byte-unchanged because no dependency changes. No application route, domain service, schema/Migration, SEO, publishing, storage, scanner policy, workflow permission or protected-environment rule is in scope.

### Immutable Product disposition

Replacement A alone is tools-side and preserves the current immutable Product identity. Source comparison and the native path reached support continuing that exact Product beyond inventory for parser-focused diagnosis; no rebuild is justified by the parser defect itself.

Replacement B3, however, exposes a separate real Product-source defect. Exact release `68c15e6b...` ships `package.json`, `scripts` and `src`; its shipped `assets:rescan-legacy` command is plain `tsx` even though it directly imports the server-only scanner factory. `docs/ASSET_AND_UPLOADS.md` defines this command as the authoritative historical-Asset rescan operation. The same ordinary-Node export-condition failure therefore affects a supported operational command in the immutable image, not only Synthetic Browser fixtures.

The existing image must not be modified, retagged, overwritten or silently declared repaired. After implementation and independent Review, the coherent corrected source requires a **new** separately authorized Build Once, new release/index/child/evidence identities and the existing exact-image gates. Release `68c15e6b...` remains retained historical evidence and must not be deleted by this plan, but it is not eligible to be the final future Stage 6 subject once the required operational CLI correction is accepted. If the coordinator or Owner instead wants to defer that Product correction and reuse `68c15e6b...`, they must explicitly accept and record the broken required rescan command as a Product risk; this Candidate does not recommend that alternative.

### Verification ladder before any native retry

The repair is complete only when all of the following are evidenced in order:

1. The fresh Implementer changes only the frozen allowed scope and records one coherent code Candidate; no Push or external action occurs.
2. Local focused tests prove the shared parser, exact-service failures, `S6-ID-L-01`, both protected-state and Runtime consumers, CI source identity, exact PostgreSQL service definition and every scanner-factory CLI caller.
3. One task-owned real Compose `5.3.1` continuation reaches exact service inventory, three unambiguous service-ID lookups, `docker inspect` assertions, in-container live/readiness/root/noindex probes, and cleanup to zero task-owned containers, networks, pulled references and host paths. It must use the production Runtime plan or its exported pure helpers, not a second hand-written acceptance authority.
4. Proportionate repository gates pass: targeted ESLint and syntax, workflow parsing/static contract, `pnpm typecheck`, focused deployment tests, full PostgreSQL-enabled suites and Browser acceptance without retries. No failing path may be relabeled non-applicable.
5. A separate fresh Reviewer verifies the Candidate and the single Security & Test Simplification Check. The Implementer cannot self-review.
6. Only after Review and separate Build Once authority, produce a new immutable Product because B3 corrects shipped source. Both children must pass the existing Build Once gates. An exact-digest, non-authoritative `linux/amd64` continuation must then repeat service lookup, container inspection, application probes and cleanup against that new Product.
7. Only after the new Product/runtime continuation and a real GitHub `CWT quality gates` run show all three substantive jobs plus dependency security green may the coordinator prepare a separately authorized native attempt. The existing 60-minute job maximum, one-Product-validator-start rule, one host, CNY 10 ceiling and no automatic retry remain unchanged.

This ordering prevents a native host from becoming the first test of another locally reachable parser, CLI or CI contract. It does not replace native Ubuntu 24.04/`linux/amd64` evidence for the final host-specific authority.

### Operational nonconformances and future teardown control

The retained timestamps must be separated honestly:

| Lifecycle event | Retained evidence | Disposition |
| --- | --- | --- |
| Instance creation | `2026-09-08 05:58:19 +0800` | Exact lifecycle-clock start. |
| Runtime job terminal | `2026-09-08 06:08:00 +0800` | Exact; cleanup narrative begins after this event. |
| First cleanup request/initiation | No exact timestamp retained; the narrative says cleanup began after terminal outcome. | **UNPROVEN timestamp.** It must not be substituted with either workflow terminal time or final absence. The earlier statement that the start-by-80 limit was definitely exceeded is not independently supported by the retained timestamp set. |
| Tencent destruction acceptance/submission | No exact timestamp retained; only the fact that interactive Owner identity validation preceded acceptance. | **UNPROVEN timestamp.** |
| Provider deletion completion | The console did not expose a stable captured completion timestamp. | **UNAVAILABLE; do not infer it.** |
| First authoritative zero-resource observation | Completed by `2026-09-08 07:49:12 +0800` | Conservative final proof at approximately 111 minutes. The finish-by-90 requirement is NOT PASS because no earlier authoritative completion proof exists. |

The token transport also missed its assigned sequencing contract. The exact attempt transferred the repository token directly into Tencent's hidden parameter and retained no token output/file/argv value, but the clipboard was overwritten **after submission**, whereas the reviewed instruction required overwrite immediately after the hidden field accepted the paste and **before submission**. This is an Operator-process nonconformance, separate from Product and validator causality. No retained evidence demonstrates token disclosure, and final Runner inventory is zero; those facts limit observed impact but do not retroactively make the sequence conformant.

The least-complex future operating strategy is an attended one-shot runbook, not a new watchdog or standing credential framework:

- before purchase, one named authorized Owner/Operator must remain continuously available through final absence with unlocked authenticated GitHub and Tencent sessions and any ordinary MFA prerequisites satisfied;
- create no host unless the direct hidden-parameter form and exact deletion console are both reachable in that session;
- start one visible monotonic lifecycle clock from provider-recorded instance creation and record wall-clock timestamps for terminal outcome, first cleanup click/request, final destruction submission/acceptance and first zero-inventory proof as distinct events;
- after paste acceptance, overwrite the clipboard with the non-secret nonce before pressing Submit, and take no screen/accessibility capture while the token is visible;
- begin cleanup immediately on terminal outcome and impose an internal manual abort at host age 60 minutes: if the validator is still active, cancel the attempt and start teardown. This does not shorten the existing workflow's configured 60-minute maximum; it gives the resource limits precedence and preserves at least 20 minutes before the 80-minute start ceiling and 30 minutes before the 90-minute final ceiling;
- measure final completion conservatively by the first authenticated zero-inventory observation for the exact VM, disk, public address, Security Group and Runner when Tencent exposes no stable completion timestamp; preserve the request/acceptance timestamps separately and never relabel them as completion;
- if Owner identity validation is unavailable or the deletion request has not been accepted by host age 70 minutes, mark the lifecycle **AT RISK** and continue mandatory cleanup as the only activity. Determine the actual start/finish result only from the separately recorded 80/90-minute events. Do not create another host, retry Product validation or reset the clock.

This strategy uses existing interactive authority and requires no TCCLI/OAuth grant, API key, daemon or cleanup framework. It materially reduces the demonstrated handoff delay but cannot mathematically guarantee a provider-side deletion completion time. If the Owner requires the 90-minute finish to be a guaranteed rather than conservatively evidenced operating bound, a separate Owner decision is required before any paid run: either authorize a narrowly scoped pre-established deletion mechanism under existing security governance, or accept that the interactive provider step remains an external timing risk. No such access or risk acceptance is granted here.

### Rollback, open risks and next gate

The source repair should be one reviewable Candidate. Before a new Product is built, rollback is one normal revert of that Candidate; it restores the known failures and therefore cannot create PASS. If the new Build Once or exact-digest continuation fails, do not mutate or promote it: retain the bounded evidence and apply the existing failed-Product disposition. Never retag or overwrite `68c15e6b...` or its index. CI service cleanup remains GitHub-owned; local Compose proof cleanup remains exact-task-owned and must not prune shared images, caches or unrelated resources.

Residual risks are bounded and explicit:

- a future Compose version may change row schema again; the shared fail-closed parser and exact service predicates convert that drift to one diagnosable refusal instead of three inconsistent interpretations;
- GitHub-hosted service availability and external Registry pull remain external dependencies, but the exact digest removes Homebrew patch drift and the existing CI result exposes failure without weakening tests;
- the corrected Product still requires a new Build Once and exact-digest continuation before native authority; source-level CLI PASS is not image proof;
- the human interactive deletion path still has provider/MFA latency; the internal age-60 abort and continuous attendance reduce risk but do not replace an Owner-approved deletion authority;
- exact provider-settled cost for the completed attempt remains unavailable and no cost-compliance claim is created.

The next gate is fixed: the coordinator either freezes `S6-TE-RP-V1.0` or returns a bounded plan correction. After freeze, a **different fresh `gpt-5.6-sol` / high Implementer** receives the exact allowed scope and verification ladder; a **separate fresh Reviewer** then evaluates its Candidate. No native rerun, Product build, Push, Registry action, deployment or phase advance may precede those gates. The only Owner decision that may be required before later paid execution is the deletion-timing authority/risk choice above; ordinary in-budget purchase/destruction authority remains unchanged and need not be reconfirmed.

## Technical Escalation Option A CI platform-convergence implementation addendum

Recorded: **2026-09-08 (Asia/Shanghai)**

Role: **Fresh Technical Implementer**

Status: **IMPLEMENTATION COMPLETED / AWAITING FRESH INDEPENDENT REVIEW / STAGE 6 REMAINS PARTIAL / HOLD**

The coordinator subsequently froze a narrower Technical Escalation plan after real CI run `34196433781` proved that source `f60f55589178490f2b987be0566a0ad9198ab391` moved the complete Quality job to Linux/x64 while its accepted proof executables still require Darwin/ARM64 behavior. The sole frozen plan is `PHASE_1B_STAGE6_CI_PLATFORM_CONVERGENCE_ANALYSIS_V1_0.md`, independently verified at SHA-256 `9fa6053f40f8ffe58fa0bbeceacd1930b3fbd5462d8550a71137275953c7de32`. The selected strategy is Option A; the earlier Linux/service strategy in this historical report is superseded for the current correction and remains immutable failure/analysis history.

### Exact source and Candidate

| Identity | Value |
| --- | --- |
| Required source | `f60f55589178490f2b987be0566a0ad9198ab391` |
| Required source tree | `a12c716e702f7a9e3a87b756e4f02e8036d1ac30` |
| Ordinary code Candidate | `d4e79982c5c8378c2c18f4d0e8a71bb1fed12350` |
| Candidate tree | `387ef5302e4a5a8d5cfe1595c843cd51cbbeef91` |
| Candidate parent | exact required source `f60f55589178490f2b987be0566a0ad9198ab391` |
| Working branch | `codex/stage6-ci-platform-convergence` |
| Accepted Product rollback authority | unchanged `a200838be34c8834a00bdcf6d1819da96e2ad26c` |

The isolated implementation worktree started clean at the old detached `9e8437ca...`, then created the branch directly from the exact required source. It did not branch from, cherry-pick, copy or replay rejected source `cec407314252cd47cef0d48d8581e5ce06cf7c25`.

The Candidate changes exactly three implementation paths:

| Path | Candidate SHA-256 | Disposition |
| --- | --- | --- |
| `.github/workflows/ci.yml` | `8e6201ec404d344bff1c856b034497a1c009df762c89585ba64badebc6e88a0c` | Restores `quality-postgres` to standard `macos-15`; deletes its Docker service; adds one exact official-source PostgreSQL 18.4 build, isolated loopback lifecycle, bounded diagnostics and unconditional task-owned cleanup. All other CI jobs remain unchanged. |
| `deploy/scripts/build-release-once.test.mjs` | `13e95252489602073203e8b12c4ff5c589257d980c398f9e2badd3b738c2a759` | Converges the existing CI contract test on the actual Quality slice, lifecycle ordering and focused negative controls. |
| `docs/CI_AND_GITHUB_GOVERNANCE.md` | `3e122cf0436f10e57bf6b4f1c528ada981f4a02f626be0fb836f377fd5e06e86` | Replaces stale current authority while preserving Phase D/ADR-0020 history and the separate Linux workload evidence. |

No `src/**`, runtime guard, package/lockfile, Migration, Dockerfile, Compose, Prompt/profile/classifier/checker, built-in-fetch, decoder or Browser-test byte changed. Explicit comparison of `src`, `package.json`, `pnpm-lock.yaml`, `scripts/check-runtime.mjs`, both architecture/public-bundle checkers, `drizzle`, root `Dockerfile` and `compose.yaml` against `f60f5558...` returned byte-identical.

### Root cause and corrected boundary

The causal defect was CI proof-platform and database-acquisition coupling, not Product logic. The Candidate restores the already governed Darwin/ARM64 proof environment and replaces only the unreliable PostgreSQL acquisition. Quality now downloads the exact official `postgresql-18.4.tar.bz2` over bounded HTTPS/TLS, verifies the literal SHA-256 `81a81ec695fb0c7901407defaa1d2f7973617154cf27ba74e3a7ab8e64436094` before extraction, configures the frozen minimal feature set, installs below one task root, and requires exact binary and live-server identity before Migration.

One new UTF8 / locale C cluster uses only synthetic `cwt_ci` identity and `127.0.0.1:55432`. Failure diagnostics expose at most the last 32 KiB / 200 lines of its synthetic server log. `if: always()` cleanup first asks the task-local `pg_ctl` to stop the owned cluster, signals no process unless its PID and exact task-local postmaster command are verified, fails if an owned server remains live, and removes only the validated task root. There is no Homebrew, rolling package, mirror, fallback patch, service container, permanent cache, helper service or second database authority.

Complexity increased only by the explicit ephemeral source/build/start/cleanup lifecycle required to retain the accepted proof environment. The former Quality service path was deleted rather than layered. No persistent state, table, Worker, queue, Lease, Recovery type, new parser/framework or dual authority was added. The test change reuses the existing workflow-slice contract and a small local assertion helper; its negative cases reject the old runner/service acquisition and verify ordering and substantive lifecycle failures.

### Verification on the exact Candidate

| Gate | Result |
| --- | --- |
| Exact host/runtime | PASS — Darwin ARM64; Node `24.14.0`; pnpm `11.9.0` |
| Frozen install | PASS — `pnpm install --frozen-lockfile` |
| AI Prompt authority | PASS — bundle verification plus 24 Node tests |
| Exact append-only history | PASS — `f60f5558...` to `d4e79982...` |
| Phase D synthetic boundary | PASS — 5 files, 67/67 tests |
| Lint / Typecheck | PASS / PASS |
| Focused CI contract | PASS; wrong runner, old service, source/hash/order/version/bind/identity/Migration/full-suite/AI/cleanup/duplicate-authority mutations all refused |
| Workflow/static | PASS — YAML parse; `git diff --check`; one source URL, startup, Migration command, unfiltered full-suite command and unconditional cleanup in the Quality slice |
| PostgreSQL acquisition/build | PASS — official source URL, embedded digest exact match before extraction, frozen configure flags, task-local compile/install, binary `postgres (PostgreSQL) 18.4` |
| Live database | PASS — one loopback server, `server_version=18.4`, `server_encoding=UTF8`, synthetic connection, clean fresh Migration including `0020`/`0021` |
| Unfiltered PostgreSQL-enabled full suite | PASS — 175 test files passed / 1 existing skip; 1337 tests passed / 10 existing skips; duration `590.38s` |
| Positive cleanup | PASS — server stopped; task-owned source/build/install/data/log root removed |
| Failure cleanup probe | PASS — a deliberate non-zero substantive result remained status `1`; partial task root was removed rather than converted to success |
| Final residue | PASS — no listener on `55432`, task-owned PostgreSQL process or task path remained |
| Final code tree | clean at exact Candidate before this evidence-only addendum |

The full Browser, public-bundle/Product and Compose suites were not repeated solely for this CI-only delta. Their applicable real `f60f5558...` run evidence already passed in run `34196433781`: Dependency security, Build + public bundle, and Browser completed successfully; only Quality failed at the former x64 runtime guard before substantive Quality gates. The local PASS above is supporting evidence and does not replace a real hosted `macos-15` run.

### External boundary and next gate

No Push, workflow run/rerun, Runner credential, Registry/cloud/native operation, Build Once, publication, deployment or phase acceptance occurred. The earlier Build Once authority was not consumed and is not transferable to this corrected source. A future Release would require a new exact-source authorization after hosted CI acceptance.

The next gate is fresh independent Review of immutable code Candidate `d4e79982c5c8378c2c18f4d0e8a71bb1fed12350`. Only after Review may the coordinator present a new concrete normal-Push/hosted-CI authorization. Stage 6 remains Partial / HOLD; this implementation does not self-approve and does not authorize any external action.

## Fresh independent Review — Technical Escalation Option A CI platform convergence

Recorded: **2026-09-08 (Asia/Shanghai)**

Role: **Fresh Independent Reviewer**

Verdict: **FAIL / BOUNDED REMEDIATION REQUIRED / STAGE 6 REMAINS PARTIAL / HOLD**

Reviewed immutable code Candidate `d4e79982c5c8378c2c18f4d0e8a71bb1fed12350`, tree `387ef5302e4a5a8d5cfe1595c843cd51cbbeef91`, direct parent `f60f55589178490f2b987be0566a0ad9198ab391`. Evidence-only successor `3d3690aa5c819f89ba4936774e892e0fc0f0c862` is a two-commit report-only descendant through `c7fd49db0a987c6a92cbf9103606643aac5413e3`; the report SHA-256 is `4d98dd3990a1480333dc6d23dfc59751ba01ce84b3a6d0346dde165a8b400f20`. The frozen Option A analysis SHA-256 is `9fa6053f40f8ffe58fa0bbeceacd1930b3fbd5462d8550a71137275953c7de32`.

### Security & Test Simplification Check

**PASS.** The Candidate deletes the former Quality service authority, uses the standard `macos-15` runner and one official-source PostgreSQL lifecycle, and adds no framework, mirror, cache, helper service, persistent state or duplicate proof authority. `pg_ctl` remains the standard first stop path; the bounded PID/command fallback is justified by unconditional fail-closed cleanup and does not convert a non-zero stop into success. The existing workflow-slice contract test is extended rather than replaced by a new harness.

### Findings

- **[MEDIUM] Governance creates an unauthorized second permission turn.** `docs/CI_AND_GITHUB_GOVERNANCE.md:95` says any later Build Once/publication requires a *separate* exact-source authorization *after* CI acceptance; implementation-report lines 900–902 repeat that sequencing. Current explicit coordinator authority has instead frozen one future concrete proposal covering normal Push/real CI and conditional Build Once/private publication after Review. The Candidate text therefore changes authorization sequencing beyond this CI repair and can unnecessarily block the operator after hosted-CI PASS. Remediate only `docs/CI_AND_GITHUB_GOVERNANCE.md`: retain exact-source authorization, hosted-CI PASS, no identity reuse and Stage 7 HOLD, but remove the requirement that authorization itself occur in a second post-CI permission turn. State that a previously explicit exact-source conditional authorization may become executable only after the hosted-CI and independent-evidence conditions pass. No workflow, Product, test or new approval mechanism is justified.
- **[LOW] The final cleanup diagnostic overstates ownership after an identity mismatch.** `.github/workflows/ci.yml:238-245` first correctly refuses to signal a non-matching live PID, then calls the same PID a “Task-owned PostgreSQL postmaster.” The behavior is safe—signal refused, root preserved, status non-zero—but the second message can confuse incident evidence. This does not block the current remediation. At the next workflow edit, use neutral wording such as “PID recorded by the task cluster remains live” and update the existing string assertion if needed; do not add a new test framework.

Severity count: **Blocker 0 / High 0 / Medium 1 / Low 1**. The Medium is deterministic governance/operational impact and therefore blocks Push under the current Review boundary until the one-file correction receives focused re-Review.

### Verification and disposition

- The Candidate changes exactly `.github/workflows/ci.yml`, `deploy/scripts/build-release-once.test.mjs`, and `docs/CI_AND_GITHUB_GOVERNANCE.md`; protected Product, package/lockfile, Migration, runtime-guard, checker, Dockerfile and Compose paths are byte-identical to `f60f5558...`. The rejected `cec40731...` Linux-only correction is not in the Candidate lineage or current Quality implementation.
- Candidate path SHA-256 values independently match the implementation record: workflow `8e6201ec404d344bff1c856b034497a1c009df762c89585ba64badebc6e88a0c`, contract test `13e95252489602073203e8b12c4ff5c589257d980c398f9e2badd3b738c2a759`, governance `3e122cf0436f10e57bf6b4f1c528ada981f4a02f626be0fb836f377fd5e06e86`.
- `git diff --check` passed; every Quality `run` block passed Bash syntax validation; the full focused file `node --test deploy/scripts/build-release-once.test.mjs` passed **9/9** on Node `24.14.0` / pnpm `11.9.0`.
- The exact cleanup script extracted from the Candidate workflow passed three controlled behavior probes: ordinary cleanup returned `0` and removed the root; forced `pg_ctl` failure terminated only a command-identical task-owned PID, removed the root and preserved status `1`; a non-owned live PID was not signalled, the root was preserved and status remained `1`.
- The Implementer’s exact-Candidate full evidence—PostgreSQL 18.4 source/hash/build/live checks, fresh Migration through `0021`, **1337 passed / 10 existing skipped**, and residue-free cleanup—was audited but not redundantly rerun. No hosted CI, Build Once, Registry, cloud/native, Production or acceptance claim is created by this Review.

Hosted `macos-15` source acquisition, compiler/image compatibility, runner capacity and the exact real-CI result remain **External Validation**. Formal Build Once count and cost budget remain unconsumed. After the one-file governance correction and focused independent re-Review, the coordinator may present the already frozen single exact-source authorization proposal; this Review does not Push, run CI, build, publish or accept Stage 6.

## Bounded governance-remediation erratum

Recorded: **2026-09-08 (Asia/Shanghai)**

Status: **DOCUMENTATION CORRECTION COMPLETED / AWAITING SAME INDEPENDENT REVIEWER / STAGE 6 REMAINS PARTIAL / HOLD**

The one-file correction is exact source commit `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`, whose parent is independent-Review evidence commit `40fe0dbd678fc172735d013a1dda9b465fde61d4`. It changes only `docs/CI_AND_GITHUB_GOVERNANCE.md`, now SHA-256 `1b4afc9da1cd7b242e0e5ebe90543aa78a7a85e87a271ab680f19e8ba6e84d81`.

The earlier implementation-addendum statements requiring another exact-source authorization only after hosted-CI acceptance are preserved above as historical evidence but are superseded by the corrected governance. After independent Review, the Owner may explicitly authorize one concrete operation bound to the exact corrected source and scope, covering normal non-force Push, real CI, and one conditional Build Once/private GHCR publication. That conditional build/publication may execute only after all applicable hosted-CI and independent-evidence conditions pass. An explicit grant that already covers that exact source and scope needs no second permission turn; it is not blanket future Release authority and cannot expand to another source or scope without authorization. The corrected source must create a new Release identity, and no earlier tag, OCI index, child digest or evidence artifact may be substituted or reused. Local proof remains supporting evidence only, hosted proof remains mandatory, and Stage 7 remains HOLD.

No formal Build Once/private GHCR publication for this corrected-source line has run; its count and cost budget remain unconsumed. The future authorization remains exact-source-bound, and the coordinator will present one complete corrected-source proposal only after this correction passes focused re-Review.

The Review Low concerning the final cleanup diagnostic is recorded for the next otherwise-required workflow edit. It is non-blocking: current behavior safely refuses the unmatched live PID, preserves the root and returns status `1`. This documentation-only correction does not change workflow or test bytes and does not expand into that Low.

Verification was limited proportionally to documentation diff and semantic consistency. `.github/workflows/ci.yml` and `deploy/scripts/build-release-once.test.mjs` retain their reviewed `d4e79982...` SHA-256 values `8e6201ec404d344bff1c856b034497a1c009df762c89585ba64badebc6e88a0c` and `13e95252489602073203e8b12c4ff5c589257d980c398f9e2badd3b738c2a759`; all executable and protected paths remain byte-identical to `d4e79982c5c8378c2c18f4d0e8a71bb1fed12350`. The previously passed full suite, PostgreSQL build, Browser, Product and cleanup probes were not rerun. No Push, CI, Runner, Build, Registry, cloud/native action or phase acceptance occurred.

The next gate is focused re-Review by the same independent Reviewer of exact correction commit `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`; this Implementer does not self-approve.

## Focused independent re-Review — governance remediation

Recorded: **2026-09-08 (Asia/Shanghai)**

Role: **Same Fresh Independent Reviewer**

Verdict: **PASS / PRIOR MEDIUM CLOSED / LOW RETAINED / EXTERNAL VALIDATION REQUIRED**

The reviewed source is exact correction Candidate `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`, direct parent `40fe0dbd678fc172735d013a1dda9b465fde61d4`. Evidence-only successor `6e2cb85832861bab3d7b79f97dfad66c2c52cd6f` is its direct report-only child. The Candidate changes only two lines in `docs/CI_AND_GITHUB_GOVERNANCE.md`; its SHA-256 is `1b4afc9da1cd7b242e0e5ebe90543aa78a7a85e87a271ab680f19e8ba6e84d81`, and the pre-Review evidence report SHA-256 is `2cd27970930810c8211db065eaf7c90d8cbaff8bddea18571cde7b0d2c79ccc8`.

The prior **Medium is CLOSED**. Item 2 and the final Stage 6 paragraph now permit one explicit reviewed exact-source/scope authorization covering normal non-force Push, real CI, and one conditional Build Once/private GHCR publication. Build/publication becomes executable only after hosted-CI and independent-evidence conditions pass; the text requires no second permission turn for the already granted same source/scope, creates no blanket future-source authority, forbids substitution or reuse of older Product/OCI/evidence identities, preserves local proof as supporting evidence only, and keeps Stage 7 HOLD. The historical erroneous report wording is explicitly preserved as history and superseded by the erratum.

Security & Test Simplification remains **PASS**. The correction deletes the unnecessary sequencing constraint and adds no gate, state, framework, test mechanism or executable path. `.github/workflows/ci.yml`, `deploy/scripts/build-release-once.test.mjs`, all Product/runtime/protected paths and the earlier executable Review findings are unchanged. Per the bounded documentation-only assignment, no test, PostgreSQL build, Browser, Product or cleanup probe was rerun; identity, diff, hash, protected-path non-regression and semantic consistency checks passed.

Severity after remediation: **Blocker 0 / High 0 / Medium 0 / Low 1**. The recorded cleanup-message wording Low remains non-blocking and deferred to the next otherwise-needed workflow edit. Hosted `macos-15` acquisition/compiler/image/capacity and the exact real-CI result remain **External Validation**. No Push, CI run/rerun, Build Once, Registry, cloud/native, publication or acceptance occurred; formal Build Once count and cost budget remain unconsumed. The coordinator may now present the already frozen single exact-source authorization proposal for `c9cac161...`; Stage 6 remains Partial / HOLD pending its external gates and coordinator disposition.

## Authorized corrected-source Push and real CI outcome

Recorded: **2026-09-08 (Asia/Shanghai)**

Disposition: **BLOCKED AT REAL CI / CONDITIONAL BUILD ON HOLD / STAGE 6 REMAINS PARTIAL**

The Owner-authorized corrected-source operation began with a fresh volatile precheck. Remote `main` was exact `f60f55589178490f2b987be0566a0ad9198ab391`; authorized source `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`, remained its ordinary five-commit descendant. There were zero active repository runs, zero registered repository Runners, zero existing release-workflow runs for `c9cac161...`, and zero private GHCR package versions carrying the corrected-source tag. The release workflow remained active and byte-unchanged across the source range. The selected existing Mac had exact Node `24.14.0`, Docker Server `29.6.2`, Buildx `v0.35.0-desktop.2`, and zero running containers.

One ordinary non-force Push advanced remote `main` from `f60f5558...` to exact authorized source `c9cac161...`. The automatic push-triggered [CWT quality gates run `34205904510`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34205904510), attempt `1`, ran from `2026-09-08T08:41:52Z` through `08:59:13Z` at exact head `c9cac161...` and completed `failure`. Its job outcomes were:

| Job | Result |
| --- | --- |
| Dependency security, job `101995199049` | **SUCCESS**, `7s` |
| Build + public bundle, job `101995199647` | **SUCCESS**, `1m10s` |
| Browser, job `101995198835` | **SUCCESS**, `6m15s`, with the no-retry acceptance step successful |
| Quality + PostgreSQL + AI proof, job `101995198688` | **FAILURE**, `17m12s` |

Within Quality, exact runtime identity, dependency installation, AI Prompt authority and append-only history, Phase D synthetic boundary, Lint and Typecheck all passed. The job downloaded the sole official PostgreSQL 18.4 archive from `https://ftp.postgresql.org/pub/source/v18.4/postgresql-18.4.tar.bz2`, verified literal SHA-256 `81a81ec695fb0c7901407defaa1d2f7973617154cf27ba74e3a7ab8e64436094` (`postgresql-18.4.tar.bz2: OK`), built and started the isolated loopback server, and applied the fresh Migration through `0021`; each of those workflow steps completed `success`.

The only failed step was `Run the full test suite with PostgreSQL suites enabled`. Vitest completed with **174 test files passed / 1 skipped (`176` total reported), 1330 tests passed / 10 skipped (`1347` total reported), one unhandled error, duration `841.18s`**. The terminal error was `SyntaxError: Unexpected token '�', "�" is not valid JSON` while `convert-source-map@2.0.0` and `@vitest/utils` were extracting a source map. No business-test assertion failure was reported, but the unhandled error correctly made the command and run fail with exit code `1`; this evidence therefore cannot be accepted as hosted-CI PASS. The bounded failure-diagnostics step completed `success`, and unconditional `Stop and remove task-owned PostgreSQL` also completed `success` immediately afterward.

Final read-only checks found remote `main` still exact `c9cac161...`, zero active repository runs, zero registered repository Runners, zero release-workflow runs at this head, zero matching GHCR versions, zero running local Docker containers, and no reserved Build Once Runner root. No registration token, Runner, Build Once dispatch, Environment approval, Product build, Registry write, publication, cloud/native operation, deployment or acceptance was created. The already authorized conditional Build Once remains non-executable because its real-CI condition did not pass. The next gate is coordinator confirmation of this failed external evidence and a reviewed causal remediation decision; this Operator stops at the CI failure boundary and grants no retry or expanded action.

## Causal verification and operational-readiness correction for run `34205904510`

Recorded: **2026-09-08 (Asia/Shanghai)**

Status: **CAUSAL INPUT DIAGNOSIS COMPLETE / REPORT-ONLY / AWAITING INDEPENDENT REVIEW AND OWNER AUTHORIZATION / STAGE 6 REMAINS PARTIAL / HOLD**

This follow-up corrects report transcription only; the actual controlled reproduction used the correct existing tag and is unchanged.

The preceding terminal-only diagnosis is incomplete historical evidence. The earliest decisive Quality failure occurred at `2026-09-08T08:57:13.1759250Z`, when `scripts/verify-ai-architecture.ts` failed closed because accepted annotated tag `refs/tags/phase-1b-stage4-approved-2026-08-28` was unavailable. Vitest later emitted the U+FFFD `convert-source-map` JSON error at `08:59:04Z` while rendering the child-process failure. The suite therefore produced no completed result for the seven-test diagnostic file; the U+FFFD was not evidence of a separate Product assertion failure.

A disposable clone of the remote-visible state was created without local tags, checked out at unchanged source `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, and given only the two tags actually advertised by `origin`. All fixed commit, tree, parent and ancestry constants in the unchanged checker were already available and matched. The required Stage 4 tag alone was absent from `git ls-remote --tags origin`.

| Controlled state at the same source | Actual result |
| --- | --- |
| Remote-visible tags only | Direct architecture checker failed status `1` with the same missing-accepted-tag message. The actual seven-test suite failed status `1`, reported `Tests (7)` plus one unhandled error and no completed test result, and reproduced the U+FFFD source-map rendering error. |
| Existing exact local tag transferred as the sole added Git ref | Tag object was exactly `d5a8828197282f2d5d2929bf59bd5589949b650b`, peeling to accepted commit `f05852dbd3c5cff80421793a4ea345e401d50361`. The direct checker passed with `ok: true`, head `c9cac161...`, `candidateCount: 944`, `executableCount: 629`, `acceptanceRootCount: 5`, and `acceptanceClosureCount: 35`. The same actual suite then passed **1/1 file and 7/7 tests** in `52.97s`; the U+FFFD error disappeared. |

This unchanged-source A/B establishes the missing accepted tag as the causal input and the U+FFFD as secondary failure rendering. The checker has one named-ref dependency—the Stage 4 tag above; its other Git dependencies are immutable commit/tree/parent/ancestry reads, all proven present in the remote-only clone. No serial hidden-ref gap, code/workflow/test/checker defect, or Product defect is evidenced, so Technical Escalation Option A remains unchanged. No executable or protected path was modified for this diagnosis.

The exact operational proposal, not executed by this report-only task, is:

1. After independent Review and explicit Owner authorization, revalidate remote `main` at exact `c9cac161...`, remote absence of the required ref, and the local tag object's exact identity and peel. Publish only the existing tag object/ref with the non-force refspec `git push origin refs/tags/phase-1b-stage4-approved-2026-08-28:refs/tags/phase-1b-stage4-approved-2026-08-28`; do not recreate the tag, use a tag glob, force, or push source. Then verify the remote object `d5a8828197282f2d5d2929bf59bd5589949b650b`, peel `f05852dbd3c5cff80421793a4ea345e401d50361`, and unchanged unrelated tags. The tag annotation itself grants no external authority.
2. Tag publication alone does not match the branch-only push trigger in `.github/workflows/ci.yml`; the release and runtime-validation workflows remain `workflow_dispatch` only. After the tag postcheck, request exactly one failed-job rerun with `gh run rerun 34205904510 --failed --repo czy282923753/cwt-cloudwave-textile`. This retains run ID `34205904510` and exact source `c9cac161...`, creates attempt `2`, reruns failed Quality and any required dependency only, and preserves the three successful attempt-1 jobs where GitHub tooling permits.
3. The resulting evidence record must identify the new attempt-2 Quality job ID and exact step outcomes, test totals, cleanup and final run conclusion, while retaining attempt-1 successful job IDs `101995199049`, `101995199647`, and `101995198835` at the same source. Any unexpected ref identity, broader rerun, new failure or ambiguous stale/skip state stops the operation. Only a final hosted-CI PASS plus required independent evidence may unlock the already scoped conditional Build Once; Build count and cost remain unconsumed.

No tag publication, workflow rerun, Push, Runner, Build Once, Registry write, cloud/native operation, deployment or acceptance occurred in this causal-verification task. The next gate is independent Review of this report-only evidence followed by one complete exact-source authorization proposal from the coordinator.

## Independent Review — missing Stage 4 tag input and failed-job rerun readiness

Recorded: **2026-09-08 (Asia/Shanghai)**

Role: **Same Independent Reviewer**

Verdict: **CONDITIONAL PASS / NO BLOCKING FINDING / NEEDS OWNER DECISION / HOSTED CI PENDING**

The reviewed Product source remains exact `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`; report-only evidence HEAD `48265b0a937eb5619b9b9f5fca321489db8b5e8f` changes only this existing report and preserves the earlier failed-run and corrected-transcription history. Run `34205904510`, attempt `1`, is still completed `failure` at exact `c9cac161...`. Its three independent jobs remain successful under job IDs `101995199049`, `101995199647` and `101995198835`; Quality job `101995198688` alone failed, after all pre-suite gates and before successful unconditional cleanup.

The causal conclusion is **PASS**. Actual task records independently confirm that a remote-visible clone at unchanged `c9cac161...` failed the direct checker with the exact missing-tag message and caused the real seven-test file to report seven tests with no completed result plus the later U+FFFD source-map rendering error. Transferring only existing annotated tag object `d5a8828197282f2d5d2929bf59bd5589949b650b`, correctly peeled to `f05852dbd3c5cff80421793a4ea345e401d50361`, made the unchanged direct checker pass and the same file pass **7/7** in `52.97s`; the U+FFFD disappeared. The unchanged checker has exactly this one named-ref dependency on the relevant path, while its fixed commit/tree/parent/ancestry objects were present and matched. The U+FFFD is therefore secondary failure rendering, not proof that the seven tests passed in attempt 1 and not evidence of a separate Product defect.

The proposed operation is proportionate and complete. Remote `main` is still exact `c9cac161...`; the required Stage 4 ref remains absent, while the local tag has the exact object/peel above and the two existing remote annotated tags remain unchanged. Publishing only the existing full source/destination tag refspec, non-force and without recreation, glob, branch or unrelated-ref Push, is the smallest correction. The Quality checkout already uses `fetch-depth: 0`, which the pinned checkout contract defines as all history and tags. The CI workflow filters `push` to branch `main`, so a tag-only Push does not match it; Release and Runtime workflows remain `workflow_dispatch` only.

Current GitHub and `gh 2.97.0` semantics confirm that `gh run rerun 34205904510 --failed --repo czy282923753/cwt-cloudwave-textile` reruns failed jobs plus required dependencies, keeps the original `GITHUB_SHA`/`GITHUB_REF`, and preserves attempt-scoped evidence. Quality has no job dependency in the current workflow, so only a new attempt-2 Quality job is expected; the three successful attempt-1 jobs remain separately auditable. This is a failed-input retry of the same source, not a source correction, all-job rerun or failed-gate suppression.

Execution is conditioned on one explicit Owner authorization covering the additional exact tag publication and exactly one failed-job rerun. Immediately before mutation, recheck remote `main=c9cac161...`, target ref absent, exact local object/peel, unchanged existing remote tags, run `34205904510` still completed `failure` at attempt `1`/head `c9cac161...`, and no conflicting active run. Publish the exact tag ref once and verify remote object/peel, unchanged `main` and unrelated tags. Then recheck that no unexpected workflow was triggered and invoke the exact `--failed` command once. If either mutation returns an ambiguous result, perform read-only state inspection and **do not repeat it**. Any pre-existing/new attempt, unexpected job set, ref drift or broader rerun stops the operation.

Attempt-2 evidence must record its new Quality job ID, exact substantive steps, complete suite totals, unconditional cleanup and final run conclusion while retaining attempt-1 identities. Only an independently confirmed hosted-CI PASS may satisfy the existing Build condition; the already approved exact-`c9` conditional Build Once/private-publication authority then applies without another permission turn. No Build, Runner, Registry or downstream action is implied by the retry itself.

Security & Test Simplification Check: **PASS**. The proposal uses one existing accepted tag, one exact standard Git ref publication and GitHub's native failed-job rerun; it introduces no source change, duplicate approval layer, dependency, harness, cache, fallback, retry loop or new authority. Severity remains **Blocker 0 / High 0 / Medium 0 / Low 1**; the accepted cleanup-message wording Low remains non-blocking and deferred. The tag publication/rerun and hosted attempt-2 outcome remain external and unexecuted; Build count, cost budget and 120-minute/30-day/owned-cleanup bounds remain unconsumed and unchanged. Stage 6 remains Partial / HOLD, with no native/Tencent/deploy or phase-acceptance authority.

## Authorized Stage 4 tag publication and failed-job attempt-2 outcome

Recorded: **2026-09-08 (Asia/Shanghai)**

Disposition: **COMPLETED / HOSTED CI PASS / AWAITING COORDINATOR EVIDENCE CONFIRMATION**

The Owner-authorized volatile precheck passed with remote `main` exact `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`; run `34205904510` exact head `c9cac161...`, attempt `1`, `completed/failure`; target Stage 4 tag absent remotely; and local existing annotated tag object `d5a8828197282f2d5d2929bf59bd5589949b650b` of type `tag`, peeled to exact `f05852dbd3c5cff80421793a4ea345e401d50361`. The two unrelated remote annotated tags, zero active runs, zero repository Runners and zero release runs for `c9cac161...` were also confirmed.

One exact non-force Push published only `refs/tags/phase-1b-stage4-approved-2026-08-28:refs/tags/phase-1b-stage4-approved-2026-08-28`. Post-Push reads returned the exact tag object and peel above, unchanged `main`, and all three expected remote tag names with both unrelated tag identities unchanged. The tag-only Push triggered no workflow run. No tag was recreated, forced or broadened through a glob, `--follow-tags`, branch Push or unrelated ref update.

At `2026-09-08T09:35:50Z`, the Operator invoked exactly once `gh run rerun 34205904510 --failed --repo czy282923753/cwt-cloudwave-textile`; the request returned exit `0` at `09:35:52Z`. GitHub created attempt `2` at the same exact source. Only new Quality job `102011317207` executed, from `09:36:00Z` through `09:54:42Z`; the attempt-2 API view also emitted copied snapshots of the three earlier successful jobs with their original attempt-1 timestamps, but they were not re-executed. Their original attempt-1 identities remain independently retrievable and unchanged: Dependency security `101995199049`, Build + public bundle `101995199647`, and zero-retry Browser `101995198835`.

Quality attempt `2` completed **success** in `18m42s`. Exact checkout, Node/pnpm runtime identity, frozen dependencies, AI Prompt authority and append-only history, Phase D synthetic boundary, Lint and Typecheck all passed. The official PostgreSQL 18.4 archive again verified literal SHA-256 `81a81ec695fb0c7901407defaa1d2f7973617154cf27ba74e3a7ab8e64436094`; source build, isolated loopback start and fresh Migration through `0021` each completed `success`. The previously unreported `src/ai/phase-f-m6-one-case-diagnostic.integration.test.ts` completed **7/7 PASS**. The complete PostgreSQL-enabled suite completed **175 test files passed / 1 skipped (176 total), 1337 tests passed / 10 skipped (1347 total), duration `850.74s`**, with no unhandled error. Failure diagnostics were correctly skipped, and unconditional `Stop and remove task-owned PostgreSQL` completed `success`; `pg_ctl` reported `waiting for server to shut down.... done` and `server stopped`.

[Run `34205904510`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34205904510) now records `run_attempt=2`, exact head `c9cac161...`, `completed/success`, updated `2026-09-08T09:54:43Z`. Final read-only checks reconfirmed exact remote `main`, exact Stage 4 tag object/peel, zero active runs, zero repository Runners, zero c9 release-workflow runs, zero matching private GHCR versions, zero running local Docker containers and no reserved Build Once Runner root. No token, Runner, Environment approval, Build Once dispatch, Product build, Registry write, cloud/native operation, deployment or phase acceptance occurred. The already authorized conditional Build Once remains unconsumed and may proceed only after independent coordinator confirmation of this hosted-CI evidence; no additional Owner permission turn is required for that frozen exact-source scope.

## Authorized exact-c9 Build Once and private GHCR publication outcome

Recorded: **2026-09-08 (Asia/Shanghai)**

Disposition: **COMPLETED / PRODUCT PUBLISHED PRIVATELY / AWAITING INDEPENDENT COORDINATOR AND PRODUCT-EVIDENCE REVIEW**

After independent coordinator confirmation that hosted CI run `34205904510` attempt `2` satisfied the existing conditional gate, one volatile precheck reconfirmed remote `main` and release/dispatch input exact `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`; zero active runs, repository Runners, prior c9 release runs and c9 package-tag collisions; active workflow ID `348403423`; private package ID `14809040`; and Environment `cwt-stage6-build-once` ID `21083844262` with required Owner reviewer, self-review permitted and its branch policy. The selected Mac provided exact Node `24.14.0` at its retained versioned path, pnpm `11.9.0`, Docker `29.6.2`, Buildx `v0.35.0-desktop.2`, Docker Scout, both target platforms, zero running containers and more than 100 GiB free. The existing 11 stopped containers and 30 shared image identities were inventoried for preservation.

The task downloaded only official macOS ARM64 Actions Runner `2.337.0`; its archive was exactly `127732571` bytes with SHA-256 `5a2cd92908a93d7276a194e1de6008099f3e7946f3f8e14aa7a1a7b4a31fdec2`. One ephemeral Runner registered from one short-lived token as ID `37`, name `cwt-build-once-mac-46e09f053dbdca4c52b56bcd845b0279`, with exact labels `self-hosted`, `macOS`, `ARM64`, `cwt-trusted-build-once`, then appeared online and idle. The token remained only in the single operator process through the required direct official `config.sh --token` child boundary; it entered no file or retained output.

Exactly one [Build Once run `34213361695`](https://github.com/czy282923753/cwt-cloudwave-textile/actions/runs/34213361695) was dispatched at `2026-09-08T10:03:09Z` from `main` with `release_commit=c9cac161...`. It records event `workflow_dispatch`, attempt `1`, exact head `c9cac161...`, job `102019307661`, Runner ID `37` and the exact four labels. The sole authorized Environment approval created deployment `6325208137`. The job ran from `10:03:32Z` through `10:20:23Z` and completed `success` in `16m51s`, within the existing 120-minute cap. Every normal step passed: exact checkout and first-attempt/source refusal, pnpm/Node/ORAS installation, accepted tool boundary, frozen dependencies, the sole dual-platform Build Once, credential-argument-free GHCR authentication, exact OCI publication and detached evidence upload. Failure-only revocation upload correctly skipped. GitHub emitted one non-blocking maintenance annotation that the pinned upload-artifact action still targets Node 20 while this Runner forces it to Node 24; artifact upload and the job remained successful.

The immutable Release record is `built`, release/source `c9cac161...`, tree `918f5277...`, epoch `1788856206`, source archive SHA-256 `975422183f05dc9eb2e2c6f874f078caca255d77bd68e844007c2e5013915995`, and release-record SHA-256 `e25a3aa363ba431c3b21cfa21361d6e834f752725550528901269044530643ae`. Its exact Product identities are:

| Platform | Manifest digest | Config digest |
| --- | --- | --- |
| OCI index | `sha256:4a1348641eb92995c8efe111e65818532489f5e1152476d33672537842b70c74` | — |
| `linux/amd64` | `sha256:3578b5e70447ee58552b58be306f3992724aedab46d26755f2ab8a9db4ad17c6` | `sha256:e439f6ac50583da8e7dd09a056fda9c614b7e9a53953cc07283790919bdf83d5` |
| `linux/arm64` | `sha256:eab5eb76bbd427e452fdf0ead7948a8f5d93991973b2f53b4f1555276a11bcf4` | `sha256:456d15f248460a3007fe527e2e5bd70b45978ca566183eecd6307b371e88966e` |

Both emitted children passed the exact-image public-bundle authority under the no-network, read-only, UID/GID `10001:10001`, capability-drop and no-new-privileges envelope, each confirming 400 eligible server runtime JavaScript files and 20 public page manifests. Both 518-package SBOM scans passed pinned runtime packages, runtime package-manager absence, executable Sharp `0.35.3` decode smoke, zero business-secret matches and zero framework value/hash evidence leakage. Both provenance records bind the exact source/tree/archive with post-acquisition network disabled, no-cache builds, detached SBOM/provenance and timestamp rewriting. The release policy remains private, immutable/no-overwrite, no-early-deletion and total-loss-requires-new-release.

Private GHCR publication returned `PASS`, exact digest reference `ghcr.io/czy282923753/cwt-cloudwave-textile@sha256:4a1348641eb92995c8efe111e65818532489f5e1152476d33672537842b70c74`, and `mutableTagIsAuthority=false`. Package version `1222645701` is that exact index and carries the sole convenience tag `c9cac161...`; untagged child versions `1222645231` and `1222645632` match the exact amd64 and arm64 manifests. Package visibility remained `private`; an independent no-credential descriptor request returned HTTP `401` with the expected GHCR Bearer challenge. Version count rose only from 12 to 15, preserving all historical Products.

Detached artifact `10051334042`, `cwt-release-evidence-c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, retained exactly `release.json` plus seven evidence files, size `9090474` bytes. The workflow-recorded artifact ZIP SHA-256 is `cecd6e4fdac0a45f5e1c9cffc8e354338e49d0d18511b2b382d3ef2849cf2d3f`; GitHub records `expired=false` and expiry `2026-10-08T10:19:32Z`, satisfying the 30-day evidence scope. A downloaded read-only copy matched every SHA-256 descriptor recorded in `release.json`.

After success, the ephemeral Runner reported job success, removed `.credentials` and `.runner`, and exited without retry. The exact task-owned Runner root, official archive, registration material, `_work`, token/auth/ORAS/Release/OCI scratch and process were removed. Final checks found remote `main` unchanged at exact c9, zero active runs, zero repository Runners, zero running containers, the same 11 stopped containers and 30 shared images, and zero owned nonce/c9 residue. No shared cache, historical Product, unrelated container/image/worktree or user change was removed. The one authorized Build count is now consumed; no second dispatch, rerun, source/ref update, native/Tencent/cloud purchase, deployment, Stage 7 action or phase acceptance occurred. The next gate is independent coordinator and Product-evidence review before any native or phase continuation.

## Independent Review — exact-c9 Build Once and private publication evidence

Recorded: **2026-09-08 (Asia/Shanghai)**

Role: **Same Independent Reviewer**

Verdict: **PASS / NO BLOCKING FINDING / BUILT PRODUCT SUITABLE AS INPUT TO SEPARATELY AUTHORIZED NATIVE RUNTIME VALIDATION / STAGE 6 REMAINS PARTIAL / HOLD**

The reviewed Product source is exact `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`, tree `918f52775d323af1c4b411758a1a97f5355d404a`; report-only evidence commit `2f8b78c6f17a5c3b965bd497e892df1080486774` changes only this existing report. Independent Git reads reproduced the recorded commit epoch `1788856206` and source-archive SHA-256 `975422183f05dc9eb2e2c6f874f078caca255d77bd68e844007c2e5013915995`. Remote `main` remains exact c9. Hosted CI run `34205904510` is `completed/success` at the same source on attempt `2`, with new Quality job `102011317207`; the original successful attempt-1 jobs remain separately retrievable as `101995199049`, `101995199647` and `101995198835`. No source substitution or hidden second Product revision is evidenced.

Build/publication execution is **PASS**. GitHub records exactly one c9 release-workflow run, `34213361695`, event `workflow_dispatch`, attempt `1`, with one successful job `102019307661` on Runner ID `37` and deployment `6325208137` in `cwt-stage6-build-once`. The job ran `10:03:32Z–10:20:23Z`; exact checkout/source and first-attempt refusal, frozen tools/dependencies, the sole dual-platform Build Once, credential-argument-free GHCR authentication, exact-digest publication and detached artifact upload all passed. The job log independently records both exact-image public-bundle checks at 400 eligible server JavaScript files and 20 public page manifests, a `built` Release, both child/config identities, publication `PASS` with `mutableTagIsAuthority=false`, and successful finalization of artifact `10051334042`. No rerun or second c9 release run exists; the one authorized Build count is consumed.

Release and Product identity are **PASS**. A newly downloaded copy of artifact `10051334042` was exactly `9090474` bytes and matched ZIP SHA-256 `cecd6e4fdac0a45f5e1c9cffc8e354338e49d0d18511b2b382d3ef2849cf2d3f`. Its safe eight-file inventory is `release.json` plus six declared platform evidence files and one sanitized Buildx metadata file. `release.json` matched SHA-256 `e25a3aa363ba431c3b21cfa21361d6e834f752725550528901269044530643ae`; all six evidence descriptor hashes and platform subject digests recomputed exactly. The existing `check-release` authority returned `PASS` for release c9 and index `sha256:4a1348641eb92995c8efe111e65818532489f5e1152476d33672537842b70c74`. The downloaded provenance binds both child manifests/configs to exact source, tree, epoch and archive; both scans record 518 packages, pinned-runtime/package-manager/Sharp checks passing, zero business-secret matches, zero framework value/hash leakage matches and no claimed external vulnerability feed. The seventh sanitized Buildx metadata file is not a release evidence descriptor, but its bytes remain custody-bound by the exact artifact ZIP digest and it repeats the same c9/index identity; this is not an integrity gap.

Private Registry custody and no-overwrite behavior are **PASS**. The live GitHub package API reports package ID `14809040`, visibility `private`, and 15 versions. Index version `1222645701` is exact `sha256:4a134864...` with the sole c9 convenience tag; untagged versions `1222645231` and `1222645632` are exact amd64 `sha256:3578b5e7...` and arm64 `sha256:eab5eb76...` manifests. Operator precheck recorded 12 versions and zero c9 match before dispatch; the current inventory adds only those three identities and retains all 12 earlier versions, including the prior `68c15e6b...` Product. An unauthenticated exact-index descriptor request returns HTTP `401` with the expected GHCR Bearer challenge. Artifact metadata independently binds ID, name, source run/head, ZIP digest, unexpired state and `2026-10-08T10:19:32Z` expiry.

Runner, credential and cleanup boundaries are **PASS**. The Operator record shows the short-lived registration value crossing only the required official `config.sh --token` child-process argument boundary: it was held in a process variable, was not embedded as a literal in retained task input, was not printed by the registration output and was not written to a token file. This transient process-argument exposure is distinct from logged or persisted secret material and matches the explicitly accepted official Runner boundary. The Runner connected as the exact nonce-bound ID `37`, completed once, removed `.credentials` and `.runner`, deregistered and exited. The mandatory cleanup record reports exact c9 `main`, zero repository Runners, active runs, running containers, Runner/build processes and owned residue, while preserving 11 stopped containers and 30 shared images; current read-only checks reconfirm zero active runs, zero repository Runners, zero running containers and the same 11/30 counts. No cleanup evidence materially changes the previously accepted cleanup-message wording Low.

The built Product is therefore an acceptable **input**, not a result, for the separately governed native Runtime Validation. The reviewed Runtime workflow consumes explicit `release_commit=c9...`, `index_digest=sha256:4a134864...` and `evidence_run_id=34213361695`, downloads this named artifact, runs the same release-identity check before credentials, materializes read-only OCI evidence from that exact private digest, selects the recorded `linux/amd64` child and only then invokes the sole native validator. This Review does not provision a Runner, authorize Tencent/native/cloud spend, dispatch that workflow, execute the Product, grant deployment or Stage 7 authority, or establish Runtime/Provider/recovery/phase acceptance.

Security & Test Simplification Check: **PASS**. In delete → merge → standard mechanism → new mechanism order, the evidence uses one exact source, one existing standard workflow attempt, one immutable index with two declared children, one standard private package and one detached artifact; there is no redundant state to delete or merge and no new harness, approval layer, retry loop, fallback or authority to add. Severity is **Blocker 0 / High 0 / Medium 0 / Low 2**: the accepted cleanup-message wording Low remains deferred, and the pinned `actions/upload-artifact` Node 20 → forced Node 24 annotation is a maintenance Low because upload, digest finalization and the job all succeeded. Any future action-version maintenance must be handled as a separately reviewed source change; it does not justify rerunning this consumed Build or weakening the exact artifact evidence.

## Exact-c9 native Runtime readiness addendum

Recorded: **2026-09-08 (Asia/Shanghai)**

Disposition: **LOCAL / READ-ONLY PREPARATION AND FRESH TENCENT CONSOLE PREFLIGHT COMPLETE; EXTERNAL EXECUTION REMAINS SEPARATELY GATED**

The operation branch was advanced locally by fast-forward from report-only Build outcome `2f8b78c6f17a5c3b965bd497e892df1080486774` to independently accepted Product Review `22da4e21060d407a42e922f2a5bc6fef87d729ee`. This Review commit is the proposed workflow/tools identity and future dispatch head. It is nine ordinary commits after immutable Product `c9cac1618d49cb111d5aa6279d1e81ec300a81d1`; the complete diff across that range changes only this integration report. The Runtime workflow blob is unchanged at `02fcbb36b14ff2405f35351624399f704f060218`, 8,932 bytes, SHA-256 `a7a0fb31dd1045044df5833a5596d5d933e58284c1154f56065e2f288ec99d23`. Direct comparison also confirmed no change under `.github`, `deploy`, or package lock/config paths. The later local commit containing this addendum is evidence only and must not become the workflow commit or Push target.

| Frozen field | Exact future value |
| --- | --- |
| Future dispatch ref | `main`, only after it resolves exactly to the reviewed control commit below |
| `workflow_commit` | `22da4e21060d407a42e922f2a5bc6fef87d729ee` |
| `release_commit` / immutable Product | `c9cac1618d49cb111d5aa6279d1e81ec300a81d1` |
| Product source tree | `918f52775d323af1c4b411758a1a97f5355d404a` |
| `index_digest` | `sha256:4a1348641eb92995c8efe111e65818532489f5e1152476d33672537842b70c74` |
| Selected `linux/amd64` child | `sha256:3578b5e70447ee58552b58be306f3992724aedab46d26755f2ab8a9db4ad17c6` |
| `evidence_run_id` / retained artifact | `34213361695` / `10051334042` |
| Reserved fresh `runner_nonce` | `2933e1cdb583787664bdcf3c4a002f3d` |
| Runner name / unique job label | `cwt-tencent-sg-2933e1cdb583787664bdcf3c4a002f3d` / `cwt-job-2933e1cdb583787664bdcf3c4a002f3d` |

Current GitHub reads found remote `main` still at exact Product c9. The proposed control commit is its normal fast-forward descendant, is currently local-only, and has no matching Runtime run. The concrete future ref update is exactly `22da4e21060d407a42e922f2a5bc6fef87d729ee:refs/heads/main`, non-force and without another ref. That Push will automatically trigger the ordinary `CWT quality gates` push workflow on the report-only control commit; any result must remain visible and separately recorded. It does not trigger either Build Once or Runtime workflow, both of which remain manual. The consumed c9 Build count remains consumed and no additional Build is permitted or needed.

Runtime workflow ID `348403425` is active. Environment `cwt-stage6-runtime-validation`, ID `21083845147`, retains the required Owner reviewer, permits self-review, uses custom branch policies and allows only branch `main`; the proposed ref is therefore legal only after remote `main` rereads as exact `22da4e...`. Build Once run `34213361695` remains `completed/success`, attempt `1`, exact head c9. Artifact `10051334042` remains unexpired through `2026-10-08T10:19:32Z`, 9,090,474 bytes, with GitHub digest `sha256:cecd6e4fdac0a45f5e1c9cffc8e354338e49d0d18511b2b382d3ef2849cf2d3f`. Private GHCR index version `1222645701` and amd64 version `1222645231` still expose the exact frozen digests. Repository Runner, queued-run and in-progress-run inventories are zero.

The exact future manual dispatch is workflow ID `348403425` on ref `main` with the five frozen inputs above. Before dispatch, remote `main` and `workflow_commit` must both equal `22da4e...`; the unique Runner must be online and idle with cumulative labels `self-hosted`, `Linux`, `X64`, `cwt-tencent-singapore`, `cwt-single-use`, and `cwt-job-2933e1...`. The workflow itself refuses equal workflow/Product commits, binds the exact Runner before credentials, checks the detached Release header before GHCR authentication, materializes the immutable private index read-only, selects native `linux/amd64`, invokes the sole accepted validator once, and retains only the two sanitized Runtime-outcome files. Its configured job timeout remains 60 minutes.

The retained Tencent host boundary remains one disposable Singapore Zone 2 CVM: exact `SA5.MEDIUM4`, 2 vCPU / 4 GiB, public Ubuntu Server 24.04 LTS 64-bit image `img-mmytdhbn`, one 60 GiB general-purpose SSD, one ordinary BGP public IPv4 capped at 5 Mbps with traffic billing, TAT-only management, and one nonce-named temporary Security Group with zero inbound rules and exactly one priority-1 IPv4 `0.0.0.0/0`, protocol `ALL`, allow outbound rule. Shared VPC/subnet `vpc-piootcsf` / `subnet-6uvqq9wg` may be selected after read-only confirmation and must not be changed. Production Lighthouse `lhins-3c2vknjb`, `cwt-production-sg`, DNS, COS and unrelated resources remain outside scope.

The Owner reported that the previous disposable resource was verified and its destruction was selected. After the ordinary Tencent login completed, fresh authenticated reads at `2026-09-08 18:58 +0800` independently confirmed the Singapore inventories at **zero CVMs, zero cloud disks, zero public IPs and zero Security Groups**. The current purchase form selected Singapore Zone 2 and showed exact `SA5.MEDIUM4`, 2 vCPU / 4 GiB, AMD EPYC Bergamo, at `库存充足`; public `Ubuntu Server 24.04 LTS 64位` remained exact image `img-mmytdhbn`, and one 60 GiB general-purpose SSD was selectable. Existing `vpc-piootcsf` / `subnet-6uvqq9wg` remained available with 4,093 subnet IPs. One ordinary BGP public IPv4 with traffic billing and 5 Mbps cap was selectable, and free Automation Assistant was checked with its no-password/no-SSH command-execution description. The exact current display was CNY `0.41/hour` base plus CNY `0.79/GB` outbound traffic. The purchase page's default new-Security-Group selection still enables public inbound rules and remains unsuitable; the separately defined zero-inbound temporary group is mandatory. No purchase confirmation was entered. Before any later purchase, reread these volatile fields and stop if any attempt-owned residue exists or any material field differs. At the fresh quote, 90-minute base exposure is CNY `0.615`; a conservative 5 GB allowance produces CNY `4.565`, within the standing CNY 10 maximum estimated exposure. The 90-minute lifetime remains the effective cost control because Tencent does not expose a per-attempt cap here.

Provisioning uses the existing 19,915-byte script at SHA-256 `86a3ceb9b53fad72e87b9676ff1c2ebef2ad2235bd4d04380de9acaf04c856f0` and invocation contract SHA-256 `98d67f285e0fd5afb57a6a4b7227c57b6214cdb8992d44a552d4059ba81e41b1`: execute as `root` from `/root`, timeout 600 seconds, no parameters or credentials, COS output disabled, and require terminal `SUCCESS` plus exit `0`. A missing initial TAT Agent may be completed only on the same host through VNC and the official Tencent installer before provisioning; it does not reset the host clock or count as the one recovery.

Registration uses the reviewed 3,785-byte launcher at SHA-256 `8722709d99d918ea3c52d020971b8a2c80cbc993b2b0df0ce0cf5c1d5ac0cf0d`, decoded 6,938-byte wrapper at SHA-256 `a3309b5ea90710f44ff75ec1d75289f932753e1f81050dbffa22e10d9bb2168f`, and complete invocation file at SHA-256 `ef0c788e8f38ffe0bbae2f8568de73d317b62077c3d2dc62e791799085c53396`. The form must show `RunCommand`, `SHELL`, parameter substitution enabled, `username=ubuntu`, `workingDirectory=/home/ubuntu`, timeout 600, `saveCommand=false`, and COS output disabled. Generate one repository-scoped short-lived token only after provisioning PASS. Copy it in the authenticated GitHub browser directly into Tencent's existing hidden `CWT_REGISTRATION_TOKEN` parameter; immediately replace the clipboard with the non-secret nonce before submission follow-up, screenshots or accessibility capture. Never display, read back, save, pipe or place the token in command text. The revoked TCCLI/OAuth path, SDK credentials, loopback helpers and request files are not fallbacks.

The lifecycle clock starts at CVM creation. Target initial TAT readiness and provisioning by host age 20 minutes, token/registration and Runner binding by 25 minutes, and dispatch/Environment approval by 30 minutes. One same-host PREPARATION recovery is admissible only after a terminal preparation failure, before GHCR authentication, OCI materialization or validator start, with all accepted clean-state checks and at least 45 minutes remaining before the teardown-start ceiling; because teardown must start by age 80 minutes, admission closes at age 35 minutes. It keeps the same host, nonce, Runner name and GitHub run, uses a fresh registration token if needed, and never permits a second dispatch or second validator start. At host age 60 minutes, cancel an active validation and begin cleanup to preserve teardown margin. Begin teardown immediately on any terminal outcome and no later than age 80 minutes; finish and prove absence of the exact Runner, CVM, disk, ordinary IP and temporary Security Group by age 90 minutes. If destruction identity validation is unavailable or its request is not accepted by age 70 minutes, mark the lifecycle `AT RISK` and continue cleanup as the only activity.

A native PASS would close only the exact c9 isolated Linux Runtime result. It would not prove current COS private policy/durability/credentials, protected Staging restore/access/start, actual recovery timing and capacity, the independent non-SMTP monitoring transport, external vulnerability-feed clearance, Owner-approved database-loss and whole-host RPO/RTO, S6-06/S6-07 closure, or whole-Stage 6 acceptance/freeze. Those remain separate Provider/recovery/operations gates. This preparation performed no Push, CI/Runtime dispatch, Environment approval, Token creation, Tencent purchase or mutation, TAT invocation, Runner registration, private Product pull, Product execution, Registry write, deployment, DNS/data action or acceptance transition.
