# CWT Phase 1B Stage 4A Phase D FONT-HERMETIC Superseding Exact Design V1.0

Status: **S1 EXACT DESIGN CANDIDATE / OWNER-AUTHORIZED FOR FRESH INDEPENDENT DESIGN/SECURITY REVIEW ONLY / DECISIVE BUILD NOT RUN / IMPLEMENTATION PROHIBITED / PHASE D NOT ACCEPTED**

Design version: `1.0`

Prepared: `2026-08-15` (`Asia/Shanghai`)

Owner authorities:

- [Phase D Synthetic-Only Boundary Owner Decision V1.0](./PHASE_1B_STAGE4A_PHASE_D_SYNTHETIC_ONLY_BOUNDARY_OWNER_DECISION_V1_0.md), exact Git blob `1ed9558ca539176dfeccd6bf8466524757f66d1c`
- [FONT-HERMETIC Technical Escalation Owner Decision Amendment V1.0](./PHASE_1B_STAGE4A_PHASE_D_FONT_HERMETIC_TECHNICAL_ESCALATION_OWNER_DECISION_AMENDMENT_V1_0.md)

Architecture: [ADR-0020](./adr/ADR-0020-phase-d-synthetic-only-bounded-convergence.md)

Verified escalation inputs:

- [Replacement and Convergence Plan V1.0](./PHASE_1B_STAGE4A_PHASE_D_FONT_HERMETIC_REPLACEMENT_AND_CONVERGENCE_PLAN_V1_0.md)
- [Technical Escalation Root-Cause Analysis V1.0](./PHASE_1B_STAGE4A_PHASE_D_FONT_HERMETIC_TECHNICAL_ESCALATION_ROOT_CAUSE_ANALYSIS_V1_0.md)

Author evidence:

- [Design Proof Result V1.0](./review-evidence/phase-1b-stage4a-phase-d-font-hermetic-superseding-exact-design-s1-v1/DESIGN_PROOF_RESULT_V1_0.json)
- [S1 Author Manifest V1.0](./review-evidence/phase-1b-stage4a-phase-d-font-hermetic-superseding-exact-design-s1-v1/MANIFEST_V1_0.json)

## 1. Exact identity, objective and supersession

The immutable source and rollback base is:

```text
ref=refs/heads/codex/checkpoint/phase-1b-stage4a-phase-d-pre-formal-proof-replacement-v1
commit=fbe88cdd7639f32f48d92a0627833918b4924458
tree=2525cf1b99b5a99986a959c357ce58235308e18e
classification=SOURCE_AND_ROLLBACK_BASE / NOT_CANDIDATE
```

S1 is one docs/evidence-only sole/direct child of exact `fbe`. It carries the verified `ce55eab9261a06a1b6cc551f7b6132617c6a1b01` analysis/plan artifacts byte-identically but does not inherit `ce55` as ancestry. S1 is not G11 and not Attempt 4.

This Design is the single forward implementation instruction. G3–G10 commits, reviews and evidence remain immutable custody/history; they are not cumulative execution documents. The following are retained only as consolidated semantics:

1. the exact synthetic-only Owner boundary;
2. G6's official typegen → atomic hold → fresh Build → restore lifecycle and zero-retry rule, without inode/timestamp/polling ceremony;
3. G9's synthetic environment, package/workspace/checker convergence, numeric-origin semantics, exactly two runtime outputs, exactly four new Phase D bespoke probes, inherited-control preservation and fresh ten-gate/no-retry sequence; and
4. the independently reviewed G10 `next/font/local` source transform plus exact Geist asset/provenance/license material.

Retired from forward authority are all Google font mock servers/counters, copied-font maps, `NEXT_FONT_GOOGLE_MOCKED_RESPONSES`, CSS URL grammars/parsers, loader-only probes, ambient source-root package-byte prerequisites, Design-level pnpm/package scripts, Webpack/custom/internal runners, full-clone/inode/timestamp/polling/cache ceremony, repeated predecessor constants, per-gate logs, dependency/build-cache inventories, compatibility fallbacks and retries. No failed Product/source byte may be cherry-picked, checked out or copied; S2 implements this specification fresh.

## 2. Phase D boundary and authority classifications

Eligible Phase D proof remains deterministic and synthetic only: adapter contract fixtures, fake/synthetic application and Prompt tests, the retained exact numeric-`127.0.0.1` injected-fetch transport test, static architecture/security/privacy validation, the local-font Build property and ordinary existing gates.

The following remain `NOT_RUN / DEFERRED_TO_PHASE_F`, never PASS:

- real/external Secret or credential ingress;
- real/external database access;
- Provider/API request, response, cache, cost or latency;
- live official-source reads;
- DNS, non-loopback or external network egress; and
- protected Staging validation.

The affected validation database credential remains `ROTATION_REQUIRED_BEFORE_ANY_REUSE`. S1/S2 may not inspect, reuse or rotate it. Supplier assurances remain unresolved Owner-accepted residual risk and are not PASS.

The existing adapter runtime and future server composition remain byte-identical and `FUTURE_PHASE_F_RUNTIME_ONLY`: default-off and unreachable from every Phase D acceptance root. The retained adapter Node transport test may bind/connect only numeric `127.0.0.1` on an ephemeral port, uses an explicit synthetic credential reader and injected fetch, performs zero DNS/external egress, and verifies redirect, actual Node headers and transport-failure semantics. It is not Provider evidence.

## 3. S2 exact closed implementation allowlist

After independent S1 PASS and Coordinator acceptance, one fresh S2 whose sole/direct parent is the exact accepted S1 may change exactly these 22 paths relative to S1. The action is binding; no 23rd path exists.

| # | Path | Exact action |
|---:|---|---|
| 1 | `package.json` | modify only the script substitution in §4.1 |
| 2 | `pnpm-workspace.yaml` | prepend only the root-only declaration in §4.2 |
| 3 | `scripts/validate-deepseek-text-adapter.test.ts` | delete whole file |
| 4 | `scripts/validate-deepseek-text-adapter.ts` | delete whole file |
| 5 | `scripts/verify-ai-architecture.ts` | fresh synthetic-only checker convergence and exact accepted-S1 binding in §4.5 |
| 6 | `src/ai/testing/controlled-provider-validation.test.ts` | delete whole file |
| 7 | `src/ai/testing/controlled-provider-validation.ts` | delete whole file |
| 8 | `src/app/api/admin/upload-intents/[token]/route.test.ts` | exact numeric-origin correction in §4.6 |
| 9 | `src/app/api/conversion-events/route.test.ts` | exact numeric-origin correction in §4.6 |
| 10 | `src/app/api/upload-intents/[token]/route.test.ts` | exact numeric-origin correction in §4.6 |
| 11 | `src/app/api/upload-intents/route.test.ts` | exact numeric-origin correction in §4.6 |
| 12 | `src/integrations/ai/providers/deepseek-official-source-preflight.test.ts` | delete whole file |
| 13 | `src/integrations/ai/providers/deepseek-official-source-preflight.ts` | delete whole file |
| 14 | `src/integrations/ai/providers/deepseek-text-adapter.test.ts` | fixture-owned synthetic contract transform in §4.4 |
| 15 | `test-fixtures/ai-architecture/graph-faults.phase-d.v5_0.json` | delete as rename source |
| 16 | `test-fixtures/ai-architecture/graph-faults.phase-d.synthetic-only.v1_0.json` | add as the one active profile in §4.5 |
| 17 | `test-fixtures/ai/deepseek-controlled-validation.v1.json` | delete whole file |
| 18 | `test-fixtures/ai/deepseek-synthetic-contract.v1.json` | add canonical synthetic fixture in §4.3 |
| 19 | `src/app/layout.tsx` | exact `next/font/local` transform in §4.7 |
| 20 | `src/seo/nonproduction-indexing.test.ts` | font mock-boundary synchronization only in §4.8 |
| 21 | `src/app/fonts/geist-latin.woff2` | add exact verified bytes in §5 |
| 22 | `src/app/fonts/geist-mono-latin.woff2` | add exact verified bytes in §5 |

No dependency, lock, Next config, global CSS, public raw-font route, extra license file, Build/proof runner, output schema, Schema/Migration, Product/API contract, URL/Redirect, SEO metadata, publishing/Index or Product-data change is permitted.

## 4. Complete text-path transforms

Every transform starts from exact S1, whose non-doc tree equals `fbe`. Historical G3–G10 source objects are conformance evidence only and must not be used as a copy source.

### 4.1 `package.json`

From exact `fbe` blob `15f1fe1b18832edf4b64854d34e2249baa6c74ab`, delete only:

```json
"ai:validate:deepseek": "tsx --conditions=react-server scripts/validate-deepseek-text-adapter.ts --execute-controlled-real-provider-validation"
```

and insert at that exact script-list position only:

```json
"check:ai-phase-d-synthetic": "vitest run src/integrations/ai/providers/deepseek-text-adapter.test.ts src/integrations/ai/providers/deepseek-text-adapter.node-fetch.integration.test.ts src/integrations/ai/providers/deepseek-pricing.test.ts src/ai/testing/synthetic-application/synthetic-application.test.ts src/ai/testing/synthetic-prompts/synthetic-prompt.test.ts"
```

Every other script, dependency, devDependency and metadata byte remains unchanged. `check:ai-prompts` remains `tsx scripts/verify-ai-prompt-bundle.ts --include-synthetic-test`; `pnpm-lock.yaml` remains byte-identical to `fbe`. Expected target identity: 3,605 bytes, Git blob `31ab568ba44a3a12ae75a9b503cbf9b9792360e1`, SHA-256 `21d7b63e54e802c26765ba4ea759aebdae6f6e4f446265452f2a52e613536a83`.

### 4.2 `pnpm-workspace.yaml`

From exact `fbe` blob `e100040ccf7e5371b18cc0db393030ca7cfb1b0d`, prepend exactly these 18 UTF-8 bytes before the existing first byte, with no blank line:

```yaml
packages:
  - "."
```

All original bytes follow unchanged. This declares the already intended root-only workspace and does not make `test-fixtures/ai-server-bundle` a workspace project. Expected target identity: 872 bytes, Git blob `57b83a4a3a9db2e15002e56f408f2f91ff7ac140`, SHA-256 `735fc96d5067d9260e40aadfd04f8f4eb6ecb5f17354003d34272f845940cf10`, parsed `packages=["."]`.

### 4.3 Synthetic contract fixture

Add one canonical JSON object with `schema="cwt.phase-d.deepseek-synthetic-contract"`, `version=1` and `classification="SYNTHETIC_TEST_DATA_NOT_CWT_FACT"`. Its request is version 1, model `deepseek-v4-flash`, empty input, instructions `Return exactly one JSON object.`, JSON-object response format `synthetic.output` version 1, `maxOutputTokens=64`, empty parameters, estimated input tokens 543, and the exact outbound body: model, system/user messages, disabled thinking, `stream=false`, JSON response format, `max_tokens=64`, `temperature=0`.

The `cases` object is complete and contains:

- configuration failures: unsupported `synthetic-unsupported-model`; simultaneous `temperature=0.5/top_p=0.5`; `temperature=2.1`; `top_p=-0.1`; and parameter `endpoint="synthetic-disabled-value"`, mapped respectively to `model_unsupported` or `parameters_invalid`;
- HTTP cases `400,403,404,409,422 → client/client_error/not_retryable`, `408 → timeout/timeout/same_provider_transient`, `429 → rate_limited/rate_limited/same_provider_transient`, `500 → server/server_error/same_provider_transient`, `401 → authentication/client_error/not_retryable`, `402 → quota_exceeded/quota_exceeded/not_retryable`, and `302,204 → unknown/unknown/not_retryable`;
- invalid-response mutations exactly `extra_service_tier`, `unknown_top_level`, `reasoning_content`, `usage_totals`, `tool_calls`, `second_choice`, `unknown_finish_reason`;
- body failures: empty, malformed `{`, duplicate-key JSON, invalid UTF-8 byte `255`, 1,048,577 repeated `x` bytes, and thrown transport; expected codes/retry classes are respectively `empty_response/not_retryable`, three `invalid_response_json/not_retryable`, `response_too_large/not_retryable`, and `transport/same_provider_transient`;
- partial body prefix `{"id":"partial"` → `transport/same_provider_transient`; timeout 120,000 ms → `timeout/same_provider_transient`; finish reasons `length → length_limit` and `content_filter → content_filter`;
- transport boundaries: already-aborted reason `synthetic_cancel` → `cancelled_no_response/not_retryable`; `insufficient_system_resource` → `server/same_provider_transient`; one-shot maximum fetch count 1 with second execution `failure/not_retryable`; redirect maximum destination hits 0 and `302/unknown/unknown`; and
- one success response `synthetic_response_01`, model `deepseek-v4-flash`, fingerprint `synthetic_fp_01`, output `{"safe":true}`, complete completion, and usage input/output/total/cache-hit/cache-miss `10/4/14/6/4`.

The fixture contains no credential/placeholder, environment key/value, database identity/query, filesystem path, account/project identity, official-source content or timestamp. Emit deterministic key ordering with one final LF. The accepted semantic conformance oracle is 5,201 bytes, Git blob `1082d3cb3449118b259177601b13e43bdc22587d`, SHA-256 `01a41b23f31667b158d57c81f77957e5da4648219a385c83dda81cd6c79cac34`; this identity is a validator, not permission to copy a failed source object.

### 4.4 Adapter unit-test convergence

Starting from exact `fbe` test blob `9c5c310f21be2f68d8d6312a35936f71530ac760`, preserve the adapter behaviors and replace duplicate inline case data with the fixture as the sole case authority:

1. default-import the fixture from `../../../../test-fixtures/ai/deepseek-synthetic-contract.v1.json` and define a read-only TypeScript shape covering every §4.3 field;
2. validate schema/version/classification and request identity before use; build the Provider-neutral request from fixture data only;
3. use only explicit in-memory fetch and `synthetic-test-value-phase-d-01` credential-reader seams; do not execute default fetch or the default credential reader;
4. table-drive every configuration, HTTP, body, timeout, finish-reason, abort, one-shot, redirect, success/usage/cache and invalid-response case in §4.3;
5. retain fail-closed invalid credential, Prompt contract, envelope hash, conservative estimate, request-header/body, single-dispatch and response normalization assertions; and
6. implement the seven named invalid-response mutations deterministically and fail on an unknown mutation name.

No Product adapter byte changes. No external request or Provider fixture. The accepted semantic conformance oracle is 17,163 bytes, Git blob `a82f31ed8b981190d93c00226c16e6e3d4fc0617`, SHA-256 `9420547d503a0a0d1c4ac2f540af909214e432d00b3e6151f4e0867a1845476a`; it validates a fresh implementation and is not a copy source.

### 4.5 One synthetic-only profile and checker

Delete the V5.0 profile path and add exactly one active profile at `test-fixtures/ai-architecture/graph-faults.phase-d.synthetic-only.v1_0.json`. Starting from the `fbe` profile semantics, preserve every non-conflicting inherited Phase B/C actual-tree, classification, generated/Prompt resource, TypeScript-symbol capability-origin, database-type/read-authority, protected/server/public-bundle and self-challenge control. Replace only Phase D controlled-real authority with this closed contract:

- acceptance roots are only the exact synthetic package command and its five named deterministic test files; no retired CLI, controlled validator, live-source preflight, future composition or Worker CLI is an acceptance root;
- all eight deleted paths are required absent; adapter/Product/future-runtime paths retained outside the allowlist remain byte-identical to `fbe`;
- bind exact package/workspace/lock, fixture, profile, checker, executable-tree and Candidate identities; reject symlink, hard-link, canonical/case escape, special node, unresolved edge or unknown executable/resource node;
- retain exact numeric `127.0.0.1` bind/destination, injected fetch and explicit synthetic credential-reader qualification for the Node transport test;
- classify `src/server/ai/phase-d-provider-composition.ts` and `scripts/process-ai-runs.ts` as `FUTURE_PHASE_F_RUNTIME_ONLY`, default-off and unreachable from Phase D roots; and
- retain inherited Prompt/foundation/architecture controls without counting them against the new-probe ceiling.

Exactly four new Phase D bespoke mutation-probe classes exist:

1. reintroduce or make reachable the old/public real-validation CLI;
2. import database or real-environment authority from a Phase D acceptance root;
3. execute the adapter default credential reader or default fetch from Phase D acceptance; and
4. import future runtime composition/Worker CLI into Phase D acceptance.

Author the one profile fresh from the exact `fbe` V5 profile plus the complete current contract above. It must bind the current accepted S1 Design/parent authority at implementation time and must not carry G3–G10 Design paths, lineage constants, failed-Candidate identities or retired execution mechanics. Historical G9 profile identity is custody evidence only, not a target-byte or copy authority; semantic preservation is decided by the current contract, inherited-control set identity and fresh checker self-challenges.

Freshly converge `scripts/verify-ai-architecture.ts` from exact `fbe` blob `e7127698c2d28389c3ed7f1a29dc3ca0f1ae89bb` to compile only that one profile and the §4.3 fixture, enforce the complete contract above, remove controlled-real/live-source exceptions and obsolete V5 outputs, and emit exactly these two bespoke runtime/proof files:

```text
PHASE_D_SYNTHETIC_ONLY_REACHABILITY_SECURITY_RESULT_V1_0.json
PHASE_D_SYNTHETIC_ONLY_REACHABILITY_SECURITY_MANIFEST_V1_0.json
```

The result contains only acceptance roots, retired-path absence, loopback constraints, explicit synthetic seams, future-runtime isolation, external/database/environment non-reachability, inherited-control preservation, package/lock delta and the four probe results. The manifest binds the result, exact S2 commit, executable-tree, checker, profile, fixture and profile-integrity hashes and excludes a self-hash. Emission is atomic into one absent directory; timestamps, host-private paths and partial outputs are forbidden.

Aggregate mode must first require a clean worktree and exactly one parent (`parentLine.length === 2`), then require that sole parent to equal the exact Coordinator-supplied accepted S1 40-lowercase-hex commit. That binding identifier occurs exactly twice and its associated diagnostic once; no G3–G10 or `fbe` fallback parent constant remains. This accepted-S1 binding is why no final checker hash is frozen in S1. Every other change must be necessary to the stated convergence; no second scanner, profile, schema, collector, compatibility path or lower-level emission entry is allowed.

### 4.6 Four route-test numeric-origin corrections

In each named test, import `env` from `@/config/env` if not already imported and define exactly:

```typescript
const TEST_SITE_ORIGIN = new URL(env.NEXT_PUBLIC_SITE_URL).origin;
```

Use that same value for both the Request URL base and `Origin` header:

- admin token PUT helper: `new URL(`/api/admin/upload-intents/${token}/`, TEST_SITE_ORIGIN)` and `origin: TEST_SITE_ORIGIN`;
- both Conversion Event POST cases: `new URL("/api/conversion-events/", TEST_SITE_ORIGIN)` and `origin: TEST_SITE_ORIGIN`;
- public token PUT case: `new URL("/api/upload-intents/test-token/", TEST_SITE_ORIGIN)` and `origin: TEST_SITE_ORIGIN`; and
- public Upload Intent POST case: `new URL("/api/upload-intents/", TEST_SITE_ORIGIN)` and `origin: TEST_SITE_ORIGIN`.

This covers the exact nine formerly red and four formerly false-positive cases under accepted `NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3200`. All genuine negative-origin and unrelated assertions remain byte-identical. Do not hard-code `localhost`, treat DNS names as equivalent, or relax Product exact-origin enforcement. Expected target blobs are respectively `06835ff5f00ea6298aeefd985f2e0cce2e22cb79`, `6d1bc69a45ee316b996f68afae826e56bc0620c6`, `fb72e65385435c9f6066892be9eb2d4a2051c153` and `c855c7b34fddee4367d0b070aa6ee19b6e202906` in the table order.

### 4.7 `src/app/layout.tsx`

From exact `fbe` blob `94e46fbf4942bbaa175d98b0e729d893c127c4b0`, delete the `next/font/google` named import and add only:

```typescript
import localFont from "next/font/local";
```

Replace the two Google font calls with exactly:

```typescript
const geistSans = localFont({
  src: "./fonts/geist-latin.woff2",
  variable: "--font-geist-sans",
  weight: "100 900",
  style: "normal",
  display: "swap",
  preload: true,
  adjustFontFallback: "Arial",
});

const geistMono = localFont({
  src: "./fonts/geist-mono-latin.woff2",
  variable: "--font-geist-mono",
  weight: "100 900",
  style: "normal",
  display: "swap",
  preload: true,
  adjustFontFallback: "Arial",
});
```

No `subsets` or manual `fallback` option. Every other byte—Metadata, validated environment, Analytics, global CSS, metadata values, `publicIndexingAllowed`, `<html lang>`, exact `${geistSans.variable} ${geistMono.variable} h-full antialiased` class, body and children—remains unchanged. Expected target SHA-256 is `7d4de1649916159fd4be48127edc674cee4bf821abeb53aec0763cef6f3c2204`.

### 4.8 `src/seo/nonproduction-indexing.test.ts`

From exact `fbe` blob `7647e7e8502755a462aa2c3caf724f41767ea204`, replace only the Google font mock with:

```typescript
vi.mock("next/font/local", () => ({
  default: (options: { variable: string }) => ({ variable: options.variable }),
}));
```

Every import after the mock, every noindex/robots/Sitemap/header expectation and every unrelated byte remains unchanged. No second font mock or metadata relaxation. Expected target SHA-256 is `68b18aa982ce2ddfbb7a7e387dc87da4085a3a3ebbb4269dce7954071c677889`.

## 5. Exact font assets, provenance and public contract

The only source is the verified installed Next `16.2.12` package selected by preflight:

```text
next/dist/next-devtools/server/font/geist-latin.woff2
next/dist/next-devtools/server/font/geist-mono-latin.woff2
```

| Target | Bytes | SHA-256 | Embedded identity |
|---|---:|---|---|
| `src/app/fonts/geist-latin.woff2` | 28,356 | `1b5ebfb3a01a97343ac96873e6d59a8cb285c66012b6a1ac509cb2765e995ba8` | WOFF2 TrueType; Geist / Geist-Regular; v1.401; `wght 100..900`, default 400 |
| `src/app/fonts/geist-mono-latin.woff2` | 31,288 | `b7ac144b394cbd81052d6397ec0c33397977b1d7e9bc095e744e652a378c6fb3` | WOFF2 TrueType; Geist Mono / GeistMono-Regular; v1.401; `wght 100..900`, default 400 |

Both source and target must be physically contained regular, non-executable, non-symlink, single-link files with Git mode `100644`. Parallel `next/dist/esm/next-devtools/server/font/` copies must be byte-identical as package-internal corroboration, not a second authority.

The embedded notice remains:

```text
Copyright 2024 The Geist Project Authors (https://github.com/vercel/geist-font.git)
licenseURL=https://openfontlicense.org
```

Provenance is the Geist Project; redistribution is SIL Open Font License `OFL-1.1`; decoded `OS/2.fsType` restriction flags are all false. Next's package MIT license is not the font license. Any missing/different source, package version, hash, size, decoded name/table/license, physicality or proposed substitute stops `NEEDS_OWNER_DECISION`; do not download or select another font.

The source files remain under `src/app/fonts`, never `public`. The only public delivery contract is Next-generated content-hashed `/_next/static/media/...woff2` referenced by generated local-font CSS. No stable `/fonts/...`, source-path, Object Key or other raw font route is authorized. Public client JavaScript must retain zero protected AI/Prompt/private path or marker exposure.

## 6. The one S1 independent Design/review proof: authority B

The S1 author does not execute this proof. A different fresh independent full Design/security reviewer must run it exactly once in a disposable task-owned root outside the repository. Logical paths only enter evidence; no host-private path is persisted.

### 6.1 Preflight and projection

1. Check out the exact S1 Candidate later supplied by the Coordinator; prove it is clean and its sole/direct parent is exact `fbe`.
2. Locate one full local installed source with exact wanted/virtual lock and all 37 declared direct packages resolving to expected versions within that physical `node_modules` root.
3. Hash native Node `24.14.0`, target `package.json`, wanted lock, workspace file, virtual lock, Next package JSON/public CLI/typegen entry, native SWC package JSON/binary and a sorted direct-dependency identity aggregate.
4. APFS-CoW clone the complete physical installed tree into the disposable root. Do not rebind `.pnpm-workspace-state-v1.json`; pnpm and package scripts have no Design-proof authority.
5. Apply the exact §3–§5 projection only inside the disposable tree. Verify the 22-path projected delta and no checker/config/other Product change.
6. Verify both font sources/targets, physicality, hashes, decoded tables, embedded Geist/OFL notice and zero Product occurrence of `next/font/google`, Google mock env/host/map/parser/server/cache/loader machinery.
7. If no exact full installed source exists, stop `BLOCKED`; do not install, download or construct a partial/content-addressed substitute.

### 6.2 Synthetic prerequisites and G6 semantic lifecycle

Construct the environment from scratch with only `APP_ENV=test`, `NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3200`, accepted non-production noindex behavior, task-owned PGlite and isolated Public/Private/Import roots, and a conspicuously synthetic auth value. Remove proxy/registry/Provider/API/credential/external-DB authority.

Using hash-bound native Node and the project-local `tsx` entry, apply unchanged migrations `0000..0020` exactly once to the initially absent/empty task-owned PGlite directory; do not seed. This is disposable validation setup, not real/external DB validation and not Schema authority.

Run typegen exactly once:

```text
node node_modules/next/dist/bin/next typegen .
```

Require `next-env.d.ts` plus exactly `.next/types/cache-life.d.ts`, `.next/types/routes.d.ts` and `.next/types/validator.ts`, with no extra generated-root file. Atomically rename the generated `.next` root to an initially absent same-filesystem hold, leaving repository `.next` absent for a fresh Build. This is the retained functional G6 lifecycle; inode/timestamp/polling/cache-state ceremony is not required. Any failure stops before Build.

### 6.3 Exact localhost-only IPC sandbox

Before Build, under the same macOS sandbox policy, run one minimal Node self-connect probe:

```scheme
(version 1)
(allow default)
(deny network*)
(allow network-bind (local tcp "localhost:*"))
(allow network-inbound (local tcp "localhost:*"))
(allow network-outbound (remote ip "localhost:*"))
```

The probe binds numeric `127.0.0.1` at port `0`, discovers the ephemeral port, connects to numeric `127.0.0.1`, exchanges one fixed marker and closes. Hostname/DNS connection, non-loopback bind/connect, fixed/external port or relaxed policy is prohibited. Profile parse or self-connect failure stops before Build and does not authorize retry.

### 6.4 One actual default-Turbopack Build and focused observation

Under the unchanged environment and sandbox, execute exactly once:

```text
node node_modules/next/dist/bin/next build
```

Require native Node and public-CLI hashes to match preflight, banner `Next.js 16.2.12 (Turbopack)`, zero `--webpack`/`TURBOPACK=0`/wrapper/package manager/internal runner, exit zero and `BUILD_ID` present. Prove exactly two distinct emitted WOFF2 files under `/_next/static/media/` match the two input byte counts and SHA-256 values; generated CSS references only those hashed local assets; no Google/native-Google request, mock response, proxy, DNS/egress or raw source-font public route occurs; and application-controlled public-bundle/security boundaries remain compatible.

Only after Build PASS, run exactly once through the same hash-bound native Node:

```text
node scripts/check-public-bundle.mjs
```

This is focused Design-review observation only, never S2 Gate 9 evidence. Any Build or focused-check failure is `FAIL/INCOMPLETE`; do not rerun, switch compiler, use a loader probe or alter the profile. After observation, atomically hold the Build root and restore the exact original typegen root; a restore failure is `FAIL/INCOMPLETE`.

Design proof B does not prove pnpm currentness, a package script, `pnpm build`, any S2 gate, C2 or Phase D acceptance.

## 7. Fresh S2 implementation sequence and ten gates

S2 repeats all authoritative work from zero. No prior diagnostic, G3–G10 result, S1 Design Build or earlier gate result is reusable.

### 7.1 Dependency/currentness boundary

1. CoW-clone one verified full installed tree into the task-local S2 root.
2. In clone-local `.pnpm-workspace-state-v1.json`, rebind only the single canonical project-root key to S2; preserve every other semantic value and source-tree bytes.
3. Construct an explicit environment omitting `CI` and every `enableGlobalVirtualStore` override; set `pnpm_config_verify_deps_before_run=error`.
4. Run exactly one zero-Build marker:

```text
pnpm exec node -e 'process.stdout.write("PNPM_CURRENTNESS_OK\\n")'
```

Require exactly one marker and zero non-volatile workspace-state delta after normalizing only `lastValidatedTimestamp`. Failure blocks; no install, warning mode, alternate tree, further metadata edit or retry is authorized.

### 7.2 Functional lifecycle and exact gates

Prepare one fresh synthetic environment and migrated no-seed PGlite fixture as in §6.2. Run `pnpm exec next typegen .` exactly once under error-mode currentness; require the same three-file generated root and `next-env.d.ts`; atomically hold the typegen root. Run Gates 1–10 exactly once and in order:

1. `pnpm check:ai-phase-d-synthetic`
2. `pnpm check:ai-prompts`
3. `pnpm check:ai-architecture`
4. `pnpm typecheck`
5. `pnpm lint`
6. `pnpm test:ai-foundation`
7. `pnpm test:run`
8. `pnpm build` — unchanged package script, pinned Next `16.2.12` default Turbopack, same verified localhost-only/external-denied sandbox
9. `pnpm check:bundle`
10. exact allowlist, `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, dependency identity, clean state and sole-parent integrity

Gates 1–7 begin with the official typegen root active; after Gate 7 it is atomically held so Gate 8 starts with an absent/fresh `.next`. Gate 8's Build root stays active through Gates 9–10. After Gate 10 PASS, atomically hold the Build root and restore the exact original typegen root before aggregate emission. Only the functional hash/file-set/atomic-hold/restore facts are required; do not recreate G6–G9 inode/timestamp/polling/Vite/cache metadata state machines.

Only after prerequisites, all ten gates and restore PASS may the unchanged package-script authority emit the two §4.5 outputs exactly once into one absent C2 evidence directory. The aggregate cannot manufacture or convert a gate result. C2 is one docs/evidence-only sole/direct child of S2 and may be created only after full PASS. Any prerequisite, gate, restore or aggregate failure stops without retry, partial rerun, resume at Gate 8, alternate runner or C2.

## 8. Evidence ceiling and role separation

S1 author evidence is exactly this compact Design plus the Owner/ADR artifacts, one `DESIGN_PROOF_RESULT_V1_0.json` with every decisive Build field `NOT_RUN` / `AUTHOR_NOT_AUTHORIZED`, and one manifest. The six escalation artifacts are immutable custody inputs, not new proof output.

The fresh independent S1 reviewer may add only one review report, one `INDEPENDENT_REVIEW_RESULT_V1_0.json` and one manifest. The result records preflight, one typegen, one self-connect, one Build, local-font emission, focused public-bundle observation, restore and review disposition; it does not retain raw logs.

Future S2/C2 retains only the two checker outputs, one concise C2 report and one manifest after full PASS. A different implementation/security reviewer may add one report, one result JSON and one manifest.

No raw logs, full `node_modules` inventory, `.next`/Build cache, PGlite database, storage fixture, host-private path, environment dump, credential value, repeated per-gate evidence or extra result schema may enter Git.

The Design author cannot self-review or accept S1. The implementer cannot self-review S2/C2. Review PASS is not Coordinator acceptance, checkpoint movement, Deploy, Push or Phase acceptance.

## 9. Rollback, stop conditions and terminal pause

Rollback before S1 acceptance discards the docs-only S1 Candidate. After S1 but before successful S2/C2, discard S2 and, if the Owner revokes this decision, revert/discard S1 to exact `fbe`. After C2, revert/discard C2, S2 and S1 in reverse authority order; never rewrite historical commits. Task-owned dependency, generated, PGlite, storage and Build resources contain no business data.

Stop `NEEDS_OWNER_DECISION` for any 23rd path; dependency/lock/config/Schema/Migration/Product/API/SEO/URL/Redirect/publishing/Index expansion; unavailable or mismatched font/license; alternate install/snapshot/materialization authority; broader network capability; real/protected/external input; attempted G3–G10 source reuse; extra profile/probe/output; or Phase E/F action.

Stop `FAIL/INCOMPLETE` without retry for typegen, loopback preflight, the one default-Turbopack Build, font emission, focused public-bundle observation, any S2 currentness/gate, restore or aggregate failure. A repeated FONT-HERMETIC causal root returns to Technical Escalation/Owner governance; it is not renamed or bypassed.

After a different fresh implementation/security review returns PASS and the Coordinator accepts/checkpoints the exact Phase D Candidate, the project pauses. Phase E/F requires new explicit Owner authority.

```text
FONT-HERMETIC-01 = OPEN_UNPROVEN
S1 DESIGN PROOF = NOT_RUN / AUTHOR_NOT_AUTHORIZED
S1 ACCEPTANCE = PENDING DIFFERENT FRESH INDEPENDENT FULL DESIGN/SECURITY REVIEW
PRODUCT IMPLEMENTATION = NOT_AUTHORIZED
NEXT GATE = ONE AUTHORITY-B S1 REVIEW PROOF, THEN COORDINATOR DECISION
```
