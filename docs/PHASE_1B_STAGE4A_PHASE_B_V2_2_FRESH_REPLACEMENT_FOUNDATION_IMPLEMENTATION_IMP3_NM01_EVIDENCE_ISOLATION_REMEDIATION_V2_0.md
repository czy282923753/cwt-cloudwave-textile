# CWT Stage 4A Phase B — IMP3-NM01 Evidence-Isolation Bidirectional Closure Remediation V2.0

## Outcome

IMP3-NM01 correction attempt 2 is implemented as a Candidate awaiting Fresh independent re-review. This report does not claim acceptance or self-approval.

The sole V3.1 checker now enforces both sides of the accepted `evidence_only_not_production` boundary. Historical diagnostic executables remain physically inventoried and classified while their outgoing historical acquisitions remain outside the current Production graph. At the same common resolved-edge authority, every current Production source edge into a diagnostic/evidence-only target now fails closed before source-side skipping can hide downstream acquisitions.

## Exact identities

- Failed attempt-1 Candidate: `b48f33f35d5d46824a8c4dec6b40d4a093050285`; tree `15f642e9d910d9625ff786d829986da6cfa7221e`.
- Corrected executable-tree seal: `627779d294e45f1fb166f70b94067c4b811aaaff`; parent `3c9bb6cbebde6b17351c38951c63b47d49a67e19`; tree `f99ef5c47ba4be828a8232682a6ee3a8d3776efd`.
- Executable-tree SHA-256: `a45c8cb0a5d15830d601442ced747067b6718a32f09ccff8c2572f11a4ea06e8`.
- Sole checker SHA-256: `89fc60805358a3aa6e7597e68fd7607e268ea5169e39ff170c970b8ac7119a3b`.
- Immutable historical probe SHA-256: `709129a2eafc4ed284427e1ec0f84f20ee264f13b8993a5dbdd57cefb944bc68`.
- Controlling FAIL report, challenge and manifest remain byte-identical at `1dda53a1...`, `5d806c9e...` / `76f59840...`, and `7d242517...` (`5/5`).

Every newly imported executable Reviewer/challenge/verifier file was committed before the executable seal. Every successor after `627779d...` is restricted to regular non-executable docs, JSON, text, proof and manifest paths. The evidence verifier derives the exact final HEAD, executes source-clean and official-present lifecycle gates, repeats the official-present proof-bound gate in a detached exact-HEAD worktree, compares identities/totals, and independently reproduces the exact Production-to-evidence rejection.

## Root correction

The pre-correction Reviewer challenge was rerun unchanged and reproduced the unsafe `EXIT 0` for a static import from `src/storage/index.ts` to the immutable diagnostic probe. The unresolved Production control continued to reject.

The correction replaces the one-sided test-only target check with one Production target-class ceiling at `enforceCapabilityEdge`. It resolves the target to the already classified canonical/physical node and rejects a Production/current source when that target is test-only, diagnostic-documentation, `evidence_only_not_production`, or documentation-only. Evidence-origin review/probe behavior is unchanged; no reverse prohibition, path substring rule, unresolved allowance, shim, second graph, compatibility resolver or Product-source workaround was added.

Diagnostics identify source path/class, target path/class/stage/bundle zones, rule, AST form, edge kind, resolution kind, specifier and reason. Twelve closure mutations cover direct import, re-export, type-import, alias resolution, transitive Production sources, an independent diagnostic target, unresolved Production, selector exclusion, evidence promotion, post-seal drift, stale proofs and compatibility targets. The inherited 28 lifecycle/classification mutations and physical symlink/hard-link/canonical-alias checks remain unchanged.

## Final executable-tree and proofs

The completed Candidate is expected and independently verified at two exact lifecycle totals:

- source-clean: 668 candidates / 478 executables, `next-env.d.ts` absent;
- official-present: 669 candidates / 479 executables, `next-env.d.ts` present.

Both official-present attached and detached gates bind executable-tree SHA-256 `a45c8cb0...`, graph SHA-256 `fe82232a...`, 2,270 edges, zero unclassified/ambiguous/current-unresolved nodes, 21 ordinary URL values, 69 fault/topology cases, 10 positives, 28 inherited mutations and 12 current closure mutations. The five canonical V3.1 proofs bind executable seal `627779d...` and the same executable-tree identity.

## Verification

- All `src/ai`: 14 files / 163 tests PASS.
- Fresh H-02 real-service matrix: 2 files / 7 tests PASS; H-02 remains closed.
- Focused DB/public/SEO: 5 files / 10 tests PASS.
- Phase B DB/schema: 2 files / 3 tests PASS.
- AI foundation Candidate verifier: PASS; independent schema mapping: `ai_model_config` 21/21 and `ai_runs` 96/96 exact order PASS.
- Prompt bundle/history: PASS; Production Prompt manifest and Provider registry exact-empty.
- Full lint with zero warnings and strict TypeScript: PASS.
- Official Next 16.2.12 typegen under network denial: PASS; exact 247-byte `next-env.d.ts` and three generated-root hashes PASS.
- Isolated network-denied webpack server/public fixture: PASS, 51 server files / 16 client chunks, marker and positive-leak controls PASS.
- Real post-seal executable mutation: FAIL CLOSED with `post-proof executable candidate drift`.

The unrelated exhaustive application suite was not run because the only Product-code change is the architecture checker. All AI and every affected authorization, DB, public/SEO, schema, Prompt, type, lifecycle and bundle boundary were exercised. The unchanged Google Font offline debt remains disclosed; no unrelated full public build is claimed.

## Scope, rollback and next gate

There is no `src/**` runtime/business, graph fixture, DB, Schema, Migration, config, package/lock, Prompt body, Product, public, SEO, URL, Provider or Phase C/D/E mutation. Rollback is the linear range after failed Candidate `b48f33f...`; that ref and all imported Reviewer evidence remain immutable.

Fresh independent implementation re-review must audit IMP3-NM01 attempt 2, H-01, H-02 and all preserved closures under `REMEDIATION_FINDINGS_REVIEW`, then decide `FULL_REVIEW_NECESSITY` with rationale. This Candidate is not eligible before that review.
